# 2026-08-25 工作日志

## 08:50 商品/课程销量自动同步 (feat)

- **需求**: 商城商品售出后已售实时同步; 课程卖出后同步"多少人学过"
- **根因**: products.sales / courses.students_count 只有后台手动能改, 支付成功后从未自动累加
- **代码**:
  - 新增 `syncSalesAfterPay(order)`: 商品订单→sales+=qty, stock-=qty; 课程订单→students_count+=1
  - 新增 `revertSalesAfterRefund(order)`: 退款回退 (商品 sales-=, stock+=; 课程 students_count-=1)
  - 接入: markOrderPaid(微信回调/查单) + orderPayBalance(元宝) + cancelOrder/courseRefund/adminOrderRefund
  - 新增 `admin.recalcSales`: 统计已支付订单重算销量, **只补不覆盖**(max 语义), 防覆盖人工展示销量
- **补算历史数据**: 5商品(1/3/4/31/36) + 3课程(1/7/10) 已同步
- **⚠️ 教训 (重要)**: 
  1. MCP `writeNoSqlDatabaseContent` 的 update 是**整体替换语义**(非 $set), 只改单个字段必须带全部字段, 否则其他字段被清空! 曾用 `update:{sales:326}` 把商品1/3 清成只剩 sales(从订单+mock.js+探测URL重建恢复)
  2. 商品1/3 的 images: 商品1 用真实 URL(探测 200), 商品3 用占位图(与商品4 同模式)
- **部署**: dy-api 已更新; git 07429a9 已push

## 16:00 APK v1.11.196 构建与下载页部署 (feat)

- **问题**: 小程序已到 v1.11.196，Android APK 停在 v1.11.184（差距 12 个版本）
- **操作**: 
  1. 提升版本号到 v1.11.196 (build.gradle versionCode 286)
  2. `cap sync android` + `./gradlew assembleRelease` → APK 8.9MB
  3. 复制为 `dist/build/h5/apk/daoyuan-v1.11.196.apk`
  4. 下载页 HTML 更新链接指向 CloudBase CDN
  5. MCP auth 设备码认证成功 (`MMET-EXBV`)
  6. `manageHosting(action=upload)` 上传 APK + 下载页 + H5
- **验证**: curl 200 OK，APK 8908383 bytes ✅
- **Git**: b330256 + 7340c87 已 push

## 18:30 首页推荐板块字号修复 (fix)

- **问题**: 首页-推荐板块内容字号所有端都偏大 (标题36rpx/价格39rpx 等), 用户希望只有 PC 端 H5 保持大尺寸
- **根因**: 推荐板块基础样式用了 rpx 大值, rpx 在手机/小程序/APP 等比放大; PC 端有独立 `@media (min-width: 1025px)` px 覆盖
- **修复**: 基础 rpx 改回正常 (标题26rpx/时间22rpx/价格30rpx/动态26rpx), PC @media 块不受影响
- **验证**: H5(115文件) + 小程序已构建部署; Git b669b2a 已 push

## 18:45 版本号统一到 v1.11.196 (用户要求: 一直保持统一)

- **问题**: src/version.js(v1.11.183) / build.gradle(1.11.196) / 下载页(196) / git tag(旧) 四端不一致
- **根因**: gen-version.js 从 git tag 读版本, 但当前 HEAD 无 v1.11.196 tag (旧 tag 183/184 指向旧 commit)
- **修复**: 给当前 HEAD 打 tag v1.11.196 → 重新 gen-version.js → 重建 H5+小程序 → 部署 H5 → push main + tag
- **验证**: 四端统一 v1.11.196 ✅; Git 790782a + tag v1.11.196 已 push

## 19:30 小程序静态资源外置云托管 (用户要求: 不打包进代码包)

- **需求**: 生成小程序时图片/文件放云服务器, 通过 URL 加载, 不打包进小程序代码包
- **方案**:
  - 新增 `src/utils/static-url.js`: `staticUrl('/static/xxx')` → `https://cloud1-d8gs2k9m311f7272f-1464523137.tcloudbaseapp.com/static/xxx`
  - 云托管上传 31 文件到 `/static/`: logo.png / pay-wechat.png / logis/*.png(8) / placeholder/*.png(18), 全部 curl 200
  - 代码引用改造: index.vue+about.vue(logo) / logistics.vue(物流图标) / checkout.vue(支付图标) / mock.js(placeholder)
  - 删除无引用冗余: products/books(384K) / app-logo.jpg(128K) / theme-dark.css(8K)
  - **tabBar 图标保留本地** (pages.json iconPath 微信强制本地文件, 不能外置)
- **结果**: 小程序包 1.9M → 1.1M, static 仅剩 32K tabbar
- **注意**: 小程序真机加载 https 云图需配置 downloadFile 合法域名 `cloud1-d8gs2k9m311f7272f-1464523137.tcloudbaseapp.com` (开发者工具需勾"不校验合法域名")
- **部署**: H5 已部署 /h5/, Git da617d4 已 push

## 19:50 版本号策略更新 (用户明确纠正)

- **旧约定取消**: "日常小改动不升版本号" (2026-08-07 记录) **已废除**
- **新约定 (用户明确)**: **每一次改动都必须升 patch 位版本号**, 无例外
- **操作**: bump-version.js 196→197, 重建 H5+小程序并部署, Git 17fde42 + tag v1.11.197 已 push
- **已同步**: MEMORY.md 版本号策略条目已更新
