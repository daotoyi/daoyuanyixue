# 2026-08-21 工作日志

## PC 端盘道界面活动卡片比例协调 (08-21 凌晨)

- **用户反馈**: PC 端 H5 盘道界面每个活动卡片上下高度太小、左右太宽、图片显示不全
- **根因**: `.pandao-card` 在 PC 上全宽铺满(1200px), 封面 `.pandao-cover` 固定高度 220rpx(宽屏换算约420px), 宽高比 >3:1 扁长条, aspectFill 裁切严重
- **修复** (均在 `@media screen and (min-width: 1025px)` 内, 不影响小程序/手机):
  - index.vue 盘道列表: `display:grid` 两列 + gap 24px, 封面 `aspect-ratio:16/9`(height:auto), 字号 px 放大, 描述两行截断, foot margin-top:auto 对齐
  - detail.vue: 页面限宽 860px 居中, 封面 16:9, 底部报名栏 max-width 860px + translateX(-50%) 居中
  - index.vue 推荐页横滑卡片(直播/盘道/商品/课程): PC 下 width 280px + 图片 16:9, 字号放大
- 构建 H5 已部署, HTTP 200; 提交 fc46dc0; GitHub 已推送
