
## v1.11.164 用户管理表格列调整

- 注册时间独立成列, 位置在道号与VIP之间; 之后新增"最后在线"列
- 最后在线 = last_active_at(心跳) 优先, 回退 last_login_at; 云函数 admin.list users 中将 ms 时间戳转 zh-CN 可读字符串
- 登录凭证列下方的 注册/最近登录 小字移除(信息已独立成列), 在线圆点移到最后在线列
- users-row min-width 1720→2160rpx
- 已部署: dy-api 云函数 + H5 静态托管(111文件) + mp-weixin 构建; 已提交推送 GitHub

## v1.11.165 订单分析双饼图并列 + 售后搜索

- 订单分析: 成交分布(单数+金额)双饼图用 .analysis-pies 容器包裹; 移动端 flex-direction:column 上下排列, PC/横屏(min-width:800px 或 landscape) flex-direction:row 左右并列, 各占 1/2
- 售后管理: 新增搜索框(.as-search-bar), 支持按用户名/订单号过滤; aftersaleRows computed 内先按 keyword 过滤再分组; 带清除按钮
- 注意坑: CSS 误插入 script 区导致 build 失败(Unexpected token), 已移至 style 块
- 已部署: H5 静态托管(111文件) + mp-weixin 构建; GitHub 已推送

## v1.11.166 登录全键盘+预约统计+操作区布局

- 登录页(login.vue): 账号输入框 type=number→text, maxlength=11→50, 支持输入邮箱(@.符号); submit() 已有 isEmail 判断, 服务端 login 支持 phone/email 双登录
- 订单管理: 新增预约订单统计条(.order-appt-stats), orderApptStats computed 从 orders 聚合 order_type=appointment 的单数+金额
- 订单操作区: .w-ops-4 overflow hidden→visible + padding-right 12rpx 防止删除按钮被裁切; 表格 .table 已有 overflow-x:auto 可横滑; 宽屏 op-col 108rpx→60px
- 已部署: H5 静态托管(111文件) + mp-weixin 构建; GitHub 已推送

## v1.11.167 H5复制+菜单对齐+订单分类统计+列宽自适应

- App.vue 全局(H5 #ifdef): page/view/text user-select:text 允许复制; 交互元素(button/input/.btn-p/.op/.pill) user-select:none; 宽屏 .uni-tabbar max-width:1200px/1320px + margin:auto 居中, 与内容面板同宽不平铺显示器
- 订单管理: 统计条从仅预约改为分类统计(orderTypeStats: 商品/课程/AI解盘/预约 各自单数+金额, .order-type-stats flex-wrap)
- 订单表格加 orders-table 类: .tr min-width:1200→0(不强制超宽); 列宽收紧 w-no 320→200, w-user 160→120, w-name flex2 min200→flex1 min140 max300(防商品列空白过宽), w-price 140→110, w-status 170→120; 宽屏 px 适配 op-col 108rpx→60px
- 表头随内容同滚: .table overflow-x:auto 已包含表头和数据行, 横滑时同步
- 已部署: H5 静态托管(111文件) + mp-weixin 构建; GitHub 已推送

## v1.11.168 页面管理重命名+玄学工具开关

- 后台: modules 数组 label 首页管理→页面管理; CSS 注释同步
- 后台: 页面管理模块内新增 我的管理 卡片(settings-card), 含玄学工具开关 show_tools(默认关闭)
- 云函数: SETTINGS_GROUPS 增加 mypage; appPayConfig 查询 mypage 组返回 show_tools(默认 false); fallback 也带 show_tools:false
- 前端 homeCfg ref 增加 show_tools:false; loadHomeConfig 加载 mypage 组; saveHomeConfig 保存 mypage 组
- 用户页 user.vue: import getPayConfig; onShow 中加载配置设 showTools; 玄学工具 panel 加 v-if=showTools
- 已部署: dy-api 云函数 + H5 静态托管(111文件) + mp-weixin 构建; GitHub 已推送

## v1.11.169 东八区时间+订单表排序+订单分析对齐

- 关键发现: 云函数服务器时区=UTC, DB 中所有 toLocaleString 字符串(created_at/last_login_at)均为 UTC, 比北京慢8h
- 云函数新增 msToCn/utcStrToCn helper: ms时间戳或UTC字符串 → 北京字符串 "2026/8/19 11:12:46"
- 用户管理: last_active_at(最后在线) msToCn 转东八区; last_login_at(最近登录) utcStrToCn 同步修正
- 订单管理: created_at utcStrToCn 转东八区(下单时间列); adminList 无筛选时也 orderBy created_at desc
- 订单表格: 新增下单时间列(w-time), 金额/下单时间表头可点击排序(orderSort ref + sortedOrders computed + fmtOrderTime 显示 M/D HH:mm), 默认 created_at desc 最新在上
- 订单分析: 去掉 status 过滤(与订单管理·全部口径一致, 12→37); typeMap 补 appointment(预约)+recharge(充值); 历史无 order_type 订单按 order_no 前缀 TL/RC/AP 归类; 前端 pieColors 加第5色, 标题 成交分布→订单分布
- 前端 orderTypeStats 用 classifyOrder 归类(兼容历史订单)+ 增加充值项
- 移动端 orders-table min-width 1560rpx 强制横滑(表头随内容同滚), 宽屏 min-width 0
- 已部署: dy-api + H5(111文件) + mp-weixin; GitHub 已推送

## v1.11.170 管理员权限分级 + 敏感信息治理 + 数据概览修复

- **敏感信息**: build.gradle keystore 密码明文已推送 GitHub, 修复为 keystore.properties 外部化(已 .gitignore); wxpay_mchid 硬编码改为读配置 c.wxpay_mchid
- **数据概览**: adminDashboard 写死日期 includes('2026-08-04') 导致不更新, 重写为动态计算今日范围精确计数; adminRecentOrders 改 JS 时间戳 _ts 排序修复字符串字典序错乱
- **权限分级(核心)**: 新角色体系 admin(超管)/operator(操作管理员,除用户管理·系统设置外全权限)/viewer(普通管理员,全站只读)/manager(旧管理员向后兼容)/staff(员工不可登录后台)
  - 云函数: requireStaffAllowed 重写——admin 全放行; viewer 仅 admin.* 读操作放行; operator 拒绝 admin.user.* / admin.settings / admin.wxmp; manager 走 MANAGER_ROUTES; staff 拒绝。rank 映射补新角色; publishMoment 豁免名单/nextDaoCode 内部前缀/adminUserCreate 可创建角色均扩展
  - 前端 dashboard.vue: 新增 canWrite/canManageUsers/canManageSettings computed; 各模块写按钮(保存配置/增删改/精选/退款/发货/回复等)统一 v-if="canWrite"; 用户管理列/编辑弹窗/角色过滤仅超管; 系统设置/小程序绑定仅超管; 顶部角色显示映射新角色; 登录页透传真实 role; onMounted 准入判定更新
  - 前台: index.vue/publish.vue/user.vue 三处 isAdmin 判断统一改为 admin/manager/operator 内部角色; 云函数 myCourses 免费看全量课程角色列表同步
- 构建: H5 成功→部署静态托管; dy-api 云函数已部署(权限变更必须重部署); mp-weixin 构建成功; curl 验证 admin.dashboard 无凭证被拦截 / admin.settings.save 未授权被拒
- 提交 2e03196, 已推送 GitHub

## v1.11.171 时间显示优化

- 最后在线列: 去绿点/去 nick-line 包装, 纯文本一行完整显示 "2026/8/19 11:12:46"; 用户表时间列加宽 220→300rpx (users-row min-width 2160→2320)
- 下单时间: fmtOrderTime 显示完整带年份 "2026/8/19 12:30" (M/D HH:mm → YYYY/M/D HH:mm); 订单表 w-time 220→280rpx, 宽屏 130→160px
- 绿点仅保留昵称处
- 提交 ea9b62b

## v1.11.172 邀请弹窗对齐+权限收敛+饼图CSS化+最近订单横滑

- 邀请有礼弹窗(user.vue): PC(≥1025px) popup-sheet 改 left:50%+translateX 居中, max-width 520px, 不再铺满屏幕
- 权限收敛: 去掉"普通管理员"(viewer)角色, 保留"管理员"(manager)=全站只读; operator 仍无用户管理/系统设置
  - 云函数 requireStaffAllowed: manager 分支从 MANAGER_ROUTES 改为 VIEWER_ROUTES 只读; adminUserCreate 白名单去 viewer; viewer 保留 API 兼容
  - 前端: ROLE_LABEL/canManageCourses/visibleModules/用户统计/roleFilterMenu/editRoleOptions/createRoleOptions/onMounted 准入 全部去 viewer; MANAGER_MODULES 常量删除; login.vue 准入去 viewer
- **饼图根因**: 微信小程序不支持 `<svg>` 渲染 → 订单分析饼图空白! 改为 CSS conic-gradient 环形图(pieSegments 加 angle + ringStyle() 生成渐变), H5/小程序通用; 饼图 280→200rpx, legend 紧凑化, pie-wrap 可横滑
- 最近订单: .recent-list overflow-x auto + .recent-row min-width 900rpx, 小程序/手机H5 可左右滑动
- 提交 7943dd1; dy-api 已重部署(manager 只读生效)

## v1.11.173 小程序用户表格修复+角色排序

- **小程序 text 内嵌 view/image 不渲染铁律**: 用户表格 头像(text>image/view)/昵称(text>view)/登录凭证(text>view) 在小程序端全部不显示! 修复: 外层 text→view; 表头 10 个 cell 与数据行其余 text td 统一改 view (text 组件 flex 布局受限也会导致表头背景宽度异常)
- 表头黄色背景兜底: .tr.th 加 width:max-content + min-width:100% 铺满
- 用户排序: usersFiltered 新增 ROLE_ORDER {admin:0,operator:1,manager:2,staff:3,user:4} 角色优先级 + 同角色按道号数字部分排序(ZHS1<ZHS10)
- 提交 bdb92b9; APK v1.11.173 已构建发布: /apk/daoyuan-v1.11.173.apk + /download/ 均 200

## v1.11.174 手机号快捷登录 + 登录设置开关 + 课程分类隐藏

- **登录页改造**: 原"微信一键登录"→"手机号快捷登录"; 小程序端 button open-type=getPhoneNumber → 云函数 phoneLogin(code→换手机号→查账号登录/未注册自动注册道友+尾号); H5/APP 端弹窗输入手机号+密码调 login
- 云函数新增 `user.phoneLogin`: getWxAccessToken→getuserphonenumber→users 按 phone 查/建→绑 openid+刷新 session; ROUTES 注册
- **后台「页面管理」新增"登录设置"开关** show_wechat_login(默认关闭): home 组 settings 保存; appPayConfig 返回; 登录页 onLoad getPayConfig 读取→v-if 控制"微信一键登录"显示(与手机号快捷登录上下排列, wx-login-btn button reset CSS)
- **后台课程管理**: HIDDEN_COURSE_CATES=['八字命理','奇门遁甲','六爻预测','风水堪舆'] 从分类栏过滤(loadCourseCates)
- 验证: user.phoneLogin 路由已上线(fake code → "获取凭证失败" = 路由通); app.payConfig show_wechat_login=false ✓; ⚠️ getWxAccessToken 线上失败待查(小程序后台 AppSecret/IP 白名单, bindWechatPhone 同依赖)
- 提交 6c06900 已推送; APK v1.11.174 已发布 /apk/daoyuan-v1.11.174.apk + /download/ 均 200
- **HTTP 网关格式铁律**: curl 直调 dy-api 必须 `{"action":"xxx","data":{...}}` 结构, 平铺参数会被丢弃(data 取 body.data)
- **AppSecret 修复 (v1.11.174 补充)**: 线上报 40125 invalid appsecret → 用户提供正确 AppSecret `****(已清除)` (config.local.js 里旧值 ba7eed... 无效), 更新+重部署后验证: phoneLogin 已走到 getuserphonenumber 阶段(fake code 报 invalid code = token 链路通)
- **云函数出口 IP 动态**: 两次探测不同 (150.158.212.240 / 111.229.152.51, 上海电信), 若小程序后台开启 access_token IP 白名单需加全部出口 IP; 当前未开启白名单, token 获取正常
