# 2026-08-05 工作日志

# ============ 七项功能修改 (08-05) ============

## 1. App 名称 → 道元易学 ✅

- 19 个文件全局替换 (src/scripts/mobile/capacitor.config.json)
- **注意**: Android 应用名在 mobile/android/app/src/main/res/values/strings.xml (Capacitor add 时生成, sync 不更新) → 手动 sed 替换后重建 APK, aapt 验证 application-label='道元易学'

## 2. App 图标替换 ✅

- 源图: /Users/wenhua/Downloads/道元易学app图标.jpg (735x720)
- 居中裁方 + LANCZOS 缩放生成: Android 6 尺寸 (static/icons) + 原生 mipmap ic_launcher/ic_launcher_round 5 密度 + iOS 17 尺寸
- 脚本: /tmp/gen_icons.py (fish 不支持 heredoc, 用脚本文件执行)

## 3. 主题/语言设置立即生效 ✅

- **根因**: 原 t() 读 storage 非响应式, 切换后页面不更新
- 重写 utils/settings.js 为响应式 store: settingsState {lang, theme, dark} + t() 读 state → 模板自动更新
- App.vue onLaunch 调 initSettings(); onShow 调 applyTheme+updateTabBar (回前台刷新)
- tabbar 双语: uni.setTabBarItem 在非 tab 页不生效 → 切语言后回到 tab 页 onShow 时 updateTabBar
- watchSystemTheme: H5 matchMedia 监听系统主题变化
- 验证: 切 English 全页文案+tabbar 立即变英文 ✓

## 4. 商品详情属性"代码块"修复 ✅

- **根因**: Object.entries() 返回 [key,value] 数组, 模板 v-for="(val,key) in attrList" 把数组当对象遍历 → 显示原始数组
- 修复: v-for="[key, val] in attrList" 解构
- 验证: 材质 = 天然朱砂 ✓

## 5. App 滑动返回修复 ✅

- App.vue onLaunch 监听 Capacitor backButton: canGoBack → history.back(), 否则 minimizeApp() (不退出)
- H5 条件编译包裹, 普通浏览器不影响

## 6. 关于我们改版 ✅

- 去掉"数据服务"; "产品定位"→"关于我们"; 三段: 真和盛文化 / 道元易学 v1.0.0 / 融合传统智慧与现代科技

## 7. 我的课程改版 ✅

- course/my.vue 重构: 头部(我的课程+全部课程入口) + 4 tab(已购课程/正在学习/已完成/收藏课程, 带数量徽标) + 列表
- counts 从 allCourses computed 计算; 订单页 pill 风格
- 验证: badges=[1,1,0,0] (昊辰 uid=4 有 1 门学习中课程)

## 部署

- H5 线上更新 (100 文件, HTTP 200)
- APK 重新打包上传: /apk/daoyuan-v1.0.0.apk (6.0MB, 应用名道元易学)
- 小程序构建通过

## 排坑

- **构建覆盖**: uni build -p mp-weixin 后 dist/build/h5 不存在 → 部署前必须重新 uni build (H5)
- **测试注意**: hash 路由下 agent-browser open 改 hash 不重载 JS → pinia 内存态旧, 需 location.reload() 强刷
- Capacitor sync 不更新 strings.xml app_name → 手动改 res/values/strings.xml
