# 2026-08-05 工作日志

# ============ 十六项功能修改 (08-05 二轮) ============

## 云函数新接口 (dy-api)

- user.register: 道号分配 (ZHSM001=管理员昊辰, 普通用户 ZHS00001 起) + invite_code=道号 + 邀请绑定 inviter_uid
- 邀请奖励: 被邀请注册 → 自动发「邀请专享·全场8折」券 (coupons: type=percent, value=80, uid)
- user.login: 老用户自动补发道号 (管理员 ZHSM001)
- user.updateProfile / user.assets / user.coupons / user.favorites / user.favorite.toggle / user.footprints / user.footprint.add
- user.wechatLogin: 小程序 code→openid 自动登录/注册 (需环境变量 WX_APPID/WX_SECRET, 未配置返回引导)
- admin.logistics.list: 7 家物流公司; admin.orders.ship 扩展 company/tracking_no
- createOrder: 支持 coupon_id 核销 + coupon_discount/balance_used 落库
- admin.coupons.create: 支持 uid_holder 绑定用户 + value (percent)

## 前端改动

1. **订单列表彩色 tab** ✅ order/list.vue: pill-待付款(红)/待发货(棕)/待收货(橙)/已完成(绿)/已退款(灰)
2. **后台发货弹窗** ✅ dashboard.vue: openShip 物流公司 pills + 单号 + 确认发货, 订单表显示物流
3. **滑动返回修复** ✅ MainActivity.java 覆写 onBackPressed: canGoBack→goBack, 否则 moveTaskToBack(最小化)
4. **我的页重构** ✅ user.vue: 我的课程面板(4 图标,订单上方) + 资产(余额/优惠券/收藏/足迹) + 头像修改弹窗(预设头像+昵称) + 道号点击复制 + 邀请弹窗(复制链接)
5. **优惠券抵扣** ✅ checkout.vue: percent 8折计算 (value=80→减20%), 下单传 coupon_id
6. **邀请注册** ✅ login.vue: ?invite= 自动填充邀请码 + 微信一键登录按钮
7. **商品按钮双色** ✅ detail.vue: btn-cart 金棕渐变 / btn-buy 朱砂红渐变 (自绘, 不用 u-button)
8. **道号** ✅ 云函数分配, 我的页显示 "道号 · ZHSM001"
9. **微信登录** ✅ login.vue 微信按钮 (小程序 wx.login; H5/App 提示需配置)
10. **关于我们移出设置** ✅ setting.vue 移除, 保留在我的页
11. **头像昵称修改** ✅ user.vue 弹窗 + user.updateProfile
12. **道源→道元清零** ✅ uni.scss 注释 + 云函数注释
13. **APK 图标** ✅ 根因: Android 8+ 自适应图标用 ic_launcher_foreground.png, 之前只换 legacy → 新生成 foreground 各密度(66%居中) + background #4E3420, aapt 验证
14. **后台时间单行** ✅ dashboard.vue: 新增 .w-time 列 (nowrap, 宽 220rpx), 直播/动态表
15. **课程购买按钮** ✅ course/detail.vue: 实心渐变按钮 + 与价格留间距 + 原价划线
16. **课程页文字** ✅ course.vue: 标题去 ellipsis-2 截断 → 完整分行 + word-break

## 验证结果

- 道号: 昊辰登录 dao_code=ZHSM001 ✓; 新用户注册 ZHS00001 ✓
- 邀请注册: inviter_uid=4 绑定 ✓, 8 折券发放 ✓ (type=percent value=80)
- 结算 8 折: 168→134.40 (省33.6) ✓
- 发货弹窗: 物流公司列表+单号 ✓
- 订单彩色 tab ✓; 我的页结构 ✓; 邀请链接复制 ✓

## 部署

- H5 线上 102 文件 ✓; 小程序构建 ✓; APK 重打包 (新图标+返回修复+名称) ✓
- 注意: 云函数部署后旧实例缓存 ~10s, 立即测试可能旧代码

# ============ 七项功能修改 (08-05) ============

## 1. App 名称 → 道元易学 ✅

- 19 个文件全局替换 (src/scripts/mobile/capacitor.config.json)
- **注意**: Android 应用名在 mobile/android/app/src/main/res/values/strings.xml (Capacitor add 时生成, sync 不更新) → 手动 sed 替换后重建 APK, aapt 验证 application-label='道元易学'

## 2. App 图标替换 ✅

- 源图: /Users/wenhua/Downloads/道元易学app图标.jpg (735x720)
- 居中裁方 + LANCZOS 缩放生成: Android 6 尺寸 (static/icons) + 原生 mipmap ic_launcher/ic_launcher_round 5 密度 + iOS 17 尺寸
- 脚本: /tmp/gen_icons.py (fish 不支持 heredoc, 用脚本文件执行)

## 3. 主题/语言设置立即生效 ✅

- **根因**: 原 t() 读 storage 非响应式, 切换后页面不更新
- 重写 utils/settings.js 为响应式 store: settingsState {lang, theme, dark} + t() 读 state → 模板自动更新
- App.vue onLaunch 调 initSettings(); onShow 调 applyTheme+updateTabBar (回前台刷新)
- tabbar 双语: uni.setTabBarItem 在非 tab 页不生效 → 切语言后回到 tab 页 onShow 时 updateTabBar
- watchSystemTheme: H5 matchMedia 监听系统主题变化
- 验证: 切 English 全页文案+tabbar 立即变英文 ✓

## 4. 商品详情属性"代码块"修复 ✅

- **根因**: Object.entries() 返回 [key,value] 数组, 模板 v-for="(val,key) in attrList" 把数组当对象遍历 → 显示原始数组
- 修复: v-for="[key, val] in attrList" 解构
- 验证: 材质 = 天然朱砂 ✓

## 5. App 滑动返回修复 ✅

- App.vue onLaunch 监听 Capacitor backButton: canGoBack → history.back(), 否则 minimizeApp() (不退出)
- H5 条件编译包裹, 普通浏览器不影响

## 6. 关于我们改版 ✅

- 去掉"数据服务"; "产品定位"→"关于我们"; 三段: 真和盛文化 / 道元易学 v1.0.0 / 融合传统智慧与现代科技

## 7. 我的课程改版 ✅

- course/my.vue 重构: 头部(我的课程+全部课程入口) + 4 tab(已购课程/正在学习/已完成/收藏课程, 带数量徽标) + 列表
- counts 从 allCourses computed 计算; 订单页 pill 风格
- 验证: badges=[1,1,0,0] (昊辰 uid=4 有 1 门学习中课程)

## 部署

- H5 线上更新 (100 文件, HTTP 200)
- APK 重新打包上传: /apk/daoyuan-v1.0.0.apk (6.0MB, 应用名道元易学)
- 小程序构建通过

## 排坑

- **构建覆盖**: uni build -p mp-weixin 后 dist/build/h5 不存在 → 部署前必须重新 uni build (H5)
- **测试注意**: hash 路由下 agent-browser open 改 hash 不重载 JS → pinia 内存态旧, 需 location.reload() 强刷
- Capacitor sync 不更新 strings.xml app_name → 手动改 res/values/strings.xml

# ============ 九项功能修改 (08-05 三轮) ============

1. **按钮实心填充** ✅ cart结算/checkout提交/课程开始学习/邀请复制 全部改自绘渐变实心 (u-button 在 App 端可能显示空心)
2. **我的课程 tab 彩色** ✅ pill-purchased(棕)/learning(红)/done(绿)/fav(橙), 已完成→完成课程
3. **订单按钮彩色** ✅ order/list: btn-pay(红)/btn-confirm(棕)
4. **意见反馈** ✅ user.feedback + admin.feedbacks.list/reply/delete + 后台"反馈管理"栏目 + 前端反馈页 pages/user/feedback
5. **物流选择独立页** ✅ pages/admin/logistics: 快递单号自动识别物流公司(前缀/数字规则) + 7家物流选择(微信小店样式), 发货跳转该页
6. **购物车结算栏** ✅ position:fixed 底部
7. **头像修改** ✅ 去掉预设文字头像, uni.chooseImage 相册选择 → 云存储上传 (avatars/u{uid}_{ts}.png, fileID→https CDN)
8a. **tabbar 中英文** ✅ onShow updateTabBar 兜底
8b. **消息中心** ✅ pages/user/messages + user.messages/unread/read + 订单支付/发货/收货自动推送消息 + 我的页🔔入口(红点)
9. **会员等级** ✅ user.vip: 按已完成订单累计消费 ≤1000=VIP0/1000-2000=VIP1/2000-5000=VIP2/5000-1万=VIP3/>1万=VIP4, 徽标分级色+等级说明
10. **集合创建** ✅ NoSQL 集合需手动建: 云函数加 admin.db.createCollection (db.createCollection), 创建了 feedbacks/messages
11. **部署** ✅ H5 108 文件 + APK 重打包 6.3MB + 小程序构建

# ============ 三项功能修改 (08-05 四轮) ============

1. **商品数量选择器** ✅ product/detail.vue: 购买数量 -/+ 控件(限库存), addToCart/buyNow 带 qty
2. **收藏红心饱满化** ✅ 去掉 ♥/♡ 字符, CSS 绘制心形(两圆+旋转方块) + 心跳动画 + 收藏红色渐变
3. **玄学排盘工具** ✅ 我的页新增"玄学工具"面板(5工具图标入口, 仿订单样式) → pages/user/tools.vue
   - utils/paipan.js 算法库:
     - 四柱八字: 立春界年柱 + 节气近似月柱(五虎遁) + 1900-01-31甲辰锚点日柱 + 五鼠遁时柱; 实测 1990-01-01午时=己巳丙子丙寅甲午 与标准万年历一致 ✓
     - 六爻: 三铜钱模拟 + 先天八卦序64卦名表(8x8); 实测 天地否卦正确 ✓
     - 紫微: 简化十四主星 + 十二宫(标注简化)
     - 奇门: 简化九宫八门九星(日干定值符)
     - 大六壬: 月将加时天地盘
   - 坑: 对象键 011/010 等 JS strict mode 八进制错误 → 必须字符串键 "011"
   - 工具页支持 ?tool= 直达 (bazi/liuyao/ziwei/qimen/liuren)
4. **部署** ✅ H5 110 文件 + APK 重打包 6.3MB + 小程序构建

# ============ 四项功能修改 (08-05 五轮) ============

1. **课程/订单 tab 紧凑彩色** ✅ course/my.vue + order/list.vue: 小按钮(64rpx) + 每项固定彩色(浅底彩字,选中实心) + 右上角数字徽标; 文字可两行(max-width 110rpx)
2. **订单列表统计** ✅ order/list.vue 改一次拉全部(status=全部) → computed counts 各状态数量 + orders 前端筛选
3. **App 滑动返回根因修复** ✅ **关键**: Capacitor 8 + AndroidX 下覆写 onBackPressed() 方法不生效(API33+ 走 Dispatcher)! 改注册 getOnBackPressedDispatcher().addCallback(OnBackPressedCallback) → canGoBack?goBack:moveTaskToBack。此修复需重装 APK 生效
4. **版本对接 Git** ✅ scripts/gen-version.js: git rev-parse/describe → src/version.js (APP_VERSION/COMMIT/FULL_VERSION); 我的页/关于页引用显示; 每次构建前运行; 版本 tag v1.3.0/v1.4.0 已推送 GitHub
5. **部署** ✅ H5 111 文件 + APK 6.3MB + tag 推送

# ============ 三项功能修改 (08-05 六轮) ============

1. **tabbar 图标放大** ✅ generate-tabbar.py SIZE 81→96, PAD 14→16, 重新生成 8 个图标 (H5/App 端明显变大, 小程序受平台限制)
2. **课程卡片布局** ✅ course.vue: 封面 240→216rpx; 查看按钮 u-button→自绘实心渐变+margin, padding-right 不贴边缘
3. **玄学工具位置** ✅ user.vue: 面板移到我的订单下方; 顺序 四柱八字/奇门遁甲/紫微斗数/大六壬/六爻 (tools.vue 同步)
4. **部署** ✅ v1.5.0: H5 111 文件 + APK 6.3MB + tag 推送

# ============ APK 包名版本同步 (08-05) ============

- APK 文件名: daoyuan-v1.0.0.apk → daoyuan-v1.5.0.apk (随版本号)
- Android build.gradle: versionCode 1→5, versionName "1.0"→"1.5.0"
- 下载页 3 处链接同步 (href/JS apkUrl/注释)
- **惯例 (用户要求)**: 每次发版 → APK 文件名 daoyuan-vX.Y.Z.apk + build.gradle versionCode 递增 + versionName 同步 + 下载页链接/文字同步
- 旧文件 daoyuan-v1.0.0.apk 保留 (回退用)

# ============ 后台横屏+员工权限+分配道号 (08-05 七轮) ============

1. **后台横屏** ✅ dashboard 顶部"🔄 旋转屏幕"按钮: Capacitor 环境用 window.Capacitor.Plugins.ScreenOrientation.lock (插件 @capacitor/screen-orientation@8.0.1 已装, 原生编译), H5 fallback screen.orientation.lock, 小程序提示手动
2. **员工权限体系 (role=staff)** ✅
   - 云函数: requireAdmin 允许 admin+staff; requireStaffAllowed 白名单 STAFF_ROUTES (订单发货/退款/商品课程增删改/分类/直播/概览/物流) + admin.list 仅 STAFF_COLLECTIONS (orders/products/courses/live_streams/categories/course_categories)
   - 前端: dashboard visibleModules 员工仅 概览/商品/课程/订单/直播; 退款按钮 staff 隐藏; 侧栏角色显示"内部员工"
   - 实测: staff 查商品 OK, 设置/优惠券 403; 员工 13800138002 分配 ZHSM002+staff 登录成功
   - **关键修复**: _adminAuth 原来覆盖业务 uid → 改附加 opUid/opRole (操作者), 业务 uid 保留; 云函数权限校验用 opUid/opRole; 修复了 adminUserUpdate 一直更新操作者自己的 bug
3. **分配道号** ✅ 后台用户管理: "分配道号"弹窗 (道号ID + 角色 普通用户/内部员工/管理员), 超管专用; adminUserUpdate 支持 dao_code+role, dao_code 同步 invite_code
4. **部署** ✅ H5 111 文件 + APK (含插件原生编译) + 小程序构建

# ============ 版本号惯例升级 (08-05) ============

- **惯例 (用户要求)**: 每次小的修改也要体现在版本号**第三位** (vX.Y.Z 的 Z)
- 流程: 修改完成 → git commit → git tag vX.Y.Z (Z 递增) → node scripts/gen-version.js → build.gradle versionCode+1/versionName 同步 → 重建 APK (daoyuan-vX.Y.Z.apk) → 下载页文字+链接同步 → 构建部署 → push --tags
- 实例: v1.6.0 (大版本) → v1.6.1 (下载页小修改), APK versionCode 6 / versionName 1.6.1

# ============ 问真风格八字排盘+AI解盘 (08-05 八轮 v1.7.0) ============

1. **问真风格排盘** ✅ tools.vue 八字模块重构:
   - 四柱表: 十神+天干(五行色)+地支+藏干(十神标注)+纳音
   - 五行统计条(带色条+数量) + 日主/空亡/起运/大运方向元信息
   - 大运 8 步横滑(干支+十神+岁数区间) + 流年(深色卡)
   - 性别选择(大运顺逆排需要)
2. **算法扩展** ✅ utils/paipan.js: fullBazi (十神/藏干/纳音/五行统计/空亡/大运顺逆/流年/日主强弱) + utils/jiepan.js (AI解盘生成器)
   - 空亡公式: 旬首地支 = (日支 - 日干%10 + 12)%12, 空亡=(旬首+10,+11)%12 (实测丙寅日=戌亥 ✓)
3. **AI 解盘付费体系** ✅
   - 免费: 六亲缘分/健康养生 (直接展开)
   - 付费 ¥9.9: 事业前程/财富格局/婚姻感情 (storage jiepan_paid_v1 永久解锁 + 生成模拟订单)
   - 总评一句话(免费展示)
4. **部署** ✅ v1.7.0: H5 111 文件 + APK daoyuan-v1.7.0.apk (versionCode 7) + 下载页同步 + tag 推送

# ============ DeepSeek AI + 横屏平铺 + 品牌修正 (08-05 九轮 v1.7.1) ============

1. **DeepSeek AI 解盘** ✅
   - Key: config.local.js (gitignore 不入仓库!) + 云函数环境变量兜底
   - **坑**: CloudBase 云函数默认超时 3 秒, DeepSeek 响应超时 → updateFunctionConfig 需顶层参数 {"func":{"name":..},"timeout":30} 才生效 (嵌套 config 无效)
   - ai.jiepan 接口: 调 api.deepseek.com/chat/completions (deepseek-chat), 按模块 prompt (career/wealth/marriage/liuqin/health)
   - 前端: 付费模块解锁后展开时自动加载真实 AI (loading 态+AI标识), 失败回退规则引擎; 免费模块保留规则引擎
   - 实测: 事业解盘 10 段 AI 输出 "杀印相生格局..." ✓
2. **后台横屏平铺** ✅ 根因: rpx 随屏宽等比缩放 → 横屏放大; 修复: @media min-width 800px 用 px 覆盖字号/间距/宽度, max-width 1400px
3. **品牌文字** ✅ about.vue/index.vue 印章 易→道, about 增加"真和盛"行
4. **部署** ✅ v1.7.1 (versionCode 8): H5 111 文件 + APK + 下载页同步 + tag 推送

# ============ 主题切换全面生效 (08-05 十轮 v1.7.2) ============

**根因**: ① App端 applyTheme 只写 storage 没设置 data-theme → APK 深色完全不生效 ② H5 端只改 page 背景, 页面几十处硬编码浅色底(#fefbf6/#f8f3ea)和深色文字(#42372c)未变 → 视觉差异极小

**方案: CSS 变量主题化** (唯一可靠做法, 后续新页面必须用变量):
- src/utils/settings.js: applyTheme 改 #ifndef MP-WEIXIN 全端设置 data-theme (App 端 WebView 同样走 document)
- src/App.vue: page 上定义 7 组变量 (浅色默认): --dy-page/card/soft/text/sub/faint/line/mask; html[data-theme=dark] page 覆盖深色值
- 变量定义放 page 而非 :root → 小程序端 var() 有默认值不会失效 (深色仅 H5/App)
- 两轮脚本批量替换 25+16 个页面 .vue 硬编码中性色 → var()
- 深色适配: u-popup 弹窗/u-action-sheet/tabbar/uni-page-head 全局覆盖
- 实测: 浅 bg#f8f3ea 卡#fefbf6 ↔ 深 bg#1c1712 卡#2a231c 文字#e9dfcd, 差异明显 ✓
- 强调色(朱砂/金棕/绿)保持不变量化; uni.scss 不替换 (uview 编译期变量)

# ============ 问真级八字排盘全量复刻 (08-05 十一轮 v1.8.0) ============

1. **三种输入方式** ✅ tools.vue 八字模块: 阳历(日期picker) / 农历(年1900-2100+月含闰+日) / 四柱直接输入(60甲子picker×4) + 真太阳时开关(46城市经度选择)
2. **真太阳时** ✅ utils/lunar.js trueSolarTime: 经度修正(120°E基准 4分/度) + 均时差(EoT 天文公式); 实测北京-14分(经度)-4分(均时差); 修正后时辰重算
3. **排盘结果问真化** ✅
   - 顶部: 农历(干支年+月+日) + 阳历 + 元男/元女 + 日主
   - 四柱: 干支同字号50rpx五行配色 + 每柱下方 星运(纳音)/自坐(本气十神)/空亡(是/无)/神煞列表
   - 地支藏干独立模块(每柱藏干+十神标注, 在四柱下方)
   - 大运可点击→展开该步10年流年(年份+干支+十神+五行色) → 流年可点击→展开12流月(月序+干支+十神)
4. **算法扩展** ✅ paipan.js: shenshaOf(天乙/文昌/禄神/羊刃/桃花/驿马/华盖/将星/劫煞/灾煞/亡神/孤辰/寡宿), liuyueOf(五虎遁), baziFromGanZhi(四柱输入), enrichFull, liunianOfDayun(大运→10流年)
5. **农历转换** ✅ 新增 utils/lunar.js: lunarInfo表(1900-2100) + solarToLunar/lunarToSolar
   - **坑1**: 闰月判断须先 leap>0; **坑2**: 闰月月份 = -leap 非 -mi; **坑3**: 闰月与 leap+1 月共享位置迭代(两个独立if非else); **坑4**: lunarToSolar 用位置序列 seq(闰月占位leap+1); **坑5**: leapDays 标志位是 0x10000 非 0x10 (bit16 闰月大小, bit4是12月大小)
   - 往返一致性: 20741/20741 (1900-2100); 锚点: 春节/中秋/重阳/2025闰六月 全对 ✓
6. **部署** ✅ v1.8.0 (versionCode 10): H5 111文件 + APK daoyuan-v1.8.0.apk + 下载页 + tag 推送

# ============ UI板块分割恢复 (08-05 十二轮 v1.8.1) ============

**重要根因**: v1.7.2 CSS变量主题化批量替换时, 把**边框色**也误替换成了背景色 (border: 1rpx solid #fefbf6 → var(--dy-card), #f8f3ea → var(--dy-page), #faf3e9 → var(--dy-soft)) → 边框=背景色完全不可见 → 整个APP板块分割感消失 (用户反馈"1.7.0有现在没了")
- 修复: 11个文件 20+处 `border(-top/bottom/left/right): Npx solid var(--dy-page/card/soft)` → `var(--dy-line)` (脚本 /tmp/fix_borders.py)
- **教训**: 颜色替换脚本必须区分语义 (背景色/文字色/边框色), 后续主题化替换需按属性白名单处理

其他修复:
1. 首页发布动态: 列表底部 → 右下角悬浮按钮 (fixed, 渐变+阴影, bottom 140rpx 避开tabbar)
2. 商品详情: 底部操作栏 1px→2px 边框+上阴影(框效果); 购买数量/attrs属性区 独立圆角板块(浅底+边框)
3. 课程详情: 课程介绍与课程大纲拆两个独立card板块; 底部栏2px边框+阴影
4. 课程列表: 查看按钮 padding-right 6→16rpx (距卡片右缘48px 不贴边)
5. 部署: v1.8.1 (versionCode 11): H5 111文件 + APK + 下载页 + tag

# ============ 侧栏框线加强 (08-05 十三轮 v1.8.2) ============

- 商城 shop.vue / 课程 course.vue 的 .cate-side 左侧分类栏: 加 2rpx 金棕半透明右边框 (rgba(140,90,43,0.35)) + 内侧阴影线, 深浅色均清晰
- 部署: v1.8.2 (versionCode 12): H5 111文件 + APK + 下载页 + tag

# ============ 后台按钮单行+logo改道 (08-05 十四轮 v1.8.3) ============

- 商品/课程操作列(编辑/上架/删除) 同一行: w-ops 宽280→340rpx + flex-wrap nowrap + .op white-space nowrap/flex-shrink 0
- tb-link/pill 加 nowrap (后台所有按钮防换行竖排)
- dashboard 左上角 logo-seal + admin/login 印章 易→道
- 实测: 商品行操作 tops=355,355,355 同一行 ✓
- 部署: v1.8.3 (versionCode 13): H5 111文件 + APK + 下载页 + tag

# ============ App端板块感修复 (08-05 十五轮 v1.8.4) ============

**根因**: CSS变量原只定义在 `page` 选择器, App端(WebView)渲染结构无标准 page 元素时 var(--dy-line) 等失效 → 边框/背景丢失, 板块感消失 (H5正常因编译产物含 body/uni-page-body)
- uni-app 编译产物验证: App.vue 的 page{} 被编译为 :root + body + uni-page-body 三重选择器
- 修复: 变量定义同时挂 `:root` (WebView必有) + `page`; 深色挂 `html[data-theme=dark]` 根级
- **教训**: uni-app 全局CSS变量定义必须挂 :root 兜底, 不能只靠 page
- 部署: v1.8.4 (versionCode 14): H5 111文件 + APK daoyuan-v1.8.4.apk (aapt验证 versionName=1.8.4) + 下载页 + tag 推送

# ============ 课程页移除查看按钮 (08-05 十六轮 v1.8.5) ============

- course.vue: 删除 btn-view 查看按钮(模板+样式), 课程卡片整卡 @tap goDetail(cc.id) 点击直接进详情 (实测跳转 detail?id=1 ✓)
- 部署: v1.8.5 (versionCode 15): H5 111文件 + APK + 下载页 + tag

# ============ App板块感最终修复 - 回归硬编码 (08-05 十七轮 v1.8.6) ============

**最终根因与决策**: CSS变量(var(--dy-*))在App端WebView支持不稳定(即使挂:root), 用户实测APK仍无边框 → **放弃CSS变量方案, 全局回归v1.7硬编码配色**
- /tmp/revert_colors.py: 26个文件全部 var(--dy-page/card/soft/text/sub/faint/line/mask) → 写死色值(#f8f3ea/#fefbf6/#faf3e9/#42372c/#857563/#b3a595/#efe7d8/rgba)
- 深色模式保留: html[data-theme=dark] page 背景/文字硬编码覆盖(卡片保持浅色, 同v1.7.1行为)
- **教训**: uni-app App端(WebView)对CSS变量支持不可靠 → 主题化方案必须硬编码双份或放弃变量; 用户明确要求回归1.7效果
- 部署: v1.8.6 (versionCode 16): H5 111文件 + APK daoyuan-v1.8.6.apk + 下载页 + tag
- 注意: 本次git tag v1.8.6 曾因命令重复执行指向旧提交, 已 git tag -f 重指向 HEAD

# ============ 排盘独立页+三工具共用时间 (08-05 十八轮 v1.9.0) ============

1. **共用时间输入** ✅ tools.vue: 四柱/奇门/紫微三个tab共用出生时间表单(阳历/农历/四柱+时辰+性别+真太阳时), 排盘按钮 runPaipan 一次计算三种结果
2. **独立结果页** ✅ 新增 pages/user/paipan.vue (pages.json 注册): 数据经 uni.setStorageSync("paipan_data", {bazi,qimen,ziwei}) 传递; 结果页三tab切换(四柱/奇门/紫微), ?tool= 直达
3. **四柱问真布局细化** ✅
   - 顶部: 农历+阳历+元男/元女+日主
   - 四柱表格: 最左列行标签(十神/天干/地支)+4柱, 每格边框, 板块感(2rpx边框#d8ccb8)
   - 地支藏干: 四柱下方独立板块, 左侧"藏干"标签列
   - 星运/自坐/空亡: 一个板块, 左侧分类标签(星运/自坐/空亡)三行
   - 神煞: 单独板块, 左侧标签
   - 大运: 点击→10流年一行(scroll-x不换行), 流年点击→12流月一行
   - 大运/流年/流月: 干支右侧灰色十神(pp-ss-right, #b3a595)
   - AI解盘(含DeepSeek付费模块)迁移至结果页
4. **奇门/紫微** ✅ 结果页tab: qm 9宫格 / zw 12宫位+命宫高亮
5. **部署** ✅ v1.9.0 (versionCode 17): H5 114文件 + APK daoyuan-v1.9.0.apk + 下载页 + tag

# ============ 排盘输入转盘化+省市县真太阳时 (08-05 十九轮 v1.9.1) ============

1. **输入交互改造** ✅ tools.vue:
   - 去掉"三种排盘共用..."提示文字
   - 阳历: 日期+时辰 并排转盘(一个 tp-row 内两个 picker); 农历: 年月日+时辰 全转盘
   - 四柱: 年/月/日/时柱 一排4个 60甲子转盘 (tp-gz-grid)
   - 时辰仅在农历模式独立显示, 阳历嵌入日期行, 四柱由时柱代替
2. **真太阳时省市县** ✅ 仅阳历模式显示:
   - 新增 utils/cities.js: REGION_DATA 34省 130+市 区县经纬度, PROVINCE_NAMES/getRegionLngLat
   - 三级转盘联动: 选省→重置市/县 (onProvinceChange)
   - 修正说明: "出生地 北京·东城区（东经116.41° 北纬39.9°）：-14分(经度) -4分(均时差)"
   - 省市同名优化: 北京+北京→北京
3. **部署** ✅ v1.9.1 (versionCode 18): H5 114文件 + APK daoyuan-v1.9.1.apk + 下载页 + tag

# ============ 神煞补全+大运流年流月联动四柱 (08-05 二十轮 v1.9.2) ============

1. **农历输入** ✅ tools.vue: 年月日时一行4转盘(tp-4紧凑), 真太阳时开关+省市县扩展到农历模式
2. **神煞补全** ✅ paipan.js shenshaOf 扩展签名(g,z,yearZhi,monthZhi,dayGan,dayZhi):
   - 新增: 天德/天德合(月支), 月德/月德合(三合月), 天医(月支前位), 金舆/学堂/词馆/福星/国印(日干), 红鸾/天喜(年支), 魁罡/十恶大败/阴差阳错/孤鸾/四废(日柱级)
   - 共 30 种神煞; enrichFull 传月支/日干/日支
3. **大运/流年/流月联动四柱** ✅ paipan.vue:
   - item 布局: 时间(最上) + 干支上下排列(干行/支行) + 右侧灰色一字十神(印/枭/杀/官/才/财/比/劫/食/伤) + 紧凑(min-width 130rpx)
   - 点击选中 → buildExtraCol 动态加列到四柱左侧: 从左到右 流月→流年→大运→四柱 (联动列 head 高亮: 大运金/流年红/流月绿)
   - 藏干/星运/自坐/空亡/神煞 板块 columns 同步渲染联动列
   - **坑**: extra col 需 g/z 数字索引字段(模板 GAN[c.g]), 只存 ganIdx 会渲染空
   - 实测: 大运乙亥(印) → 流年1995乙亥 → 流月1月戊寅(食) 三级联动全对 ✓
4. **部署** ✅ v1.9.2 (versionCode 19): H5 114文件 + APK + 下载页 + tag

# ============ 流时+地支十神+节气+输入优化 (08-05 二十一轮 v1.9.3) ============

1. **农历输入** ✅ 年月日时一行4转盘加宽(tp-4 font-size 22rpx/padding 10rpx)
2. **四柱输入** ✅ 手动输入框(输入干支文本自动解析) + 右侧▾下拉滚动选择(tp-gz-input-row/tp-gz-drop); onGzInput 需防御 e.detail 可能 undefined
3. **地支十神** ✅ 大运/流年/流月/流时 item 地支右侧显示本气十神一字灰(zhiSs1: ZHI_CANGGAN[z]本气→shishen)
4. **流时功能** ✅ 流月展开→12时辰一行横滑(五鼠遁以流月干为日干, buildLiushi); 点击联动加列; 四级联动: 流时→流月→流年→大运→四柱(从左到右), ex-liushi 列头蓝
5. **节气** ✅ 流月 item 上方显示"1月 · 立春 2/4"等12节气(JIEQI 数组)
6. **板块标题** ✅ 藏干/星运自坐空亡/神煞三个板块标题去掉(保留边框板块感)
7. **bug修复** ✅ baziFromGanZhi 返回原始pillars(无canggan) → enrichFull 报错 → 四柱模式排盘失败; 改返回增强版 full (pillars: full)
8. **其他** ✅ 邀请链接框溢出修复(overflow-wrap+width100%); 登录页品牌区加"眞和盛"
9. **部署** ✅ v1.9.3 (versionCode 20): H5 114文件 + APK + 下载页 + tag

# ============ 邀请链接+排盘紧凑化 (08-05 二十二轮 v1.9.4) ============

1. **邀请链接框溢出** ✅ user.vue: .invite-sheet 加 box-sizing/width 100%/overflow hidden; .invite-link 加 overflow:hidden + overflow-wrap:anywhere + white-space:normal (App WebView 长URL断行: break-word 不够, 必须 anywhere)
2. **大运/流年/流月/流时紧凑** ✅ paipan.vue: .pp-dy-row gap 10→6rpx, .pp-dy-item padding 6/12→4/8rpx + min-width 130→100rpx, 干支 26→24rpx, 右侧十神 15→12rpx, .pp-expand padding 12→8rpx/margin-top 8rpx, .pp-dy-age 加 nowrap
3. **藏干/星运/自坐/空亡/神煞表格化** ✅ 三个 block 加 .pp-tbl 类: padding 0 + border 2rpx solid #d8ccb8 (与四柱 .pp-grid 同款框线), 内层 .pp-cg-grid/.pp-meta-grid 去自身边框
4. **部署** ✅ v1.9.4 (versionCode 21): 提交 ca871fa + tag v1.9.4, H5 114文件 + APK daoyuan-v1.9.4.apk (6.4MB) + 下载页3处同步, push 完成 (dedab66), 线上验证 APK/下载页 200 OK

# ============ 排盘保存+流日+横滑+紧凑 (08-05 二十三轮 v1.9.5) ============

1. **保存排盘开关** ✅ tools.vue: 表单加"保存排盘"switch (form.bazi.saveHistory); runPaipan 开启时存 storage 'paipan_history' (unshift 上限20, 含 ts/time/label/gzText/data{bazi,qimen,ziwei}); paipan.vue 顶部加"📜 历史"按钮 → u-popup 列表(时间+四柱+摘要, 可点击加载/✕删除), applyData() 统一新盘/历史盘加载(重置联动+重新生成解盘)
2. **流日功能(5级联动)** ✅ paipan.vue: 大运→流年(10)→流月(12)→流日(30)→流时(12); buildLiuri 以流月节气日为锚点(dayPillar 导出自 paipan.js)递推30天, 小寒1/6跨年(仅 sm===1 时 +1 年); 流时改以**流日干**五鼠遁(原流月干); 联动列顺序 流时→流日→流月→流年→大运→四柱, ex-liuri 列头淡紫 #ece6f2; 锚点实测 2024-02-04 立春=戊戌日 ✓
3. **干支框减半+月份节气多行** ✅ .pp-dy-item min-width 100→54rpx, padding 2/4rpx, gap 3rpx, 干支 22rpx, 十神 11rpx; 流月 age 拆两行 `1月\n立春 2/4` (white-space: pre-line)
4. **四柱盘横向滑动** ✅ 四柱表+藏干+星运/自坐/空亡+神煞 全部包 scroll-view scroll-x; .pp-grid/.pp-tbl 设 width:max-content + min-width:100%, .pp-cell flex: 1 0 92rpx (grow1+shrink0: 列少均分满宽, 列多固定92rpx 超宽可滚), label 110rpx
5. **部署** ✅ v1.9.5 (versionCode 22): 提交 cc095bc + tag v1.9.5, H5 + APK daoyuan-v1.9.5.apk (6.4M) + 下载页, push (07a7553), 验证 200 OK
6. **下一步(用户提出)**: 接入真实微信支付 — 需用户提供 mchid/APIv3密钥/证书; 当前 payOrder 为演示(直接改待发货); 已调研: createOrder/payOrder 在 cloudfunctions/dy-api/index.js 616-637行
