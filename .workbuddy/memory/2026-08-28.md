# 2026-08-28 工作日志

## v1.11.216: 后台左侧导航调整顺序
- 用户要求：不是精简为 4 项，是**调整顺序**——前 4 项为「数据概览→盘道管理→直播管理→商品管理」，其余模块（课程/订单/订单分析/售后/优惠券/动态/反馈/用户/页面/系统设置）保留排后
- `src/pages-sub/admin/dashboard.vue` modules 数组重排
- H5 + 小程序构建部署

## v1.11.217: 课程视频上传 failed to fetch 诊断
- 现象：管理端课程上传视频报 `failed to fetch`
- 定位：H5 端 `storage.uploadFile` 走 COS 直传链路（`getUploadUrl` 拿凭证 → `fetch(meta.url, POST formData)`），浏览器跨域被拦时就是这个错
- 改动：
  1. `cloudfunctions/dy-api/index.js` `storageGetUploadUrl`：增加 `host`/`sig` 字段回传 + 完整原始 meta 日志
  2. `src/api/cloudbase.js` `uploadFile`：COS 直传段增加完整诊断日志（凭证各字段有无、fetch 异常捕获、响应体截取）
- 部署：dy-api 云函数 + H5 (v1.11.217) 均已上线；线上验证 `storage.getUploadUrl` 正常返回 COS 凭证
- 验证 curl: `{"status":200,"data":{"url":"https://636c-...cos.ap-shanghai.myqcloud.com/...","token":"...","authorization":"..."}}` ✅
- 注意：若线上仍报 failed to fetch，需检查 COS 桶 CORS 配置（允许 club.zhenhesheng.cn 域名 POST）

## 视频上传 failed to fetch 最终解决 (v1.11.217 后续)
- **根因确认**：COS bucket CORS 白名单不含 club.zhenhesheng.cn → 浏览器向 COS 发 POST 被跨域拦截
- **修复**：用 COS 官方 SDK (cos-nodejs-sdk-v5) + CloudBase 临时密钥 (`auth get_temp_credentials`) 查询/修改 bucket `636c-cloud1-d8gs2k9m311f7272f-1464523137` 的 CORS，加入 `https://club.zhenhesheng.cn` / `http://club.zhenhesheng.cn` / www 变体
- **临时密钥方法**：`npx -y mcporter call cloudbase.auth action=get_temp_credentials envId=... confirm=yes reveal=true` → secretId/secretKey/token
- **工具脚本**（可复用）：`/tmp/cos-cors.js`（手写签名 get/put）与 `/tmp/cos-sdk.js`（官方 SDK get），`/tmp/cos-add-cors.js`（自动追加域名）；本地 npm 装 cos-nodejs-sdk-v5
- 验证：`cos.getBucketCors` 确认白名单已含 club.zhenhesheng.cn ✅

## v1.11.218: 课程详情页视频铺满屏幕 + 课时文本
- 需求：① 进入课程详情页 PC 端视频默认铺满整个屏幕 ② 详情页显示文本内容，后台在上传视频处增加文本编辑
- 改动：
  1. `src/pages-sub/course/detail.vue`：顶部封面区域改为视频播放器（有 course.video 或第一集课时视频时），PC 端 (@media min-width:1025px) `.cover.has-video` 100vw×100vh 铺满全屏；课程大纲每课时下方显示 `ep.text`（课时文本说明）
  2. `src/pages-sub/course/lesson.vue`：PC 端视频 100vw×100vh 全屏，标题/导航半透明浮层叠加（fixed 定位）
  3. `src/pages-sub/admin/dashboard.vue`：课程表单每个课时增加 textarea 文本输入（ep.text 字段，显示在详情页课时下方）
- 数据模型：episodes 数组每集 {title, video, free, text}，云函数整体存取无需改
- 版本 v1.11.218 (commit 1596ceb) 已构建部署，线上验证 version.CPWmEVvB.js = v1.11.218 ✅
- ⚠️ gen-version.js 的 `git describe --tags` 会读错版本（tag 打在 parent），bump 后需手动确认 src/version.js 版本号正确再构建

## v1.11.219: 课程弹窗PC加宽 + 课程待更新 + 课时页视频全屏 + C/OSS存储管理
- 需求：① 课程编辑弹窗 PC 更宽 ② 未上传视频显示"课程待更新" ③ 视频全屏在**课时页**(lesson.vue)而非详情页，详情页恢复封面 ④ 课程大纲移到描述之后 ⑤ C/OSS 存储开关+视频搬运+上传提示
- 改动：
  1. `dashboard.vue`：课程弹窗 `.pp-mask.course-mask` + `.course-sheet` PC 端 900px 宽、垂直居中；课程大纲(episodes)移到描述字段之后；settings tab `oss` 改名 **C/OSS 存储** + `enabled` 开关；oss tab 新增"视频存储管理"区块(列表本地/C/OSS + 搬运按钮)；上传视频后 C/OSS 启用时 `maybeMigrateToOss()` 弹窗询问是否存储到 C/OSS
  2. `course/detail.vue`：恢复封面显示(撤销 v1.11.218 详情页视频全屏)；大纲课时无视频显示"课程待更新"徽标；openLesson toast 改"课程待更新"
  3. `course/lesson.vue`：PC 端视频 100vw×100vh 全屏(保留) + 空态改"课程待更新"
  4. `course/course.vue`：课程卡片无视频显示"课程待更新"角标 (hasCourseVideo 判断)
  5. `cloudfunctions/dy-api/index.js`：新增 `admin.oss.videos.list`(列出所有课程视频本地/C/OSS状态) + `admin.oss.videos.migrate`(下载本地→PUT 直传 COS/OSS→更新课程 episodes)；https 模块实现 GET 下载/PUT 上传(云函数无原生 fetch)；cosPutUrl/ossPutUrl 手写预签名；VIEWER_ROUTES 加 list
  6. `api.js`：adminVideosList / adminVideoMigrate
- C/OSS 判断: 本地=tcb.qcloud.la/cloud://, C/OSS=cos.myqcloud.com 或 settings.oss.domain
- 版本 v1.11.219 (commit 4bc56b1) 已构建部署，H5 线上 + 云函数 dy-api 均已更新；验证 admin.oss.videos.list 返回 403(未登录) 与现有 admin 接口一致 ✅
- ⚠️ 云函数部署后要重新验证路由，404=旧代码未生效，403=路由正常仅未登录

## v1.11.220: 课程大纲徽标顺序调整
- 需求：课程大纲每课时行——🔒锁放最右边，"课程待更新"贴着锁放左边
- `src/pages-sub/course/detail.vue`：lesson-name-row 顺序改为 标题 → 课程待更新 → 锁；lesson-name 加 flex:1+ellipsis，lesson-pending margin-left:auto 贴右、lesson-lock 最后带 margin-left
- 版本 v1.11.220 (commit eb7f266) 构建部署，线上验证 version.M7VTKWzA.js ✅

