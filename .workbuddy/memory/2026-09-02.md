# 2026-09-02 工作记录

## 搬运到 COS 深度排查 + 自诊断 (00:13)
- **用户反馈**: 搬运到 COS 仍报错, 要求深度排查具体原因
- **排查结论**: 代码路径正确 — COS 预签名 v1 签名算法逐行核对无误(HttpString=`put\n/key\n\nhost=xxx\n`, q-header-list=host, q-url-param-list 空); 流式管道 `getRes.pipe(putReq)` 正确接好; httpsPipe/httpsPutBuffer 均校验 HTTP 2xx。根因在**目标 C/OSS 配置/密钥**(Region 错/Bucket 名无 APPID 后缀/子账号无 PutObject 权限/SecretKey 复制带空格), 非代码 bug
- **新增自诊断**:
  - 云函数 `cloudfunctions/dy-api/index.js` 新增 `adminOssConfigTest` 路由 → 用配置做一次极小预签名 PUT+DELETE 探测, 直出腾讯云精确错误码(AccessDenied/NoSuchBucket/SignatureDoesNotMatch/InvalidAccessKeyId)+中文原因; 注册 `admin.oss.config.test`
  - `adminOssVideoMigrate` catch 错误信息追加目标桶域名 `xxx.cos.region.myqcloud.com`, 一眼区分配置/权限/区域 vs 源下载问题
  - 前端 `src/api/api.js` 加 `adminOssConfigTest`; `dashboard.vue` 系统设置 C/OSS 卡片加「测试连接」按钮 + 结果展示(.oss-test-result)
- **修 deploy 脚本 bug**: `scripts/deploy-cloudbase.js` 的 functions 部署用 `localPath` → mcporter 静默不更新; 改为 `updateFunctionCode` + `functionName` + `functionRootPath=<绝对路径>/cloudfunctions/dy-api`(函数目录本身)。已用正确参数重部署 dy-api 成功(success:true)
- **版本**: bump 295→297(中间出现游离 tag v1.11.296 无对应 commit, bump 读最高 tag 得 296→297), commit 8e438be, tag v1.11.297 -f 指向发布提交
- **部署**: dy-api 云函数 updateFunctionCode 成功(accessUrl=club.zhenhesheng.cn/dy-api); H5 构建 upload /h5/ 成功; 线上源站 `version.DGr-c_eE.js` 含 1.11.297 已 curl 校验 ✅; GitHub push 成功(9c05d89..8e438be, 新 tag v1.11.297)
- **下一步**: 让用户进后台「系统设置→C/OSS 存储」点「测试连接」, 按提示修正 Region/Bucket/密钥/权限; 通过后再搬运视频即不会再因配置错而失败

## 测试连接报 adminOssConfigTest is not defined (00:24)
- **根因**: 纯前端 import 漏项 — `dashboard.vue` 第1455行 import 列表含 adminVideoDelete 等但**漏了 adminOssConfigTest**; 第3893行 `testOssConfig` 调用 `adminOssConfigTest({})` → 点击即抛 `ReferenceError: adminOssConfigTest is not defined`。云函数侧已正确定义并部署(4835行函数体 + 5071行路由注册 + cosDeleteUrl/ossPutUrl/ossDeleteUrl/httpsPutBuffer/httpsDelete 辅助函数均在)
- **修复**: import 补 `adminOssConfigTest,`(1455行)
- **版本**: bump→v1.11.299, commit 310d281, tag v1.11.299 -f; H5 构建 upload /h5/ 成功, 源站 `version.GBZ7tVoK.js` 含 1.11.299 已 curl 校验 ✅; GitHub push 成功(8e438be..310d281 + 新 tag v1.11.299)
- **铁律补充**: 前端新增 api 封装后, 凡在页面里调用, 必须把对应导出名加入该页面 import 列表(否则运行时 ReferenceError, 只在点击时炸, 构建期不报错)

## 「测试连接」返回 SignatureDoesNotMatch (00:27)
- **用户现象**: 点「测试连接」直出 `签名不匹配：AccessKeySecret 填写错误（多了空格/换行，或复制不全）` → 自诊断精准命中密钥问题(非代码/桶/区域), 设计预期达成
- **代码兜底**: 在 `dy-api` 4 个签名函数(cosPutUrl/cosDeleteUrl/ossPutUrl/ossDeleteUrl)内对 `access_key`/`secret_key` 做 `String(x||'').trim()` 再算 HMAC, 消除粘贴时带入的首尾空格/换行干扰(正确密钥 trim 无副作用); 注: 若密钥本身复制不全/错配, trim 不救, 仍需用户重拷完整正确 SecretKey
- **版本**: bump→v1.11.301, commit d32e7e0, tag v1.11.301 -f; dy-api 重部署(accessUrl=club.zhenhesheng.cn/dy-api, success:已更新函数); H5 构建 upload /h5/ 成功, 源站 `version.DKHArVNP.js` 含 1.11.301 已 curl 校验 ✅; GitHub push 成功(310d281..d32e7e0 + 新 tag v1.11.301)
- **给用户操作**: 腾讯云「访问管理→访问密钥→API密钥管理」点显示完整 SecretKey 全选复制(勿分段拷); 后台重填 AccessKeyId+AccessKeySecret 保存; 再点「测试连接」直到 ✓

## 上传队列"只生效最后两个" (00:33)
- **现象**: 课程视频排队较多时, 只有最后两个上传真正生效(写回课程), 前面排队的视频丢失; 队列顺序显示正常
- **根因**: `openCourseForm`(dashboard.vue ~3449) 每次打开/重开课程编辑弹窗都用 `epKey()` 给 episodes 生成**全新 _key** 重建数组; 若上传/排队途中重开弹窗, 队列里持有的 `ep` 对象引用脱离新数组 → 这些上传完成时把 video 写回已失效的旧对象(丢失), 只有重开后才入队的最后两个(活对象)才生效
- **修复(纯前端, 无需重部署云函数)**:
  1. `openCourseForm` 顶部加早返回: 同一课程(id 相同)且仍有 `currentUploadingEp`/`uploadQueue` 时, 直接 `showCourse=true` 复用内存表单, 不重建 episodes(防脱离); 切换不同课程仍走原清理逻辑
  2. `startUpload` 完成写回改为稳健命中: `liveEp = episodes.find(_key匹配) || episodes[indexOf(ep)] || ep`, 再 `liveEp.video=url`; 弹窗重建/课时重排也不丢视频
- **版本**: bump→v1.11.303, commit f6ef3e4, tag v1.11.303 -f; H5 构建 upload /h5/ 成功, 源站 `version.BCO4OMLH.js` 含 1.11.303 已 curl 校验 ✅; GitHub push 成功(d32e7e0..f6ef3e4 + 新 tag v1.11.303)
- **注**: 单弹窗会话(不重开)本就串行写回正常; 此 bug 触发条件是"上传/排队途中重开弹窗"。若用户未重开仍只生效最后两个, 需再排查单会话路径
