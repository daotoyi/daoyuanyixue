# 2026-08-27 工作日志

## 00:07 生成 APP (Android APK v1.11.208)

- **需求**: 生成 APP, 用当前已同步版本 v1.11.208 直接构建 (不单独升版, 符合 APK 构建策略)
- **流程** (按铁律): 确认四端均 208 (version.js/build.gradle/H5产物/tag) → 手动更新下载页 200→208 (sed 直接替换, 不用 bump --apk 避免误升 209) → cap sync → gradlew assembleRelease → unzip 验证 APK 内 version*.js = v1.11.208 → 复制 daoyuan-v1.11.208.apk(4.0M) → 上传 /apk/ + /download/
- **验证**: APK 200 / 下载页 Android v1.11.208 / H5 200
- **部署**: Git 1d37d1b 已 push (无新 tag, 版本仍是 208)
- **备忘**: 生成 APK 但版本不变时, 下载页用 python/sed 直接替换版本号即可, 勿跑 bump-version.js (会 patch+1)

## 07:39 首页推荐部分手机端尺寸恢复 v1.11.173 (fix)

- **问题**: 用户反馈首页推荐部分尺寸不对, 要恢复到 1.11.173 的尺寸; **PC 端 H5 保持不变**
- **根因**: v1.11.184 (b669b2a) 调整过推荐字体, 且 188-190 为 PC 放大卡片时也放大了 rpx 基础值(330rpx/195rpx/420rpx 动态精选)
- **修复** (index.vue, 仅改非 media query 基础样式, 颜色保留红色系):
  - 推荐卡片 330→220rpx / 图 195→130rpx / padding 18→12rpx / 圆角 21→14rpx / 标题 26→24rpx / 时间 22→20rpx / 价格 30→26rpx
  - 动态精选 420→280rpx / 图 225→150rpx / 标题 26→24rpx / min-height 108→72rpx
  - PC media query (>=1025px, 570px/660px) 完全不动
- **方法**: python 脚本按唯一文本块替换 (media query 内是 px+深缩进, 不会误匹配); fish shell 不支持 heredoc, 用写临时 .py 文件执行
- **部署**: v1.11.209, H5+小程序重建部署(未构建APK); Git a1fcebb + tag v1.11.209 已 push
- **验证**: 基础样式 grep 确认 220/130/24rpx; PC media 570px/660px 完好

## 10:50 首页推荐 PC/移动端独立显示彻底修复 v1.11.210 (fix)

- **问题**: 209 恢复移动端 173 尺寸后, 用户反馈 **PC 端尺寸也变小了**, 且此前多次尝试都失败
- **根本原因 (关键铁律)**: src/pages/index/index.vue 中 **rec- base 样式块位于 @media(min-width:1025px) 之后**! CSS 同优先级后出现者生效 → base 的 rpx 值(220rpx) 一直覆盖 media 的 570px 规则 → **PC media query 从未真正生效过**, PC 显示的从来是 base rpx 换算值 (208 时代=330rpx 换算, 209 改成 220rpx 后 PC 跟着变小)
- **修复**:
  1. rec- base 块(含 .pandao-cover/.rec-pd-img/.rec-moment-*)整体移到 @media 1025px **之前** → media 规则真正生效
  2. media 内 rec- 规则从 570px/660px 固定值改为 **v1.11.208 base 的 rpx 值**(卡片330rpx/图195rpx/标题26rpx/moment 420rpx/225rpx 等, 逐项取自 git show v1.11.208) → PC 显示与 208 线上逐像素一致
  3. base 保持 173 小尺寸(220rpx/130rpx) → 移动端 = 173
- **效果**: 两端彻底独立 — PC≥1025px 显示 330rpx(=208线上), 移动端显示 220rpx(=173); **以后改 base 不再影响 PC**
- **验证**: 编译产物 dist/build/h5/static/index2.css — base 6.875rem(220rpx)@2456 在 @media(14014) 前, media 10.3125rem(330rpx)@15593 在 @media1025 与 @media1440 之间 → 级联顺序正确
- **部署**: v1.11.210, H5+小程序已构建; Git cab7854 + tag v1.11.210 已 push; ⚠️ 线上部署待 CloudBase 设备码授权 (user_code=95D5-ZPKC 可能已过期, 需重新 start_auth)

## 11:00 v1.11.210 部署完成 (H5 线上)

- 用户完成设备码授权 (auth READY) → manageHosting upload dist/build/h5 → /h5/ 成功 (failedFiles=[])
- **线上验证**: version.DA6nNkPk.js = v1.11.210; index2.css 顺序正确 (base 6.875rem@2456 < media1025@14014 < 330rpx@15593 < media1440)
- **小程序**: 产物 dist/build/mp-weixin 已构建 (version.js=v1.11.210), 需用户在微信开发者工具导入上传, 版本号填 1.11.210
- 下载页未动 (APK 未重建, 保持 208 正确)

## 19:25 v1.11.211 三合一修改 (fix)

- **①删除已上架帛书德道经注释**: products 集合有两条"帛书德道经注释" — 一条 `_id=238cef266a747cb90137fc4f439e6f42` **无 id 字段**且 is_show=true(已上架), 另一条 id=37 is_show=false。后台编辑/删除按 id 定位 → 缺 id 的条目无法编辑删除。直接 `writeNoSqlDatabaseContent action=delete query={_id}` 删除上架那条, 已下架的 id=37 保留
- **②去掉登录设置提示**: dashboard.vue 登录设置卡片里 sd-text 长文案 "开启后登录注册页显示「微信一键登录」..." 整行删除, 保留开关和底部 tip
- **③玄学工具开关仅控制小程序**: user.vue `showTools` 改为条件编译 — `#ifdef MP-WEIXIN` 时 `cfg.show_tools===true`, `#ifndef MP-WEIXIN` 时恒 `true`(H5/App 始终显示)。dashboard.vue 玄学工具 tip 改为 "关闭后仅微信小程序不展示玄学工具，H5/App 正常显示"
- **验证**: 小程序产物 `w.value=!0===e.show_tools`(受开关控制) / H5 产物 `sa=!0`(恒显示) ✅
- **部署**: v1.11.211 H5 已上传线上 (version.Bq9cD2mA.js=v1.11.211); 小程序产物 dist/build/mp-weixin 待用户开发者工具上传; Git c2a38e1 + tag v1.11.211 已 push
- **备忘**: 后台商品/课程等按 id 定位编辑删除, 若条目缺 id 字段会无法操作, 需数据库直接修数据

## 19:50 v1.11.212 0价商品免费直接下单 (feat)

- **需求**: 后台盘道/课程等价格=0 → 前端显示"免费", 不拉起支付, 点击直接完成订单, 无"订单金额异常"提示
- **云函数** (dy-api/index.js):
  - 新增 `orderFreeConfirm`: 校验 total_price<=0 → syncSalesAfterPay 同步销量 → 状态置已完成/待发货 → 免费课程自动 buyCourse 发课 → pay_method='免费'; 重复调用防呆(非待付款拒绝)
  - 注册路由 `'order.freeConfirm'`
  - `pandaoBook` 免费场次直接创建"已完成"订单 (pay_method='免费', 返回 free:true)
- **前端**:
  - checkout.vue: 按 **subTotal<=0(原始商品价)** 判断免费(避免元宝抵扣到0被误判), 免费直接 orderFreeConfirm + 跳详情; 合计显示"免费"(st-free 绿色样式)
  - 价格显示免费: course/detail(立即学习), pandao/detail(免费报名+预约成功不跳支付), product/detail, shop 列表, 首页推荐(盘道/商品/课程) + 盘道tab, 订单列表/详情(合计免费+支付方式'免费'映射)
- **验证**: 云函数部署后 curl 网关 order.freeConfirm → "订单不存在"(路由生效), pandao.book → "活动场次不存在"(新代码生效)
- **部署**: v1.11.212, 云函数 dy-api 已更新 + H5 已上传 (version.CVa20p_z.js); 小程序产物待用户上传; Git 126b846 + tag 已 push
- **备忘**: HTTP 网关调用格式 `{action, data}` (不是 params); 元宝抵扣到0 ≠ 免费订单, 免费判断必须基于原始价格
