# 2026-08-20 工作日志

## v1.11.175 老微信用户强制绑定手机号 (08-19 晚完成)

- 需求: 之前微信一键登录的老用户重新登录时必须先绑定手机号
- 云函数 wechatCheck 返回增加 needPhone (!user.phone); wechatLogin 老用户(!user.phone && !phone) 兜底拒绝 fail('请先绑定手机号后登录')
- 前端 login.vue: wxLogin 老用户 needPhone → 弹授权面板强制绑定(wxNeedPhone ref); 面板手机号按钮文案"绑定手机号(必填)", 登录按钮禁用态(wx-auth-disabled)直到绑定; doWxLogin 兜底拦截; wxLoginConfirm 引导
- 提交 a4063da; 云函数+H5 已部署

## v1.11.176 我的元宝充值记录 (08-19 晚完成, 08-20 晨部署)

- assets.vue (我的元宝页) 新增"充值记录"区块: 订单号/时间/金额(元+元宝数)/状态, 深色卡片下方
- 数据源: getOrders({uid}) 过滤 order_type==='recharge' || order_no RC 前缀 (listOrders 返回全部订单, 前端过滤)
- 时间显示: 新增 fmtUtcTime(s) — Date.UTC 构造 +8h 转东八区 (浏览器本地时区会干扰 Date.parse, 必须用 Date.UTC 避免)
- onShow 刷新 (充值支付返回自动更新)
- 提交 a80221a; H5 已部署 (认证过期需重新设备码授权: auth action=start_auth → 浏览器 tcb.cloud.tencent.com/dev#/cli-auth 授权 → auth action=status READY)

## v1.11.177 道号登录 + 登录设置独立卡片 + 表头对齐修复

- **道号登录**: 云函数 login 改为 isEmail ? email 查 : _.or([{phone}, {dao_code: acc.toUpperCase()}]) 查询, 道号忽略大小写; 前端登录模式标签/placeholder "手机号/邮箱/道号", submit 校验放行道号(/^[A-Za-z0-9]{3,20}$/), 注册模式保持手机号/邮箱; H5 快捷登录弹窗 placeholder 同步
- **登录设置独立卡片**: 从"首页显示"卡片挪出, 单独 settings-card "登录设置"(sd-title), 开关"显示微信一键登录" show_wechat_login 默认关, 打开后登录页微信一键登录与手机号快捷登录上下排列
- **表头对齐修复(重要铁律)**:
  - 根因1: v1.11.173 加的 `.tr.th { width:max-content; min-width:100% }` 破坏所有表头 flex 对齐(max-content 按内容宽, 数据行 flex 分配) → 移除
  - 根因2: 表头"操作/首页推荐"列无条件显示, 数据行 v-if="canWrite/canManageCourses/canManageUsers/canManageSettings" → 表头同步加 v-if (订单/产品/课程/售后/用户/直播/动态/优惠券/反馈/wxmp 全部)
  - 影响: 只读管理员登录后台时表头不再多出操作列
- 验证: user.login 传道号返回"账号或密码不正确"(路由通); 提交 cd3eae2; 云函数+H5 已部署

## v1.11.178 微信老用户默认密码 + 登录设置卡片位置

- **微信老用户默认密码**: login 重构——先查用户(不带 password 条件, email/phone/dao_code 三路), 再校验: 已设密码按密码; 未设密码(微信一键登录老用户) 默认密码 **123456**; 首次用默认密码登录后落库 password='123456' (懒加载, 无需批量迁移, 避免 update 整体替换风险)
- 验证: curl user.login ZHSM555/123456 → status 200 返回用户(uid 120395, dao_code ZHSM555, nickname daotoyi) ✓
- **登录设置卡片**: 从"首页显示"与"推荐页展示"之间移到"我的管理"卡片之后; 补 settings-tip("默认关闭，开启后与手机号快捷登录上下排列") 使保存按钮靠右(settings-actions flex space-between 需要 tip 占位, 否则按钮靠左)
- 提交 3efb5b9; 云函数+H5 已部署; GitHub 已推送

## v1.11.179 饼图颜色 bug + 自定义域名绑定准备

- **饼图只显示两种颜色根因**: `angle: seg * 3.6` 错误 — seg 是弧长(ratio × PIE_C≈251.33), seg*3.6 ≈ ratio×904°, 放大 2.5 倍导致前两段就超 360° 盖住后续色块 → 改为 `angle: ratio * 360` (dashboard.vue pieSegments)
- 已构建 H5+小程序并部署 H5 (v1.11.179), 本地 commit a3b7fe3
- **MCP 反复下载修复**: npx 缓存损坏导致每次启动重下 cloudbase-mcp → 本地安装到项目 `.mcp/cloudbase-mcp`(dist/cli.cjs), config/mcporter.json 改 node 直接执行本地文件, 秒启动
- **自定义域名绑定**(用户新需求): zhenhesheng.cn 已备案, DNS 在 DNSPod; 目标 club.zhenhesheng.cn 指向静态托管 staticstore; 流程 = ①用户申请 TrustAsia DV 免费证书(SSL 控制台, 要 CertId) ②MCP manageGateway bindCustomDomain(需 certificateId) ③用户 DNSPod 改 CNAME ④MCP createRoute(club.zhenhesheng.cn / → STATIC_STORE staticstore) ⑤验证。当前环境 0 自定义域名, 静态托管实例名 staticstore
- 指引文档: docs/自定义域名绑定指引.md (等用户提供 CertId 后执行 ②④)

## v1.11.179 APK 发布修复 (08-20 晚)

- **用户痛点**: 每次打开 H5/下载页都弹下载 daoyuan-v1.11.153.apk
- **根因**: 线上 /apk/ 目录堆了 31 个历史 APK(1.9.4~2.0.2), 最新只有 1.11.174; 175~179 迭代只更新了下载页 HTML, **从未重新发布 APK 文件**(bump-version.js 只改链接, APK 需手动复制 app-release.apk 为 daoyuan-vX.apk 再上传)
- **修复**: ①上传 /apk/daoyuan-v1.11.179.apk (本地 mobile/android/app/build/outputs/apk/release/app-release.apk) ②删除全部 31 个旧 APK(旧链接全部 404) ③重新上传 /download/index.html (CDN 缓存已刷新到 179)
- **验证**: 153/174 → 404, 179 → 200, 下载页显示 v1.11.179
- **MCP 代理坑 (重要铁律)**: mcporter daemon 在带 http_proxy=127.0.0.1:62838 的坏环境下启动后, 所有腾讯云 API 调用 ECONNREFUSED 127.0.0.1:62838 (静态托管类尤其) → 修复: kill daemon 进程 + rm ~/.mcporter/daemon/*.sock, 用 `env -u http_proxy -u https_proxy -u HTTP_PROXY -u HTTPS_PROXY -u all_proxy -u ALL_PROXY npx mcporter call ...` 干净重启。auth 类调用不受影响, 会误以为 MCP 正常
- 提交 14bb767; GitHub 已推送
