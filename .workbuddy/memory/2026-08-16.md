# 2026-08-16 工作日志

## v1.11.144: 动态发布权限开关

- **需求**: 后台"动态管理"增加控制普通用户/员工是否允许发布动态的开关，超管/管理员始终可发布
- **云函数 dy-api**:
  - `SETTINGS_GROUPS` 新增 `'moment'` 组
  - `appPayConfig()` 读取 `moment` 组 `allow_publish_moment`（默认允许，`!== '0'` 判断）
  - `publishMoment()` 服务端权限校验：查用户 role，非 admin/manager 且开关关闭 → fail('当前暂未开放动态发布，请联系管理员')
- **后台 dashboard.vue**:
  - 动态管理模块顶部新增"允许发布动态"开关 + 保存设置按钮
  - 新增 `momentCfg` ref、`loadMomentConfig()`、`saveMomentConfig()`（存 moment 组）
  - `loadModule('moments')` 时同时加载 moment 配置
- **前端 index.vue**:
  - 新增 `allowPublishMoment` ref + `canPublishMoment` computed（admin/manager 始终 true）
  - 发布悬浮按钮 `v-if` 加 `canPublishMoment` 条件；`goPublish()` 增加权限拦截提示
- **前端 publish.vue**:
  - `onMounted` 获取 payConfig 判断权限，`canPublish` ref
  - 无权限时隐藏输入/发布按钮，显示 🔒 "当前暂未开放动态发布" 提示
- **部署**: 云函数已部署 + H5 已上传（111 文件），提交 e855a06 推送 GitHub 成功

## v1.11.145: 开关一行显示

- 后台动态管理「允许发布动态」改为**标签+开关+保存按钮一行**（flex row），提示文字独立一行
- 新增样式 `.moment-cfg-row / .moment-cfg-label / .moment-cfg-save`
- 提交 9a34721（push 网络波动，本地 commit 已完成）

## v1.11.146: Android APK 构建发布

- 用户单独提出"生成app"需求（按约定 APK 单独提需求才构建）
- 流程: bump-version → gen-version → uni build h5 → cap sync android → gradlew assembleRelease（34s 成功）
- 产物: dist/build/h5/apk/daoyuan-v1.11.146.apk (4.2M) + download/index.html（bump 自动更新版本）
- 上传静态托管成功, 验证: APK 200 / 下载页 200
- 下载地址: https://cloud1-d8gs2k9m311f7272f-1464523137.tcloudbaseapp.com/apk/daoyuan-v1.11.146.apk
- 提交 2c5db64（push 443 超时，本地 commit 完成，待网络恢复重推）

## v1.11.147: 四合一功能 (公众号唤起/订单取消退款/元宝支付修复/App跳小程序支付)

- **①公众号/服务号关注 (H5+App)**: 不再直跳 mp.weixin.qq.com（浏览器报"请在微信客户端打开"），改为复制微信号/绑定链接 + 弹窗"打开微信"按钮 → `weixin://` scheme 唤起微信 App（`openWeixinApp()`; H5 用 location.href, App 用 plus.runtime.openURL）；小程序端保持 wx.openOfficialAccountProfile
- **②订单取消扩展到"待发货"**: cancelOrder 允许待发货取消 → 自动退款（元宝余额退回 / 微信退款 v3 `wxpay.refund`，复用 pandaoCancel 模式）；订单置"已退款"+refund_at；list.vue/detail.vue 待发货显示"取消订单"按钮（detail 用 btn-cancel 已有样式, list 新增 btn-cancel 样式）
- **③元宝支付不扣减 bug 修复**: checkout.vue 元宝支付原调 payOrder（仅 balance_used>0 才扣，用户选元宝支付但未勾抵扣时 balance_used=0 → 不扣）；改为 `orderPayBalance({order_no, uid})` 按 total_price 真实扣减（与 detail.vue 一致）
- **④App 端微信支付跳小程序**: 云函数新增 `pay.wxmpScheme`（wxa/generatescheme 生成 openlink, 30天有效, path=pages-sub/order/detail?order_no=, 依赖 config.local WX_APPSECRET + getWxAccessToken）；checkout.vue/detail.vue 的 APP-PLUS 分支 plus.runtime.openURL(openlink) 唤起小程序完成支付；H5 保持收银台/提示；小程序保持 JSAPI 直付
- **部署**: 云函数已部署（路由必须重部署）+ H5 已上传；接口测试 pay.wxmpScheme 返回"订单不存在"（路由生效）
- 提交 026b8ce 推送 GitHub 成功

## v1.11.148: Android APK 构建发布 (含 v1.11.147 全部功能)

- 用户再次提出"生成app"，打包流程同 v1.11.146（bump → gen → build h5 → cap sync → gradlew assembleRelease 27s 成功）
- 产物: dist/build/h5/apk/daoyuan-v1.11.148.apk (4.2M) + download/index.html
- 上传静态托管成功, 验证: APK 200 / 下载页 200
- 下载地址: https://cloud1-d8gs2k9m311f7272f-1464523137.tcloudbaseapp.com/apk/daoyuan-v1.11.148.apk
- 提交 bf46222（push 后台进行中）
