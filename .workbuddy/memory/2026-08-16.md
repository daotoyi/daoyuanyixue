# 2026-08-16 工作日志

## v1.11.144: 动态发布权限开关

- **需求**: 后台"动态管理"增加控制普通用户/员工是否允许发布动态的开关，超管/管理员始终可发布
- **云函数 dy-api**:
  - `SETTINGS_GROUPS` 新增 `'moment'` 组
  - `appPayConfig()` 读取 `moment` 组 `allow_publish_moment`（默认允许，`!== '0'` 判断）
  - `publishMoment()` 服务端权限校验：查用户 role，非 admin/manager 且开关关闭 → fail('当前暂未开放动态发布，请联系管理员')
- **后台 dashboard.vue**:
  - 动态管理模块顶部新增"允许发布动态"开关 + 保存设置按钮
  - 新增 `momentCfg` ref、`loadMomentConfig()`、`saveMomentConfig()`（存 moment 组）
  - `loadModule('moments')` 时同时加载 moment 配置
- **前端 index.vue**:
  - 新增 `allowPublishMoment` ref + `canPublishMoment` computed（admin/manager 始终 true）
  - 发布悬浮按钮 `v-if` 加 `canPublishMoment` 条件；`goPublish()` 增加权限拦截提示
- **前端 publish.vue**:
  - `onMounted` 获取 payConfig 判断权限，`canPublish` ref
  - 无权限时隐藏输入/发布按钮，显示 🔒 "当前暂未开放动态发布" 提示
- **部署**: 云函数已部署 + H5 已上传（111 文件），提交 e855a06 推送 GitHub 成功
