# 2026-09-06 工作日志

## 玄学工具按端独立开关 (v1.12.11)
- **需求**: 后台「我的管理→玄学工具」原只有一个总开关(仅控微信小程序, H5/App 强制显示); 改为三端独立总开关, 前端同步后台显示
- **实现**:
  - `settings.mypage` 分组新增字段 `show_tools_app`(App端) / `show_tools_h5`(H5), 与既有 `show_tools`(微信小程序)并列; 存 `'1'`/`'0'`
  - 后端 `app.payConfig`(index.js ~5080) 返回三字段: 默认 微信小程序=false, App=true, H5=true
  - 后台 `dashboard.vue` 我的管理→玄学工具 卡片改为三个 switch(微信小程序/App端/H5), `homeCfg` 增两字段, 加载/保存 `mypage` 三字段
  - 前端 `user.vue` onLoad 按 `#ifdef` 分支: MP-WEIXIN→show_tools, APP-PLUS→show_tools_app, H5→show_tools_h5; catch 按各端默认(微信小程序关/App·H5 开)兜底
- **部署**: dy-api 重部署; H5 部署 /h5/; 小程序 dist/build/mp-weixin 已构建(需开发者工具上传 1.12.11); 线上校验 version chunk=v1.12.11, app.payConfig 返回三字段正确, 官网根 200 ✅
- **注意**: 玄学工具页(user/tools)非 tabBar, 仅由「我的」页面板入口控制, 关闭即隐藏; 若要彻底禁 App 端需单独打 App 包(本次未打)

## App 打包 (v1.12.11 APK)
- **流程**: `node node_modules/.bin/uni build`(H5构建) → `cd mobile && node node_modules/.bin/cap sync android` → `cd mobile/android && ./gradlew assembleRelease --no-daemon`(BUILD SUCCESSFUL 33s)
- **产物**: `mobile/android/app/build/outputs/apk/release/app-release.apk` (4.1M), 已复制至 `dist/build/h5/apk/app-release.apk` (发布源)
- **校验**: `unzip -p ... assets/public/assets/version*.js` 内部版本 = v1.12.11 ✅; build.gradle versionName "1.12.11" versionCode 431; Java 21 Temurin, Gradle 8.14.3
- **架构事实(重要)**: 本项目 App 走 Capacitor, `webDir=../dist/build/h5`, 即 App 包的是 **H5 构建产物**, 故 App 内实际生效 `#ifdef H5` 分支(由 `show_tools_h5` 控制); v1.12.11 加的 `#ifdef APP-PLUS` 分支在 Capacitor 打包链不被编译进去 — 已如实告知用户
- **已发布(2026-09-06 续)**: 按用户要求下载页只链 `/h5/apk/`, 不链 `/apk/`
  - 改跨项目 `zhs-website/zhenhesheng.cn/download.html`: 下载链接 → `https://club.zhenhesheng.cn/h5/apk/daoyuan-v1.12.11.apk`, 版本号 v1.11.179→v1.12.11; 部署到 `/download/index.html`(线上 /download 即返回它)
  - APK 上传到 `/h5/apk/daoyuan-v1.12.11.apk`(http 200, 4.07MB); **不再传 `/apk/`**
  - 验证: /download 页面链接与版本已更新, APK URL 200 ✅
  - 坑: 单文件 upload cloudPath 必须以文件名结尾(否则存成 `h5/apk/` 脏对象, URL 404), 已重传修正; 该 `h5/apk/` 脏对象(4MB 误传)已于 2026-09-06 经 mcporter `manageHosting action=delete cloudPath=/h5/apk/ confirm=true` 删除成功(delete 破坏性操作需 confirm=true), 复查 h5/apk/ 下历史 daoyuan-v1.*.apk 与 daoyuan-v1.12.11.apk 均完好 ✅

## 玄学工具改为单一总开关 (v1.12.12 → v1.12.13)
- **需求**: 用户要求后台「我的管理→玄学工具」的三个独立开关(微信小程序/App/H5)合并为**一个总开关**, 同时控制三端
- **实现**:
  - 后端 `app.payConfig`(index.js ~5059-5082): 新增 `masterTools` 计算, 让 `show_tools`/`show_tools_app`/`show_tools_h5` 三字段恒等于总开关值; `user.vue` 按端读取逻辑无需改(自然全部跟随)
  - 后台 `dashboard.vue`: 三 switch 合并为一个 `玄学工具（总开关）`; `homeCfg` 仅留 `show_tools`; 加载用 `masterToolsOf(mp)` 辅助函数; 保存只写单字段 `show_tools`(同时把 app/h5 也写成同值保持存储一致)
  - `masterToolsOf` 与后端规则一致: `show_tools==='1'/true`→开; `==='0'/false`→关; **未显式设置时默认开启**(避免线上原 App/H5 已显示的玄学工具被误隐藏)
- **部署**: dy-api 重部署; H5 部署 /h5/; 小程序 dist/build/mp-weixin 已构建(需开发者工具上传 1.12.13); 线上校验 version chunk=v1.12.13(http 200), app.payConfig 三字段恒等, 网关存活 ✅
- **踩坑/铁律**:
  - v1.12.12 首版 masterTools 在未设置时 `inherit app/h5` → 因 v1.12.11 的 app/h5 只是计算默认(未真正写入存储), 读到 undefined → false, 导致线上三端工具全被隐藏(回归). 升 v1.12.13 修正为「未设置默认开启」
  - **线上现状**: `settings.mypage.show_tools` 存了 `'0'`(v1.12.11 时期遗留, 即当时把微信小程序端设为关), 故总开关当前=关; 用户在后台「我的管理」打开即三端同时显示. 已如实告知, 未擅自改存储
  - 版本: bump-version.js(patch+1) + gen-version.js; 每次修复必升小版本; commit + `git tag -f vX` 移到发布提交 + 推 main 与 tag

## 下载页两处同步为最新 (2026-09-06 续)
- **用户决策**: "还是两处都同步到最新的" → 不删除旧副本, 把 `/h5/download/index.html` 也同步成最新 download.html 内容
- **现状(同步前)**: 线上有 2 个下载页: `/download/index.html`(最新, 链 h5/apk/daoyuan-v1.12.11.apk, v1.12.11) 与 `/h5/download/index.html`(僵尸旧版, 还链 zhenhesheng.cn/apk/daoyuan-v1.11.278.apk)
- **操作**: 上传 `zhs-website/download.html` 到 `/h5/download/index.html`(successCount:1); 验证两页现均返回 h5/apk/daoyuan-v1.12.11.apk + v1.12.11 ✅
- **铁律更新(2026-09-06 再改)**: 下载页部署改为**只更新 `/h5/download/index.html`**(用户明确要求, 不再两处都传, 也不再动根目录 `/download/index.html`); 同源 download.html; APK 链接一律 `/h5/apk/daoyuan-vX.Y.Z.apk`, 不再用 `/apk/` 或旧域名 zhenhesheng.cn
- **用户删除根目录旧入口(2026-09-06 续)**: 用户删除线上 `/download/index.html` 旧入口; 核实 club.zhenhesheng.cn/download → 404, club.zhenhesheng.cn/h5/download → 200; 跨项目源 `zhs-website/zhenhesheng.cn/download.html`(3.9k) 完好 → 后续改下载页仍以它为源, 只部署到 `/h5/download/index.html`; 现状: 下载页唯一线上入口 = /h5/download/index.html(最新 v1.12.11), 无过时旧入口残留

## 修正"前往更新"链接指向已删 /download (v1.12.14)
- **排查**: src 里唯一直指已删 `/download` 的代码 = `setting.vue:356` "检查更新→前往更新" H5 兜底 `window.open(info.url || '.../download/')`; 且后端 `checkUpdate` 返回的 `info.url` 写死 **测试域名 + /download/**(`cloud1-...-1464523137.tcloudbaseapp.com/download/`) → info.url 有值时优先于前端兜底, 旧入口删后用户点更新必 404 + 弹测试域名警告
- **修复**: 前端兜底改 `https://club.zhenhesheng.cn/h5/download/`; 后端 dy-api `checkUpdate` url 改 `https://club.zhenhesheng.cn/h5/download/`(去掉测试域名); 升 v1.12.14, 构建 H5+小程序, 部署 dy-api+H5, commit 9ebcea4 + tag v1.12.14 + push
- **校验**: `app.checkUpdate` 返回 url = club.zhenhesheng.cn/h5/download/ ✅; H5 线上版本 chunk = v1.12.14(入口 index-C-u_01NX.js 引用 version.DjfmQ-Xo.js) ✅
- **分享按钮链接核查结论**: 全部分享链接(盘道详情 `h5/#/pages-sub/pandao/detail`、动态 `h5/#/pages/index/index?moment=`、邀请 `h5/#/pages-sub/login/login?invite=`、公众号绑定 `gzh-bind.html`)均指向 `/h5/` 或 club 根有效页, **无指向 /download 的, 无需改动**

## 下载页改版(纯静态, 跨项目 zhs-website/download.html, 仅部署 /h5/download/index.html)
- **背景**: cream 渐变 → 中国红渐变 `linear-gradient(165deg, #c41e3a 0%, #9c1630 100%)`(品牌红); body 去掉 `justify-content:center` 改 `flex-start`, 上下留白 `7vh`, 卡片上下内边距 `48/40→34/30`, 手机端需下滑
- **标签**: `周易排盘→河洛文化`; 去 `经典课程`; 增 `奇门遁甲/五行术数/道家文创` → 现 `河洛文化/八字命理/奇门遁甲/五行术数/道家文创`; 同步改 meta description
- **微信提示**: `.tip` 默认 `display:none`, 加 `<script>` 仅 `/MicroMessenger/i` UA 时显示「微信内无法下载? 右上角···→在浏览器打开 / 扫码登录 H5」; 非微信浏览器不显示
- **部署**: manageHosting upload → `/h5/download/index.html`; 线上校验 5 标签正确+旧标签消失+红背景+MicroMessenger 脚本 ✅ (版本文本仍 v1.12.11, 未重打 APK)
