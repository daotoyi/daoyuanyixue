# 2026-08-31 工作日志

## v1.11.268: 根治排队不出队 (竞态+对象引用双bug)
- **用户反馈**: 有2个排队任务但没有活跃上传，队列不自动出队
- **根因分析**:
  1. **竞态条件**: `dequeueNext` 是 async，先检查 `currentUploadingEp` 锁再 `await startUpload`，但锁在 `startUpload` 内部才设置 → 取消回调和 finally 同时触发 `dequeueNext` 时两个异步调用互相覆盖 → 队列卡死
  2. **对象引用过期**: 队列存的 ep 是旧 JavaScript 对象，关闭/重开弹窗后 `openCourseForm` 重建 episodes 数组(`map` 创建新对象)，队列里的 ep 指向不可见的旧对象 → 上传在后台跑但 UI 看不到
- **修复**:
  1. `_dequeueRunning` 同步锁 + `currentUploadingEp = ep` 提前到 `await startUpload` 之前 → 防竞态双保险
  2. `dequeueNext` 按 `_key` 映射到当前 `courseForm.episodes` 中的对象 → 防旧引用
  3. `removeFromQueue` 按 `_key` 匹配 → 防旧对象引用删不掉
  4. `startUpload` 入口防御: 若旧 `currentUploadingEp` 已不是上传中状态，清空避免死锁
  5. `dequeueNext` finally 中残留检测: 队列还有任务但无上传中时重新触发
- **部署**: H5 已部署 (线上 index-DvIaxJqV.js) + 小程序已构建
- **版本**: v1.11.268 (commit f3bdd20)

## v1.11.269: 根治分片上传卡死在26% (AbortController 不万能 → 硬超时)
- **用户反馈**: 当前上传任务又卡死了，半天进度条不变化。书法艺术第一课时一直26%
- **根因 (关键教训)**: 代理/VPN 网络下单个分片 PUT 请求**挂起(hang)**而非失败，
  `setTimeout(() => controller.abort(), PUT_TIMEOUT_MS)` **不产生 reject** —
  fetch 在挂起态 abort 可能不触发 promise reject → 整批 `Promise.allSettled` 永不 settle
  → onProgress 不再被调用 → 进度永久冻结在某一百分比(26%)，且用户点取消/暂停也无反应
- **修复: 硬超时模式 (不依赖 abort 是否生效, 到点必定 reject)**
  1. **分片 PUT** (`uploadMultipartToCos` 内): 用 `Promise.race([fetch(...), 硬超时Promise])`
     在 `PUT_TIMEOUT_MS + 3000` (93s) 强制 reject(code=HARD_TIMEOUT) → 进入既有失败→重试轮次
  2. **`apiRequest` (云函数)**: 同样加 `Promise.race` 硬超时 `timeoutMs + 3000`，
     防护"取签名/listParts/completeMultipart"挂起拖垮整段上传
- **铁律**: **AbortController 在网络挂起(hang)场景下不保证 reject** —
  凡涉及网络的超时兜底，必须用 `Promise.race([请求, 定时reject])` 硬超时，不能只靠 abort
- **部署**: H5 已部署并**已验证** (线上 index-B4uisOtY.js + version chunk 含 v1.11.269，与本地一致)
- **小程序**: 已构建 (version.js = v1.11.269)；**CLI upload 挂在等 DevTools IDE 登录**，
  需用户在微信开发者工具 GUI 手动上传，或用已登录的 IDE 重跑 cli upload
- **GitHub**: push 超时(网络)，本地 commit 已完成，网络恢复后重推
- **版本**: v1.11.269 (commit 92e75f0)

## GitHub 推送被拒: 不是分支规则, 是密钥扫描 (GH013)
- **现象**: `git push origin main` → `remote rejected (repository rule violations)`
  (注意: 之前几次是网络超时, 这次网络通了才暴露出真实原因 —— 两种错误要区分开)
- **真实原因**: **GitHub 密钥扫描推送保护 (GH013, Push cannot contain secrets)**,
  而非分支保护/PR 规则。**PR 方式绕过不了**(推送分支时同样会被扫描拦截)
- **命中内容**: 腾讯云短信 **SecretId/SecretKey** 明文写在 `.workbuddy/memory/2026-08-29.md:94`
  (agent 记忆笔记里记了用户提供的密钥)，涉及 5 个未推送提交: 81cf0aa/1e14d32/e028834/1cb217b/40e6c31
- **泄露范围核查**: 远端 `origin/main` 上的同名文件是 87 行的旧版本, **AKID 计数=0**,
  其中的 "SecretKey" 只是字段名引用 —— **密钥尚未泄露到 GitHub**，仅存在于本地未推送提交
- **仓库可见性**: `daotoyi/daoyuanyixue` = **public 公开仓库** → 一旦放行, 密钥将全网可见,
  且腾讯云短信凭据可被机器人扫描盗刷扣费
- **排查命令备忘**: `git push origin main 2>&1 | head -30` 看完整 GH013 详情(含放行URL);
  `git show origin/main:<file> | grep -c AKID` 判断是否已泄露; `git branch -r --contains <sha>` 判断提交是否已推送
- **教训**: **agent 记忆笔记(.workbuddy/memory/*.md)不要记录明文密钥/Cookie/Token** —— 会被提交进仓库并被密钥扫描拦截
- **轮换影响面(重要)**: 该密钥同时被 settings 集合的 **sms 组**(在用的腾讯云短信) 和 **oss 组** 使用。
  但 oss 组是 **Aliyun 占位配置**(dy-api/index.js 里拼的是 `bucket.region.aliyuncs.com`,
  且 domain=`cdn.example.com` 明显是占位符, 实际未启用; 真实存储走 CloudBase 自带 cloud:// 存储) 
  → **轮换只需更新 sms 组, oss 组为死配置不影响功能**
- **读库命令**: `npx -y mcporter call cloudbase.readNoSqlDatabaseContent action=find envId=... collectionName=settings`
  (写库仍是 writeNoSqlDatabaseContent action=update, 注意整体替换语义要带全字段)

## v1.11.270: 后台版本号位置调整 (修复 vv 双写)
- **用户反馈**: 后台顶部每个功能菜单后面都显示版本号 "vv1.11.268" → 改为 v1.11.268 格式, 并移到「道元易学·管理后台」后面
- **根因**: 顶栏 `tb-version` 写的是 `v{{ APP_VERSION }}`, 而 **APP_VERSION 本身已含 'v' 前缀** → 渲染成 "vv1.11.x"。
  且它跟在 `currentModule.label`(当前模块名)后面 → 切菜单时版本号"跟着每个菜单出现一次"
- **修复** (dashboard.vue):
  1. 顶栏删除 `tb-version` 整行 (含 .tb-version / .tb-version:hover CSS 一并清理)
  2. 侧边栏 logo 区 `logo-sub` 由单个 `<text>管理后台（{{ verNum }}）</text>` 改为 `<view>` 容器:
     `<text class="logo-sub-txt">管理后台</text>` + `<text class="logo-ver" @tap.stop="showVersion">v{{ verNum }}</text>`
     → 显示「管理后台 v1.11.270」, 点击仍可看 commit/构建日期
  3. **折叠态保留版本号**: `<text v-else class="logo-ver">v{{ verNum }}</text>` —— 侧边栏收起时徽标不消失, 避免失去 showVersion 入口
  4. 新增 .logo-ver 胶囊样式(深色侧边栏用浅色描边); 响应式 `@media(min-width:800px)` 里
     `.logo-sub { font-size:12px }` 改为 `.logo-sub-txt{12px} + .logo-ver{11px}` (容器变 flex 后字号要下放到子元素)
- **坑**: **`APP_VERSION` 自带 'v' 前缀 → 模板里不要再手加 v**; 需要纯数字用 `verNum = APP_VERSION.replace(/^v/,'')`(已有)
- **部署**: H5 已部署并验证 (线上 index-DsC7rtm_.js + version chunk = v1.11.270) + 小程序已构建
- **版本**: v1.11.270 (commit 2b66d90)

## v1.11.271: 根治图片上传失败 (盘道封面) —— 网关体积上限踩坑
- **用户反馈**: 后台盘道界面「上传封面」失败
- **实测数据 (关键, 用脚本直接打线上接口得出)**:
  | base64 长度 | 结果 |
  |---|---|
  | 14,572 | 200 ✅ |
  | 90,320 | 200 ✅ |
  | **100,184** | **200 ✅ (上限附近)** |
  | **109,908** | **413 ❌ EXCEED_MAX_PAYLOAD_SIZE** |
  | 271K / 256K | 413 ❌ |
- **根因 (阈值死区)**: `cloudbase.js` 里判断是否走云函数中转的阈值写的是 `base64.length < 110000`,
  但**云函数网关请求体上限实为约 100KB**。而压缩器 `compressImageToBase64(filePath, 80)`
  的目标是把图压到 80KB → **base64 上限约 109KB**, 正好落在
  「网关拒收(≈100KB) 与 旧阈值(110000)」之间的**死区** → 稍大/细节多的封面图**必然 413**。
  小图(<100KB)反而正常, 所以表现为"有时能传有时不能"。
- **修复** (src/api/cloudbase.js, 公共上传层 → 头像/商品/课程/直播封面/动态图片全部受益):
  1. 新增常量 `BASE64_GATEWAY_LIMIT = 90000` (给 JSON 包裹字段留 ~10KB 余量), 替换硬编码 110000
  2. **云函数中转失败不再直接抛错, 而是降级走 COS 直传** (用 try/catch 包住 uploadBase64 分支)
     → 即使阈值判断仍有边界误差或网关抖动, 上传也能成功, 不会卡死在"上传失败"
  3. 降级链路已**端到端实测可用**: `storage.getUploadUrl` → 200 (返回 url/token/authorization/fileId)
     → POST multipart 到 COS → **HTTP 204 成功** (199KB 原图实测)
- **排查方法论**: 遇到"上传失败"别只看前端代码, 用脚本**直接打线上接口探测体积边界**最快定位;
  压缩目标值 与 网关上限 与 前端分支阈值 **三者必须自洽**, 否则必然出现死区
- **部署**: H5 已部署并验证 (线上 index-BzVBsr8G.js + version chunk = v1.11.271, 产物内 9e4 新阈值在、110000 已消失) + 小程序已构建
- **版本**: v1.11.271 (commit af567c7)

## v1.11.272: 盘道详情页「已预约用户头像墙」
- **需求**: 预约盘道活动后, **不论有没有支付成功**, 详情页「活动介绍」板块**上面**显示预约用户头像, 点击头像进用户主页
- **数据链路**: 预约记录存在 `orders` 集合, 条件 `order_type:'appointment'` + `session_id`;
  状态: 免费/已付=`已完成`, 未付=`待付款`, 取消=`已取消`/`已退款`(未付取消会直接 remove 订单)
- **实现**:
  1. 云函数新增 `pandaoBookers`(路由 `pandao.bookers`): 查 orders → **排除已取消/已退款** →
     按 created_at 倒序**同用户去重** → `_.in(uids)` 批量查 users → 返回 `{uid,nickname,avatar,paid}`
  2. `src/api/api.js` 加 `getPandaoBookers`
  3. `pages-sub/pandao/detail.vue`: 头像墙插在「活动介绍」之上; 最多显示 10 个(超出折叠为 `+N`, 可展开/收起);
     点击 → `uni.navigateTo('/pages-sub/user/profile?uid=')`。**头像必须 `resolveCloudUrl` 转签名 URL**(私有桶直渲染 403)
  4. 免费预约成功 / 取消预约后调 `loadBookers()` 刷新; 加载不 await, 失败置空不影响详情主内容
- **CSS 顺序铁律复现 (重要)**: 本文件 detail.vue 的 PC `@media(min-width:1025px)` 位于 style 块**最前面**,
  而 base 规则在其**后** → 同优先级下 base 会覆盖 media(该文件原有的 `.pd-cover` 等 PC 覆盖实际是失效的)。
  新增样式时**把 PC 覆盖单独写成新的 media 块放在 style 末尾**(保证 base 在 media 之前)才生效
- **部署完成**: 云函数 dy-api 已重新部署 + H5 已部署并验证 (线上 index-Ddkmhk-U.js + version chunk = v1.11.272) + 小程序待构建
- **线上实测 pandao.bookers (确认"不论是否支付成功"生效)**:
  | 场次 | 预约数 | 备注 |
  |---|---|---|
  | 3 | 2 | 含 uid=3 **paid=False**(未支付也展示 ✅) |
  | 4 | 5 | 含 uid=120395 **paid=False** ✅ |
  | 5 | 1 | paid=False ✅ |
  | 6/7 | 0 | 无人预约 |
  缺 session_id → 400「缺少场次」(优雅报错)
- **踩坑**: 测试脚本里把云函数网关域名写成静态托管域名
  (`...ap-shanghai.app.tcloudbase**app**.com`) → SSL CERTIFICATE_VERIFY_FAILED hostname mismatch。
  **云函数网关是 `...ap-shanghai.app.tcloudbase.com`(无 app 后缀), 静态托管才是 tcloudbaseapp.com**, 两者别混
- **版本**: v1.11.272 (commit d4c4b8b)

## v1.11.273: 修复盘道详情页 PC 媒体查询长期失效
- **承接**: v1.11.272 发现 detail.vue 的 PC `@media(min-width:1025px)` 写在 style 块**最前面**,
  base 规则在其后 → 同优先级下 base 覆盖 media → **`.pd-cover` 16:9、`.pd-title` 32px 等 PC 覆盖从未生效**;
  用户确认后一并修复
- **修复**: 删除 style 块顶部那个 media 块, 将其内容(.pd-page/.pd-cover/.pd-title/.pd-meta-item/
  .pd-content-title/.pd-content-text/.pd-book-bar)**合并进 style 末尾的 PC media 块**, 保证 base 在 media 之前
- **验证方式 (可复用)**: 页面 scoped CSS 编译产物在 **`dist/build/h5/static/detail.css`**(不是 assets/*.js,
  JS 里只有渲染函数的 class 名)。用脚本比对 `1025` 与 `.pd-cover` 的下标即可确认顺序:
  线上实测 media=3668 > base=207 ✅, 且 `aspect-ratio:16/9` 已存在于线上 CSS
- **部署**: H5 已部署并验证 (线上 index-D1Sl4vHr.js + version chunk = v1.11.273) + 小程序已构建
- **版本**: v1.11.273 (commit ff552c8)

## v1.11.274: 盘道多封面轮播 + 首页盘道动态轮播图
- **需求**: ①后台盘道封面可传多张, 详情页左右滑动 ②后台新增「动态轮播图」, 显示在首页-盘道「关注公众号」板块**上方**, 自动左右循环
- **数据设计**:
  - 多封面: `pandao_sessions` 新增 **`covers` 数组**(cloud:// fileID, 最多 9 张);
    **`cover` 保留为首图** 兼容列表页/订单/旧数据。**旧场次无 covers 字段 → 前端用 `[cover]` 兜底构造**
  - 轮播图: 存 **settings `pandao` 组的 `banners` 字段**(复用既有 adminSettingsGet/Save, 无需新接口)
- **云函数改动** (dy-api, 已重新部署):
  1. `appPayConfig` 新增返回 `pandao_banners`(读 pandaoDoc.banners, catch 分支也补了默认值)
  2. `adminPandaoCreate` / `adminPandaoUpdate` 支持 `covers` 数组, 并同步 `cover = covers[0]`
- **前端改动**:
  1. `dashboard.vue`: 封面改为多图上传(`chooseImage count=剩余额度`, **逐张上传, 单张失败不拖垮整批**),
     缩略图网格含序号角标 + × 移除; 新增「动态轮播图」板块, 上传/删除后**自动保存**生效
  2. `detail.vue`: 多图渲染 `<swiper circular indicator-dots>`, 单图沿用静态 `<image>`; 封面点击可预览大图
  3. `index.vue`: 盘道 tab 在**活动列表之后、关注公众号之前**插入 `<swiper :autoplay circular :interval=4000>`;
     用 `pandaoBannerList` computed 过滤转换失败的空 URL, 避免渲染空白页
- **要点**: 所有 cloud:// 都要 `resolveCloudUrl` 转签名 URL(私有桶); 首页 PC 高度用 px 写在既有 media 块内
  (index.vue 的 media 在 1796 行, base 在 1458 → base 在 media 之前, 顺序正确)
- **线上实测**: `app.payConfig` 返回 `pandao_banners` 字段 ✅ 且 `pandao_fixed` 未被冲掉 ✅;
  旧场次 covers 字段缺失 → 走 `[cover]` 兼容分支(已验证 pandao.list 6 场均为旧数据)
- **部署**: 云函数 dy-api 已重新部署 + H5 已部署并验证 (线上 index-B_NvqKV3.js + version chunk = v1.11.274) + 小程序已构建
- **版本**: v1.11.274 (commit 5e4f421)
