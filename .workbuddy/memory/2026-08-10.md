# 2026-08-10 工作日志

## 动态分享+后台权限+免费看课 v1.11.47 (09:57-10:05)
- **①动态分享按钮生效**: 首页推荐/关注动态右下角"分享"原来无事件绑定(act-share 无 @tap, 页面无 onShareAppMessage) → 加 MP 端 open-type=share 按钮(data-id/data-title 传参, onShareAppMessage 读 event.target.dataset 转发动态) + H5 端 shareMoment 复制动态链接; 新增 .share-btn-plain 样式去 button 默认样式
- **②超管/管理员/员工免费看课**: myCourses 云函数按 users.role 判断, admin/manager/staff 直接返回全部课程(_owned:true, progress:100), 普通用户仍查 user_courses
- **③后台登录仅限超管+管理员**: login.vue 校验 role!=='admin'&&!=='manager' 拒绝(员工提示"员工账号无后台登录权限"); 云函数 requireAdmin 收紧(原 admin/staff/manager 均可 → 现仅 admin/manager, staff 调后台接口 403)
- **④管理员后台扩权**: MANAGER_ROUTES 增加 admin.settings.get/save(首页管理)、admin.coupons.*(优惠券)、admin.moments.audit/delete(动态)、admin.feedbacks.*(反馈); dashboard visibleModules: staff 仅员工模块, manager 显示概览/首页/商品/课程/订单/优惠券/直播/动态/反馈(用户管理/系统设置仅超管)
- **⑤超管可降级**: adminUserUpdate 已支持 role 改 user(仅超管可调, 不在任何 ROUTES); dashboard editRoleOptions 加 {user 普通用户}; adminUserCreate 支持创建 manager; createRoleOptions 加管理员
- ⚠️ GitHub push 首次失败(443超时)重试成功; v1.11.47 云函数+H5 部署曾被认证拦截, 用户重新授权后部署成功
- 版本: 1.11.46 → 1.11.47

## 首页/关于我们 logo H5 不可见修复 v1.11.48 (10:01-10:09)
- **现象**: 首页 banner 印章 logo + 我的-关于我们 logo, 小程序可见, H5 不可见
- **排查**: ①文件正常(src 88KB PNG, 线上 static/logo.png 200 且 MD5 一致) ②产物引用 ./static/logo.png 正确 ③线上部署版本确认
- **真正根因**: logo.png 是 **93% 透明、不透明区域深紫黑 RGB(68,31,73)**; banner/关于我们背景深棕(#4e3420~#8c5a2b), 容器背景半透明浅金 rgba(201,169,106,0.12) → **对比度仅 1.26:1~2.01:1 几乎不可见**; 小程序端渲染差异所以可见
- **修复**: banner-seal 与 about-seal 容器背景改**实心米金 #f6ead3**(对比度约 10:1), 深紫黑 logo 清晰可见
- 验证: MP wxss 两处 f6ead3 ✅ H5 index2.css/about.css 线上含 f6ead3 ✅; 已部署 H5(108文件)
- ⚠️ 经验: H5 端严格按 CSS 渲染, 深色透明 logo 在深色背景必不可见, 需实心底色衬托; 排查图片"看不见"先用 PIL 分析透明度+对比度
- 版本: 1.11.47 → 1.11.48

## GitHub 推送失败处理约定 (10:09)
- 用户明确: GitHub 推送若网络不通(443超时), **先本地 commit, 网络恢复后再 push**; 用户可能切换网速/关闭 VPN 导致网络波动
- 以后 push 失败不再反复重试, 本地提交即完成, 提示稍后重推

## H5 logo/头像不显示真根因+修复 v1.11.49 (10:36-10:50)
- **用户反馈**: 首页 logo、关于我们 logo 在 H5 仍是"只有个框"(米金底容器在但图空白); 我的页面左上角头像不显示但个人主页显示
- **排查方法**: 系统 Chrome headless `--dump-dom` 看渲染后 DOM → uni-app H5 image 组件内部是 `<div style="background-image:...">`+`<img>`
- **真根因①logo**: Vite `publicPath:"./"` 把源码 `/static/logo.png` 编译成相对 `./static/logo.png`; **uni-app H5 image 组件不支持相对路径 src → background-image:none 空白**; 而动态绑定的图片(如 moment-img 接口返回 /static/xxx 绝对路径)不受影响 → 只有静态 logo 挂
- **修复①**: logoUrl 计算属性, H5 用 `window.location.origin + '/static/logo.png'`, 小程序用 `/static/logo.png`; index.vue/about.vue 两处 `:src="logoUrl"`
- **修复②头像**: user.vue onShow 先 userProfile 从后端刷新最新头像(setUserInfo), 再 resolveCloudUrl 转换; 模板 v-if 改仅依赖 displayAvatar(转换成功才显示, 否则 fallback 首字) — 原 v-if 含 userInfo.avatar(可能是 cloud:// 导致 H5 渲染失败)
- **验证**: 本地 http.server + Chrome dump-dom → background-image: url(http://localhost:8899/static/logo.png) ✅; 部署后线上同样验证两处均为绝对 URL ✅
- ⚠️ **经验**: 静态资源绝对路径在 H5 必须运行时拼接 origin, 不能依赖 Vite 处理(publicPath=./ 会改相对); 排查图片用 Chrome --dump-dom 看 uni-image 内部 background-image 值
- 版本: 1.11.48 → 1.11.49

## 动态默认推荐+小程序后台头像 v1.11.50 (12:34-13:25)
- **①动态默认推荐**: moments.publish 云函数 is_recommended 从 false 改 true → 所有用户发布的动态默认进"推荐"页; 线上实测发布测试动态 is_recommended=True 后清理
- **②小程序后台用户管理头像不显示**: 根因 - resolveCloudUrl/List 把 cloud:// 转 https(tcb.qcloud.la 临时链接), 但**小程序 image 加载外部 https 需 downloadFile 合法域名白名单**, 未配置则空白; 修复 - avatar.js 加 MP-WEIXIN 条件编译: 小程序端直接返回 cloud:// (image 原生支持渲染), H5/App 仍转 https
- 验证: MP dashboard.js 保留 resolveCloudList 调用但函数体直接返回 list; 云函数+H5 部署成功; GitHub 已推送
- ⚠️ 经验: 小程序 image 对 cloud:// 原生支持, 不要对小程序端做 https 转换(需域名白名单); 条件编译 #ifdef MP-WEIXIN 处理多端差异
- 版本: 1.11.49 → 1.11.50

## 排盘界面优化 v1.11.51 (13:36-13:45)
- 位置: src/pages-sub/user/tools.vue (四柱/奇门/紫微共用输入面板)
- **①性别移到输入方式上方**: 性别块(元男/元女)从输入方式下方移到最上方(tp-title 之后第一个 tp-row)
- **②农历时间选项框与阳历等高**: .tp-pickers-inline.tp-4 .tp-picker 去掉 padding:10rpx 4rpx, 显式 height:76rpx + white-space:nowrap + overflow:hidden (与阳历 tp-picker 一致)
- **③新增"当前时间"开关**: form.bazi.useNow 默认 false; 模板在时间输入下加 tp-row"当前时间"+switch+"开启后以当前时间起局"; runPaipan 开头若 useNow: f.date=今天, 时辰按当前小时归十二时辰(子时 23:00-次日1:00 跨天特判 hourNow>=23||<1→子时), f.mode='solar'
- ⚠️ **经验**: 十二时辰归算必须处理子时跨天(from=23,to=1), 简单 for 循环 `hour<to` 会匹配失败落到亥时; 先测边界(23点/0点/1点)再提交
- 验证: node 单测时辰归算全部通过; MP wxml 性别@766 在输入方式@945 前 ✅ 当前时间@3096 ✅; H5+小程序构建部署; GitHub 已推送
- 版本: 1.11.50 → 1.11.51

## 后台权限+农历加宽+按盘收费+AI去markdown v1.11.52 (13:51-14:05)
- **①后台访问收紧**: dashboard onMounted 守卫从 admin/staff 改为 admin/manager(员工/用户拒); 云函数 requireStaffAllowed: staff 直接返回 false(员工彻底无后台权限, 实测403), admin.list 集合白名单仅 manager 生效; **STAFF_COLLECTIONS 补 coupons/moments/feedbacks**(manager 优惠券/动态列表可读, 实测200; 用户管理仍403仅超管)
- ⚠️ **经验**: 云函数部署后**实例有缓存, 需等5-15秒再测**, 刚部署完立即 curl 会测到旧代码误判"没生效"
- **②农历时间框加宽**: 农历从4列改"年月日一行3框(tp-3类, 更宽)+农历时辰单独一行", 字体22→26rpx 与阳历一致
- **③四柱/奇门/紫微 AI 解盘按盘收费**: 解锁存储从数组改 map {盘标识:[模块]}; diskKey: bazi=四柱干支/qimen=局+旬首+四柱/ziwei=局+命宫+四柱; readPaidMap 兼容旧数组(直接忽略旧数据→需重新付费); isJpUnlocked/applyJpUnlock 等6个函数改造; 验证 MP 产物 ha(e){...a[aa()]||[]...} 按盘逻辑打包
- **④AI解盘去markdown**: aiJiepan 加段落清洗(去 #/**/*/`/引用> /[链接](url)); aiAsk 补全同样规则; 本地测试 8 种 markdown 语法全清洗
- 线上验证: 员工403 ✅ manager coupons 200 ✅ manager users 403 ✅ admin 200 ✅; 云函数+H5 已部署; GitHub 已推送
- 版本: 1.11.51 → 1.11.52

## 阳历时辰显示具体时间 v1.11.53 (14:27-14:30)
- 需求: 阳历排盘时辰不要"午时"等阴历写法, 显示具体时间段
- tools.vue 新增 solarShichenLabels = SHICHEN.map(s => `${padStart(s.from)}:00-${padStart(s.to)}:00`) (如 23:00-01:00); 阳历 picker range/显示改用 solarShichenLabels; 农历保持"子时"写法
- 同步: 排盘结果 solarText(阳历加时间段) + 保存历史 label(阳历用时间段)
- 验证: MP 产物 k=子时标签/f=时间段标签双存在, 阳历模板引用 f ✅; H5 已部署; GitHub 已推送
- 版本: 1.11.52 → 1.11.53

## 阳历具体时刻选择 v1.11.54 (14:31-14:35)
- 需求: 阳历时间要"几点几分"具体时刻效果(不要时间段)
- 阳历时辰选择从 selector 时间段改 **picker mode="time"** 选具体时刻; form.bazi.time 默认 '12:30'
- 排盘: resolveShichenHour 阳历分支 time.split(':')→hh+mm/60 小数小时(如12:30→12.5), hourPillar 支持小数自动归入对应时辰(12.5→午时); useNow 同步 f.time=当前时刻; solarText/历史 label 显示 f.time; 删除 solarShichenLabels
- 验证: node 单测 8 个时刻(含子时跨天23:15→子)全通过; MP wxml mode="time" ✅ JS split(":")转换 ✅; H5 已部署; GitHub 已推送
- ⚠️ 经验: hourPillar 按 from<=hour<to 判断支持小数小时, 具体时刻可直接转小数参与排盘, 无需先映射时辰 index
- 版本: 1.11.53 → 1.11.54
