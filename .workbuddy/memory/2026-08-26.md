# 2026-08-26 工作日志

## 18:40 订单物流展示 + 结算页元宝抵扣位置调整 (feat)

- **需求**:
  1. 后台添加运单信息后, 用户在订单页面可以看到运单号, 以及物流当前的状态信息
  2. 确认订单界面, 元宝抵扣提示"已抵扣 ¥xx.00（用 xx0 元宝）"调整到支付方式-元宝支付的部分
- **实现**:
  - 订单详情页 detail.vue: 新增物流卡片(物流公司/运单号可复制/发货时间/状态徽标), 有 tracking_no 才显示
  - 订单列表页 list.vue: 新增物流摘要行(状态+公司+运单号), 点击进详情
  - 物流状态由订单状态映射: 待收货=运输中 / 已完成=已签收 / 已退款=已退回 / 其他=已发货 (无第三方轨迹API, 不接真实轨迹)
  - checkout.vue: 元宝抵扣行从"优惠与支付"卡片移到"支付方式"卡片底部, 加 hasBalancePay 判断(元宝支付被后台关闭时不显示); balance-deduct 虚线分隔
- **说明**: 后台发货已存 logistics_company/tracking_no/shipped_at (adminOrderShip), 订单列表/详情直接返回全字段, 无需改云函数
- **部署**: v1.11.200, 四端同步 (H5 重建→cap sync→APK gradlew→验证APK内200→上传 H5/APK/下载页)
- **验证**: H5 200 / APK 200 / 下载页 Android v1.11.200; Git 692874c + tag v1.11.200 已 push

## 18:50 我的页面顶部布局修复 + APK 构建策略调整 (fix + 约定)

- **问题1**: "我的"页面顶部, 用户名/VIP等级 与右上角 主页/通知/设置 标签重合
- **修复1**: 右上角入口 top 44rpx→16rpx(上移); user-main 加 margin-top:60rpx(用户区下移); user-name 限宽省略(320rpx); user-meta flex:1 + padding-right:60rpx 预留右侧空间
- **约定2 (用户明确)**: H5/小程序/下载页/APK 版本始终同步, 但 **Android APK 不随每次迭代构建**; 需要生成 APK 时, 用当前已同步的版本号直接构建, 不再单独升版
- **脚本优化**: bump-version.js 下载页更新改为 `--apk` 可选参数 — 默认不更新下载页(避免指向未构建的APK 404); 构建 APK 时用 `node scripts/bump-version.js --apk`
- **部署**: v1.11.201, 仅 H5+小程序重建部署(未构建APK); 下载页保持 v1.11.200(指向已存在的APK)
- **验证**: H5 200(201) / 下载页 Android v1.11.200 / APK v200 200; Git b6916a5 + tag v1.11.201 已 push

## 19:04 我的页面用户名位置恢复居中 (fix 修正)

- **问题**: 上次修复把 user-main 加了 margin-top:60rpx 整体下移, 用户名位置太靠下不协调
- **修复**: 移除 user-main 的 margin-top:60rpx, 用户区回到 header padding-top(48rpx) 原始位置; 防重合仍靠"入口上移(16rpx)+user-name限宽省略(320rpx)+user-meta padding-right:60rpx"
- **教训**: 解决重合应优先移动绝对定位的入口(上移), 而非下推用户区; 下推过多破坏原布局协调
- **部署**: v1.11.202, H5+小程序重建部署(未构建APK); Git f701467 + tag v1.11.202 (push 中)

## 19:29 结算页元宝抵扣改"选元宝支付自动抵扣" (feat)

- **需求**: ①支付方式里去掉常驻"元宝抵扣板块"(含switch开关) ②选择"元宝支付"时, 显示"抵扣 ¥xx.00（消费 xx 元宝）"
- **实现** (checkout.vue):
  - 移除常驻 balance-deduct 行 + toggleBalance 函数 + hasBalancePay computed
  - `balanceUsed` 由 ref 改为 computed: `payMethod === 'balance'` — 选元宝支付即自动抵扣
  - 模板: 元宝支付项内联显示 `pay-deduct` 提示(仅 payMethod==='balance' && 余额>0), 文案"抵扣 ¥xx（消费 xx 元宝）"
  - 提交逻辑不变: balance_used = discount - couponDiscount; 元宝支付走 orderPayBalance 真实扣减, 余额不足云函数报错
- **部署**: v1.11.203, H5+小程序重建部署(未构建APK); Git bf39a1b + tag v1.11.203 (push 中)

## 19:37 结算页收货地址弹窗修复 (fix)

- **问题**: 选择收货地址时, 若账号无地址记录, `chooseAddress()` 走 else 分支**直接弹"新增地址"表单**, 跳过了地址列表弹窗, 用户看不到已有地址和新增按钮
- **修复** (checkout.vue):
  - `chooseAddress()`: 去掉 if/else, **始终弹"选择收货地址"列表弹窗**, 打开前先 loadAddresses() 刷新
  - 空态优化: "暂无收货地址" + 红色"＋ 新增收货地址"按钮(addr-none-btn), 右上角保留"＋ 新增地址"
  - 已有地址正常显示列表(名称/电话/默认标/删除) + 点击选中
- **⚠️ 版本坑 (08-26)**: 连续迭代时若上一个 bump 的 push 未完成, 下次 bump 读 git tag 可能重复递增(204 tag 已存在→误升205)。修复: 删除重复 tag、修正 build.gradle 回退、以 HEAD 实际 tag 为准; 提交前用 `git describe --tags --exact-match HEAD` 核对
- **部署**: v1.11.204, H5+小程序重建部署(未构建APK); Git 449c6b5 (push 网络超时, 稍后重推)

## 19:44 结算页按钮文案简化 (fix)

- **需求**: "提交订单 · 微信支付" 按钮改为 "提交订单", 去掉微信支付文字
- **实现**: checkout.vue 提交按钮文案 `提交订单 · 微信支付` → `提交订单` (提交中状态不变)
- **部署**: v1.11.205, H5+小程序重建部署(未构建APK)
- **验证**: H5 200; Git 32f0720 + tag v1.11.205 已 push (204/205 tag 均推送)

## 21:49 顶部红色区高度调整 + 保存地址按钮居中 (fix)

- **需求**: ①"我的"页顶部红色区高度变小 + 头像距下方卡片距离减小 ②首页顶部红色区同步调整 ③新增地址弹窗"保存地址"按钮居中
- **实现**:
  - user.vue: user-header padding 48/40rpx→36/28rpx, min-height 160→140rpx; asset-card margin-top -30→-16rpx
  - index.vue: banner padding 48/40rpx→36/28rpx, min-height 160→140rpx (与我的页一致)
  - checkout.vue: 保存地址按钮外包 af-btn-wrap (flex居中), 移除内联 margin-top
- **部署**: v1.11.206, H5+小程序重建部署(未构建APK)
- **验证**: H5 200; Git 5c00de3 + tag v1.11.206 已 push

## 23:23 后台导航顺序 + 设置权限调整 (feat)

- **需求**: ①后台左侧功能栏: 页面管理/用户管理移到系统设置上面, 顺序=用户管理→页面管理→系统设置(置底三块) ②这三块只有超级管理员/操作管理员有设置权限
- **实现**:
  - dashboard.vue: modules 顺序调整 (users/home/settings 置底依次排列)
  - 前端权限: canManageUsers/canManageSettings 从仅admin改为 admin+operator; "新建员工/管理员"按钮改 canManageUsers
  - 后端权限 (云函数): OPERATOR_BLOCKED 移除 admin.users.create/update/delete + admin.settings.save — operator 可管理用户/系统设置; 保留运维级禁止(renumberUids/assignDaoCodes/recalcVip/createCollection)
- **部署**: 云函数 dy-api 已重新部署 (改权限必部署!); H5+小程序 v1.11.207 重建部署
- **验证**: H5 200 / 云函数 403(未带凭证,预期); Git 15ed33b + tag v1.11.207 (push 网络超时, 稍后重推)

## 23:36 更正: 三块设置权限仅超管 (用户更正)

- **用户更正**: 用户管理/页面管理/系统设置 只有**超级管理员**有设置权限 (不是超管+操作管理员)
- **修正**:
  - 前端: canManageUsers/canManageSettings 改回仅admin; 新增 canManageHome(仅admin) 替换 home 模块内 9 处 canWrite (页面管理+嵌套的盘道管理设置按钮); "新建员工/管理员"按钮回归 canManageUsers
  - 后端: OPERATOR_BLOCKED 恢复 admin.users.create/update/delete + admin.settings.save; **wxmp.* 发布接口(getAuthUrl/listBound/getExperienceQr/uploadCode/submitAudit/release) 主分发加仅超管校验** (原本无权限校验, 任何登录用户可调, 属系统设置范畴)
- **⚠️ 教训**: ①用户对权限的表述可能反复, 先按最严格(仅超管)实现更稳妥 ②wxmp.* 非 admin. 前缀绕过 requireAdmin, 权限校验要检查全路由表
- **部署**: 云函数已重新部署; H5+小程序 v1.11.208 重建部署; Git 1e47d82 + tag v1.11.208(force修正指向HEAD) 已 push
