# 2026-08-08 工作日志

## 新增课程 (00:01) v1.10.21
- **分类**: 书法艺术(id6)/汉字文化(id7) 创建成功
- **课程**: ①书法精品课42讲(id7, 张灃老师, 四部分: 文房四宝概述/控笔训练及篆书入门/隶书入门/楷书入门, 3980元) ②汉字解析百讲(id8, 四部分: 追根溯源/举一反三/字形演变/全龄段, 副标题'从汉字入手助你看懂道德经——道德经关键200字', 2980元) ③书法不只是写字(id9, status:false 待上架不显示)
- **修复**: courses.list 过滤 status 布尔false+字符串off 兼容(原来只滤'off', 布尔false漏网)
- 云函数已部署, 前端列表验证: id7/8显示, id9隐藏

## 课程上架下架按钮 (00:10) v1.10.22
- 课程管理每条课程行: 状态列显示 已上架/已下架(status false/off判断), 操作列加 上架/下架 按钮(toggleCourse → adminCourseUpdate status 反转 → loadModule courses 刷新)
- 仅管理员/超管可见(canManageCourses)

## 课程上架下架按钮 (00:10) v1.10.22
- 课程管理每条课程行: 状态列显示 已上架/已下架(status false/off判断), 操作列加 上架/下架 按钮(toggleCourse → adminCourseUpdate status 反转 → loadModule courses 刷新)
- 仅管理员/超管可见(canManageCourses)

## 课程权限修复+作用关系全盘 (00:22) v1.10.23
- **需求①**: 课程上下架后等级状态不变 → 根因: admin.courses.update 从 STAFF_ROUTES 移除后, manager(管理员)也 403 → 新增 MANAGER_ROUTES 白名单(admin.courses.create/update/delete), manager 可管课程, staff 仍不可; 验证 manager 200/staff 403
- **需求②**: 作用关系板块列出盘面全部干支两两关系(四柱+选中大运/流年/流月/流日/流时), 不分柱, 去重(Set 按 min/max+关系名) → 重写 activeTarget→allGanzhi, ganRelations/zhiRelations 全组合; 模板改 chip 展示(甲·己 / 甲己合 / 年柱↔月柱)

## 课程权限修复+作用关系全盘 (00:22) v1.10.23
- **需求①**: 课程上下架后等级状态不变 → 根因: admin.courses.update 从 STAFF_ROUTES 移除后, manager(管理员)也 403 → 新增 MANAGER_ROUTES 白名单(admin.courses.create/update/delete), manager 可管课程, staff 仍不可; 验证 manager 200/staff 403
- **需求②**: 作用关系板块列出盘面全部干支两两关系(四柱+选中大运/流年/流月/流日/流时), 不分柱, 去重(Set 按 min/max+关系名) → 重写 activeTarget→allGanzhi, ganRelations/zhiRelations 全组合; 模板改 chip 展示(甲·己 / 甲己合 / 年柱↔月柱)

## APP 更新 (00:30 构建, 07:27 部署) v1.10.24
- **流程**: uni build h5 → cap sync android → gradlew assembleRelease (JAVA_HOME=~/Library/Java/JavaVirtualMachines/jdk-21.0.12+8/Contents/Home)
- **产物**: app-release.apk 4.1MB, 内嵌 v1.10.24(3a602dc), 签名有效, 540文件完整
- **部署**: /apk/daoyuan-v1.10.24.apk (HTTP 200 4.1MB) + 下载页 /download/ (HTTP 200), 共104文件
- **坑**: JAVA_HOME=/Users/wenhua/.workbuddy/binaries/java/jdk21 不存在 → 实际在 ~/Library/Java/JavaVirtualMachines/jdk-21.0.12+8/Contents/Home

## 后台折叠+用户表+微信头像昵称 (07:32) v1.10.25
- **①后台侧栏默认折叠**: sidebarCollapsed ref(true)
- **②用户管理表**: users-row min-width 1380rpx 触发横向滚动; users-nick 定宽200rpx nowrap省略; w-no 260rpx nowrap; w-price nowrap 字段紧凑
- **③微信登录头像昵称**: getUserProfile 新版基础库失效(返回匿名) → 登录页加 chooseAvatar 按钮 + type=nickname input (官方方案), wxLogin 优先用所选头像昵称, 未选才 getUserProfile 兜底; 云函数已支持存/更新头像昵称

## 后台折叠+用户表+微信头像昵称 (07:32) v1.10.25
- **①后台侧栏默认折叠**: sidebarCollapsed ref(true)
- **②用户管理表**: users-row min-width 1380rpx 触发横向滚动; users-nick 定宽200rpx nowrap省略; w-no 260rpx nowrap; w-price nowrap 字段紧凑
- **③微信登录头像昵称**: getUserProfile 新版基础库失效(返回匿名) → 登录页加 chooseAvatar 按钮 + type=nickname input (官方方案), wxLogin 优先用所选头像昵称, 未选才 getUserProfile 兜底; 云函数已支持存/更新头像昵称

## app.json not found 真正根因 (07:38)
- **根因**: project.config.json 缺 miniprogramRoot! 18:38 加过 postbuild 补丁, 但回退 v1.10.10 时 postbuild 也被回退 → 补丁丢失
- **教训**: 代码回退会连脚本一起回退! postbuild-mp.js 的 miniprogramRoot 补丁必须常驻
- **修复**: 重新加入 postbuild 补丁(miniprogramRoot='./'), 当前产物已补, CLI auto-preview 通过

## 头像修复+后台头像列 (07:42) v1.10.26
- **①头像不显示根因**: 登录页 chooseAvatar/getUserProfile 返回临时路径(wxfile://tmp)直接存库, 小程序重启失效 → wxLogin 时临时路径先上传云存储 avatars/wx*.png 换永久URL(https://7a68-...tcb.qcloud.la/); 用户页 pickAvatar 已正确上传不受影响
- **②后台用户管理**: 昵称前加头像列(56rpx圆形, 无头像显示昵称首字), users-row min-width 1460rpx
- 验证: 用户数据头像均为永久URL; 双端构建+CLI通过

## 头像修复+后台头像列 (07:42) v1.10.26
- **①头像不显示根因**: 登录页 chooseAvatar/getUserProfile 返回临时路径(wxfile://tmp)直接存库, 小程序重启失效 → wxLogin 时临时路径先上传云存储 avatars/wx*.png 换永久URL(https://7a68-...tcb.qcloud.la/); 用户页 pickAvatar 已正确上传不受影响
- **②后台用户管理**: 昵称前加头像列(56rpx圆形, 无头像显示昵称首字), users-row min-width 1460rpx
- 验证: 用户数据头像均为永久URL; 双端构建+CLI通过

## 头像根因修复+微信授权弹窗 (07:52) v1.10.27 — 用户强烈不满后重做
- **①头像不显示真根因**: 存储域名写死 7a68- 前缀(旧环境域名, HTTP 418 死域名)! 真实域名是 636c-cloud1-...tcb.qcloud.la 且需签名 → 改方案: 小程序端直接存 cloud:// fileID (wx.cloud 原生渲染, 无需域名白名单/签名)
- **②存量数据**: 4 个用户 7a68 死 URL 批量转 cloud://636c-... fileID (user.updateProfile 接口)
- **③微信登录**: 用户明确要求头像昵称从微信主动获取, 不要手动板块 → 删登录页手动板块, 点微信一键登录弹出授权面板(chooseAvatar 微信头像选择器含'使用微信头像' + type=nickname 微信昵称自动填充), 确认后登录; getUserProfile 兜底
- **验证**: CLI 编译通过, H5 部署, 存量头像修复成功
- **教训**: 头像 URL 必须实测可访问(7a68 是旧环境残留, 迁移后未更新); cloud:// fileID 才是小程序正解

## 头像根因修复+微信授权弹窗 (07:52) v1.10.27 — 用户强烈不满后重做
- **①头像不显示真根因**: 存储域名写死 7a68- 前缀(旧环境域名, HTTP 418 死域名)! 真实域名是 636c-cloud1-...tcb.qcloud.la 且需签名 → 改方案: 小程序端直接存 cloud:// fileID (wx.cloud 原生渲染, 无需域名白名单/签名)
- **②存量数据**: 4 个用户 7a68 死 URL 批量转 cloud://636c-... fileID (user.updateProfile 接口)
- **③微信登录**: 用户明确要求头像昵称从微信主动获取, 不要手动板块 → 删登录页手动板块, 点微信一键登录弹出授权面板(chooseAvatar 微信头像选择器含'使用微信头像' + type=nickname 微信昵称自动填充), 确认后登录; getUserProfile 兜底
- **验证**: CLI 编译通过, H5 部署, 存量头像修复成功
- **教训**: 头像 URL 必须实测可访问(7a68 是旧环境残留, 迁移后未更新); cloud:// fileID 才是小程序正解

## 课程上下架即时刷新+等级小字 (08:16) v1.10.29
- **①课程上下架状态列不刷新**: 后端链路全通(update 200/list 返回新状态) → 前端 toggleCourse 改为本地即时更新 courseGrouped(响应式重建) + loadModule 兜底; 商品正常因直接列表刷新, 课程是分类分组视图
- **②我的页等级说明**: 176 行 vip-tip 加 vip-formula 类, 28rpx #3a2c1e 加粗加深

## 课程上下架即时刷新+等级小字 (08:16) v1.10.29
- **①课程上下架状态列不刷新**: 后端链路全通(update 200/list 返回新状态) → 前端 toggleCourse 改为本地即时更新 courseGrouped(响应式重建) + loadModule 兜底; 商品正常因直接列表刷新, 课程是分类分组视图
- **②我的页等级说明**: 176 行 vip-tip 加 vip-formula 类, 28rpx #3a2c1e 加粗加深

## 授权面板按钮+后台头像显示 (08:23) v1.10.32
- **①授权面板**: 取消/微信登录按钮改 wx-auth-actions flex 左右排列, flex:1 等高 92rpx 加大
- **②后台头像不显示根因**: 小程序存 cloud:// fileID, H5 image 渲染不了 → 双保险: 云函数 adminList 转 getTempFileURL(相对路径)+前端 dashboard 加载用户后 getStorage().getTempFileURL 转换(兼容小程序1参数组/js-sdk对象签名)
- **坑**: 云存储 ACL 改公开读失败(个人版套餐限制); node-sdk 本地测试需密钥; getTempFileURL 批量转换部分漏(单段/双段 fileID 格式差异) → 前端逐个转

## 等级说明去公式行 (08:37) v1.10.33
- 会员等级说明弹窗删除 '等级=累计消费+累计储值，自动划分' 行 + vip-formula 样式

## 等级说明去公式行 (08:37) v1.10.33
- 会员等级说明弹窗删除 '等级=累计消费+累计储值，自动划分' 行 + vip-formula 样式

## 课程上下架跳回根因 (08:39) v1.10.35
- **症状**: 点上下架后状态变一下又跳回
- **根因**: toggleCourse 里 status:!off 逻辑写反! 当前上架(off=false)点'下架' → !off=true → 设成上架 → 本地更新显示下架, 后端存上架 → 整表刷新拉回上架 → '跳回'
- **修复**: status: off?true:false (当前下架→设上架, 当前上架→设下架) + 本地即时更新; 移除整表刷新覆盖
- 后端实测: update false → list 返回 False ✅

## 课程上下架跳回根因 (08:39) v1.10.35
- **症状**: 点上下架后状态变一下又跳回
- **根因**: toggleCourse 里 status:!off 逻辑写反! 当前上架(off=false)点'下架' → !off=true → 设成上架 → 本地更新显示下架, 后端存上架 → 整表刷新拉回上架 → '跳回'
- **修复**: status: off?true:false (当前下架→设上架, 当前上架→设下架) + 本地即时更新; 移除整表刷新覆盖
- 后端实测: update false → list 返回 False ✅

## 登录凭证列+微信免授权 (08:49) v1.10.38
- **①后台用户管理**: 手机号列→登录凭证列, 按凭证分色: 微信一键登录(绿)/邮箱(蓝)/手机号(紫)/无凭证(灰)
- **②微信登录分流**: 新增云函数 user.wechatCheck(openid 是否注册+hasProfile); 前端 wxLogin 先 check → 老用户且有头像昵称直接 doWxLogin 免授权; 首次/无资料才弹授权面板; doWxLogin 上传判断改为非 https/cloud 才上传

## 微信凭证列定稿 (09:18) v1.10.41
- 用户先要求显示微信ID(openid), 后改'微信号', 最终确认: 保持'微信一键登录'绿色标签
- **说明**: 微信不开放第三方获取用户微信号(平台限制), 只能拿 openid; 用户理解后保持原样
- 列宽还原 260rpx/max-width 220rpx

## 用户备注列 (09:23) v1.10.42
- 用户管理表最后加'备注'列(操作前), 点击备注可直接打开编辑; 编辑弹窗加'备注'输入框(仅管理员可见, 200字上限)
- 云函数 adminUserUpdate 白名单加 remark
- 验证: update remark → list 返回 ✅

## 用户备注列 (09:23) v1.10.42
- 用户管理表最后加'备注'列(操作前), 点击备注可直接打开编辑; 编辑弹窗加'备注'输入框(仅管理员可见, 200字上限)
- 云函数 adminUserUpdate 白名单加 remark
- 验证: update remark → list 返回 ✅

## 备注列位置调整 (09:41) v1.10.44
- 用户管理备注列从操作前移到操作列右边

## 备注列位置调整 (09:41) v1.10.44
- 用户管理备注列从操作前移到操作列右边

## 微信支付 API v3 直连接入 v1.11.0 (12:10-12:34)
- **背景**: cloud1 是 CloudBase 独立环境(非微信小程序云开发环境), cloudPay 不可用(实测 undefined); 微信小程序后台绑商户号 ≠ 云开发支付开通
- **方案**: API v3 直连 (不迁环境) — 商户号+APIv3密钥+商户证书(apiclient_cert.pem/key.pem), 零依赖原生 crypto 实现
- **新文件**: cloudfunctions/dy-api/wxpay-v3.js (统一下单JSAPI/回调验签+AES-GCM解密/查单/退款)
- **改造**: order.wxpay → v3 unifiedOrder(需用户openid); HTTP 入口加 pay.notify/refund.notify 拦截(验签+更新订单+发消息)
- **回调URL**: https://cloud1-...tcloudbase.com/dy-api?action=pay.notify (现有/dy-api路由 query带action, 无需新路由)
- **修复bug**: 订单状态'待付款' vs 判断'待支付'不一致导致永远无法支付
- **待用户提供**: WXPAY_MCHID(商户号) WXPAY_V3_KEY(APIv3密钥) WXPAY_APIV3_SERIAL(证书序列号) WXPAY_CERT_PEM WXPAY_KEY_PEM → 填 config.local.js 后实测
- **商户平台还需**: 产品中心-开发配置 添加回调域名 cloud1-...tcloudbase.com
- **版本**: 1.10.45 → 1.11.0 (手动设tag, bump major 会跳2.0.0不能用)

## 微信支付 v3 实测下单成功 (12:48) v1.11.0
- **商户配置已填**: mchid 1116271440, APIv3密钥, 证书序列号 37CB2C3FB0AFF7872F59EBBA981FFED43FB8FB24, apiclient_cert.pem/key.pem (config.local.js, 不入库)
- **下单实测成功**: order.wxpay 返回 prepay_id=wx95800748...+paySign(RSA) → 真实微信支付下单打通
- **回调**: 新建路由 /dy-api/pay/notify (enablePathTransmission=true), 云函数按 event.path 识别回调; notify_url 不能带query(微信正则拒绝), 必须纯路径
- **待用户**: ①商户平台添加支付回调域名 cloud1-...tcloudbase.com ②真机0.01元测试完整支付闭环
- **证书源文件**: /Users/wenhua/cert/1116271440_20260808_cert/
