# 2026-09-03 工作记录

## 邀请有礼增加分享按钮 (12:18, v1.11.318)
- **用户需求**: 我的 → 邀请有礼界面增加分享按钮; 小程序分享后, 别人打开**直接到注册界面**并**自动填好邀请人的注册码**
- **关键发现**: 登录页 `src/pages-sub/login/login.vue` **早已支持**该链路 —— `onLoad` 中 `if (options.invite) { inviteCode.value = String(options.invite); mode.value = 'register' }`
  (line ~268-273)。即 `?invite=xxx` 会自动切到注册模式并回填邀请码, 所以只需做分享入口, 无需改注册页
- **改动** (`src/pages/user/user.vue`):
  1. import 补 `onShareAppMessage`(from `@dcloudio/uni-app`)
  2. 新增 `onShareAppMessage(() => ({ title, path, imageUrl: '' }))` —— path 动态拼 `/pages-sub/login/login?invite=${encodeURIComponent(daoCode)}`; 无邀请码时 fallback `/pages/index/index`
  3. 新增 `copyInviteCode()`(小程序内链接打不开, 改为复制邀请码)
  4. 邀请弹窗按钮按平台条件编译:
     - `#ifdef MP-WEIXIN`: `<button class="btn-fill btn-invite" open-type="share">分享给好友</button>` + `<view class="btn-p btn-invite-sub" @tap="copyInviteCode">复制邀请码</view>`
     - `#ifndef MP-WEIXIN`: 保留原「复制邀请链接」
  5. 样式: `button.btn-invite`(重置小程序 button 默认边框/行高, `::after { border: none }`)、`.btn-invite-sub`(描边次按钮)
- **验证**: `uni build -p mp-weixin` 与 `uni build` 均通过; 小程序产物已确认含 `open-type="share"`(user.wxml)、`onShareAppMessage`(user.js)、分享路径 `pages-sub/login/login?invite=`
- **版本**: bump→v1.11.318, commit ed40365, tag v1.11.318 -f; H5 upload /h5/ 成功, curl 校验线上 `version.FBYQXXk-.js` = **1.11.318** ✅; GitHub 已推送
- **注意**: 分享功能**仅小程序端可见**(H5 上看不到分享按钮), 需用微信开发者工具导入 `dist/build/mp-weixin` 上传发布(版本号 1.11.318)才能生效
- **踩坑(重复出现)**: mcporter 授权再次过期, 部署脚本又误报"✅ H5 部署完成" —— curl 校验线上 version 文件为空才暴露。本次设备码 `63W4-NWGH` 授权成功
- **教训强化**: **每次部署后必须 curl 校验线上 version 文件**; 授权有效期短(设备码仅 10 分钟), 隔夜基本必过期, 早上首次部署前应先 `auth action=status`
