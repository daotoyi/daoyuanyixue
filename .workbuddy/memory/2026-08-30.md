# 2026-08-30 工作日志

## v1.11.251: 上传"永不放弃"模式 (参考百度网盘大文件上传)
- 用户需求: 上传经常断, 参考百度网盘"不会断链" → 优化
- 方案 (纯前端 cloudbase.js + dashboard.vue):
  1. **补齐轮模式**: 分片循环改 Promise.allSettled — 单片失败【不再中断整体】, 失败片进补齐队列, 轮间退避 2s/4s/6s/8s/10s 反复重试, 最多 8 轮才放弃 (且分片保留可续传); 取消仍立即生效
  2. **分片 32MB → 16MB** (细粒度: 单片失败影响小, 重试易成功; 4.58G≈287 片, 6 并发)
  3. **断网自动暂停/恢复自动继续**: window offline/online 监听 — 断网置 paused+状态"网络已断开,等待恢复后自动继续…", 恢复自动 resumed; 上传结束/失败 removeNetListeners
- 验证: /tmp/cos-test/retry-round-test.js (10片, 第2/5/8片首次失败 → 补齐轮重试 → 全部成功, 整体未中断) ✅
- ⚠️ dashboard PART_SIZE_BYTES 须与 cloudbase.js MULTIPART_PART_SIZE 同步 (resumePercent 计算)
- 版本 v1.11.251 (commit ddd2b65) H5 已部署 (线上 index-BzB7DKNZ.js) + 小程序已构建

## 8-29 遗留: 上传链路历次修复回顾 (241-250 均在 08-29.md)
- v1.11.241 网络异常重试4次+刷新签名 | 242 120s超时+AbortController | 243 按钮样式 | 244 abortFns Set修复并发中断+32MB分片 | 245 setTabBarItem异步报错+按钮绑ep对象 | 246 v-for索引key错位(真凶) | 247 断点续传(storageListParts+localStorage) | 248 匿名登录超时(卡0%根因) | 249 按钮手势 | 250 并发3→6
- 用户网络环境差(代理/VPN波动), 上传大视频建议关闭代理直连

## v1.11.252: 全局鼠标手势 (App.vue 全局 CSS)
- 需求: 所有涉及点击的元素鼠标悬停变手势图标
- 方案: App.vue H5 专属样式块加全局规则 — uni-button/button/a/navigator/picker/switch 等原生组件 + uni-view[hover-class] 等带 hover 的组件 + 类名模式 [class*="btn"/"pill"/"tab"/"option"/"tag"/"badge"/"menu"/"switch"/"icon-btn"/"close"/"card"/"item"] (项目卡片均 class 含 card/item)
- 部署遇 CloudBase 认证过期 → 设备码授权 (user_code 流程) → READY 后部署成功
- 版本 v1.11.252 (commit 888e306) H5 已部署 (线上 index-CLjGU5Ua.js) + 小程序已构建

## v1.11.253: 关于我们页信息更新 (src/pages-sub/setting/about.vue)
- 用户要求: 开发: 昊辰(zhenhesheng@126.com) / 网站: https://zhenhesheng.cn / 同修汇: https://club.zhenhesheng.cn
- 改动: "开发"行值改为 昊辰（zhenhesheng@126.com）(点击复制邮箱); 新增"网站"行(点击 H5 window.open 新窗口/小程序复制链接); 新增"同修汇"行同上; 原"开发者/联系我们"两行合并为"开发"一行
- 版本 v1.11.253 (commit c149bd3) H5 已部署 (线上 index-PN8mPXVg.js) + 小程序已构建

## v1.11.254: 修复后台删除用户不生效 (软删除机制, 重要)
- **根因**: 后台删除用户是物理 remove users 文档, 但三个登录入口(账号密码 login / 微信 wechatLogin / 手机号 phoneLogin)在查不到用户时会**自动注册新账号** → 被删用户一登录账号就复活, 看似"删除不生效"
- **修复**: ①adminUserDelete 改为软删除 — users 文档 update {status:'deleted', deleted_at, deleted_by, session_token:''}(会话立即失效), 关联数据(orders/favorites/footprints/coupons/messages/user_courses/live_bookings/moments)照删; ②三个登录入口查用户后若 status==='deleted' → fail('该账号已被注销，无法登录')(软删除记录保留故不会走自动注册分支); ③userHeartbeat 对 deleted 用户返回 kicked+gone 立即踢下线; ④adminList users 过滤 status==='deleted'; ⑤register 查重天然拦截(记录保留)
- **教训**: 凡"删除用户/封号"类功能必须同时封禁所有登录/注册入口, 否则物理删除会被自动注册绕过
- 云函数 dy-api 已重新部署 (manageFunctions updateFunctionCode)
- 版本 v1.11.254 (commit ba5363b) H5 已部署 (线上 index-BppA_mBF.js) + 小程序已构建

## v1.11.255: 后台登录凭证列手机号优先 (dashboard.vue)
- 问题: 手机号快捷登录用户同时有 openid+phone, 凭证列原判断顺序 openid→email→phone 导致显示"微信一键登录"而非手机号
- 修复: 判断顺序改为 phone→email→openid (有手机号显示手机号, 纯微信登录无手机号仍显示"微信一键登录")
- 版本 v1.11.255 (commit 846c97d) H5 已部署 (线上 index-Sk-pUOzJ.js) + 小程序已构建

## v1.11.256: 删除用户真正的根因 = 角色权限 (重要)
- **用户"还是删除不了"的真正根因**: 用户昊辰账号角色是 **operator(操作管理员)**, 而 OPERATOR_BLOCKED 黑名单含 'admin.users.delete' → 云函数返回"该操作需要超级管理员权限"; 且前端 canManageUsers 仅 admin 才显示操作列 → operator 连删除按钮都看不到。v1.11.254 软删除修复没错但没走到那步
- **修复**: ①OPERATOR_BLOCKED 移除 'admin.users.delete' (2026-08-30 用户要求操作员可删用户); ②adminUserDelete 内部保护: 非超管仅能删除 role==='user' 的普通用户, 删 manager/operator 需超管, 删 admin 仍走原保护; ③前端 canManageUsers 改为 ['admin','operator'] 可见操作列, "新建员工/管理员"按钮仅超管; ④关于我们"开发"行去掉 info-link (红色+下划线), 保留点击复制
- **验证方法**: curl 直调云函数网关模拟 operator(opUid=120407) → 删不存在用户返回"用户不存在"(权限放行) / 删 admin 返回"只有超级管理员可以删除管理员账号" / 删 manager 返回"仅超级管理员可删除后台账号"; 部署后需等 ~15s 生效(updateFunctionCode 有延迟)
- 云函数 dy-api 已重新部署
- 版本 v1.11.256 (commit 6445192) H5 已部署 (线上 index-CZ1OuhhV.js) + 小程序已构建

## v1.11.256 追加排查: 超管删除后端全链路实测正常
- 用户反馈超管(ZHSM001/uid1)也删不了 → 用 admin 身份 curl 实测全链路: 创建测试用户(uid 120415)成功 → admin.users.delete 返回 {deleted:true} → 数据库确认 status=deleted/deleted_by=1 → admin.list 已过滤(列表总数 25, 测试用户不在)
- 结论: **后端 100% 正常, 问题在前端缓存或删除目标类型** (超管账号被设计为不可删, 需先降级; 浏览器旧缓存需强刷 Cmd+Shift+R)
- 测试用户已用 writeNoSqlDatabaseContent action=delete query={uid} 清理 (注意: delete 参数是 query 不是 filter, update 是整体替换语义)

## 数据操作: 删除 ZHS0099(uid 120397) + ZHSM113(uid 120396)
- 用户要求删除两个"李明"普通用户 → 超管(opUid=1)调 admin.users.delete 均返回 {deleted:true}, 数据库确认 status=deleted/deleted_by=1, 关联数据已清除

## v1.11.257: 修复视频上传卡在84%且无法取消 (分片读取中断)
- **根因**: 6并发批次制(每批 allSettled 等全部结束), 分片读取 readBlobSlice(H5 fetch blob 读16MB片) **无超时且未接入取消中断** → 某片读取挂起时整批卡死(进度停84%), 取消只中断 PUT 请求中断不了读取 → "无法取消"
- **修复**: ①readBlobSlice 增加 signal 参数(fetch 传 AbortSignal); ②uploadOne 中 AbortController/abortFn 注册**提前到读分片之前**(取消/暂停可中断读取+PUT全阶段); ③读分片 60s 超时兜底(超时 abort → waitIfPaused 判方向: 取消抛UPLOAD_CANCELLED/暂停等恢复/纯超时走补齐轮重试)
- **用户当前卡住处理**: 刷新页面(进度在 localStorage resume 记录) → 重新选同一文件 → 弹窗点"继续上传" → 从84%续传
- 版本 v1.11.257 (commit 38f5f0e) H5 已部署 (线上 index-BkDfNoQU.js) + 小程序已构建

## v1.11.258: 根治上传卡死 — 真正的根因 = 多课时并行上传 (重要)
- **用户再次反馈**: 第1,2,6,8,9,10节(6节)全部卡住, 刷新后问题依旧, 暂停/继续显示生效但取消一直"正在取消"不结束, 反复多次未好
- **真正根因**: 每集有独立"上传"按钮**无全局限制** → 多课时并行上传时, 每课时内部 6 路并发分片 = 6×6=36 路并发挤在同一 COS 域名连接池 + 浏览器内存同时读 16MB×36 片 → 连接池/内存耗尽, 全部互相拖死; 且取消链路在"取签名(partUploadAuth, apiRequest 无外部 signal)"和旧版"读取阶段"无法中断 → 取消永不生效/生效很慢
- **修复三层**: ①**全局单课时上传锁** — 同时只允许 1 个课时上传, 其他课时点上传提示"请先完成XX集的上传或取消后再传本集" (最核心, 从源头杜绝并行竞争); ②apiRequest 支持 externalSignal + call 透传 signal, partUploadAuth 取签名可被取消/暂停中断; ③cancelEpisodeUpload 3s 乐观兜底 — 取消 3s 后强制复位 UI(_uploading=false/_status='')并尽力保存续传点, 杜绝"正在取消"永久显示 (主流程 catch 幂等)
- **经验教训**: 大视频分片上传必须全局串行(参考百度网盘多文件队列), 禁止多文件并行 — 浏览器连接池(同域名6个 HTTP/1.1)与内存是硬限制; 取消/暂停必须覆盖"读取→签名→PUT"全链路每一环
- 版本 v1.11.258 (commit 63f4732) H5 已部署 (线上 index-IGV1mEK1.js) + 小程序已构建

## v1.11.259: 上传排队策略 (用户要求, 替代硬锁)
- 需求: ①卡住状态自动清空(用户反馈没实现); ②多文件排队策略, 一个传完自动传下一个, 等待的显示"等待上传"
- 实现: ①**上传队列** uploadQueue(存储 {ep,filePath,fileSize}) + currentUploadingEp/currentUploadingCourseId — 点上传选文件后, 无上传中则立即 startUpload, 有则入队显示"⏳等待上传"(可"取消排队"); 完成/失败/取消后 finally 里 dequeueNext 自动开始下一个 (防重入靠 currentUploadingEp 判断); ②取消 3s 兜底也触发 dequeueNext; ③**状态清空**: 刷新页面内存状态必然清空(用户之前看到的是关弹窗重开, 上传任务仍在内存继续); 同课程重开弹窗时按 _key 把上传完成 URL 同步到界面新对象; 切换课程(openCourseForm)时中止旧课程上传+保存续传点+清队列, 防幽灵上传
- 版本 v1.11.259 (commit 27371e1) H5 已部署 (线上 index-BTDNDck0.js) + 小程序已构建

## v1.11.260: [终止全部上传] 一键强制中断按钮
- 用户反馈第2/8节暂停/取消失效, 要求强制中断 → 排查: 用户标签页很可能一直跑旧版代码(旧版读取阶段不可中断); 静态托管 index.html cache-control max-age=300 有 5 分钟 CDN 缓存, 强刷(Cmd+Shift+R)才能确保拿新版
- 新增: 课程弹窗标题旁"⛔ 终止全部上传"按钮 (hasActiveUploads 控制显示) — 一键强制中断所有上传中+排队中任务(cancelled+abortAllParts+保存续传点+清状态清队列), 解决个别课时暂停/取消失效时无法强制中断
- 版本 v1.11.260 (commit ddaeb37) H5 已部署 (线上 index-Q-dxmVQd.js) + 小程序已构建

## v1.11.261: 上传初始化阶段取消立即生效 (关键修复)
- **用户激烈反馈**: 隐私模式(必是最新版)下第2/8节暂停取消仍没反应; 其他课时取消后一传就报"上传中断"
- **真正根因**: 卡在**云存储初始化阶段**(SDK匿名登录 getStorage, 15s 超时窗口内无任何分片请求, abortFns 为空) → 点取消无 abort 目标只能干等 15s → 表现"暂停/取消没反应"; "取消后一传就报上传中断" = 用户网络差(代理/VPN 波动, 早前已记录)导致 getStorage 频繁卡 15s 超时, 且报错文案笼统看不出原因
- **修复**: ①初始化阶段加 250ms 轮询 cancelled 检测, 点取消立即抛 UPLOAD_CANCELLED(不再干等); ②初始化失败文案区分: '云存储初始化失败（网络波动），请检查网络或关闭代理/VPN 后重试' / '云存储初始化失败（服务未就绪），请刷新页面重试' / 其他仍为'上传中断,已保留进度'; ③cancelWaiter 用 stop() 防 interval 泄漏
- 版本 v1.11.261 (commit c08f4b7) H5 已部署 (线上 index-C6K9oMZ5.js) + 小程序已构建

## v1.11.262: 修复"一上传就报上传中断" — 续传 uploadId 失效 (重要)
- **实测**: curl 测线上 storage.createMultipart/partUploadAuth/abortMultipart 全部正常 → 后端无问题, 问题在前端续传链路
- **根因**: 取消/失败后 localStorage 留下 resume 记录(旧 uploadId); 重新上传同一文件续传该 uploadId, 但该未完成分片上传可能已被 COS 清理/失效 → listParts 返回空 → 从头传完全部片 → completeMultipart 服务端查不到分片 → "合并分片失败: 未找到已上传的分片" → 用户看到"上传中断"。反复取消重试积累脏记录 → 一直报
- **修复**: ①cloudbase.js 续传分支 listParts 返回空(且本地有 skipPartNumbers) → 判定 uploadId 失效, 自动重建 createMultipart 从头传(不再合并失败); ②dashboard catch: 错误含"未找到已上传的分片/合并" → removeResume 清除脏记录; ③失败 toast 显示真实原因(截断30字)便于定位; ④课程弹窗新增"清除断点记录"按钮(一键清空 zhs_upload_resume)
- 版本 v1.11.262 (commit e00e69a) H5 已部署 (线上 index-BVYUqqaX.js) + 小程序已构建

## v1.11.263: 上传全链路取消感知 + 后台版本号徽标
- 用户仍反馈第2/8节暂停取消失效 → 根因: 主流程 await 点(probeFileSize/createMultipart/listParts/completeMultipart)无取消检测, 取消只能等其 30s 超时 → 表现"取消没反应"
- 修复: ①uploadMultipartToCos 加 cancelAware 包装(250ms 轮询 control.cancelled, 取消立即抛 UPLOAD_CANCELLED), 包裹 probeFileSize 和所有 call(createMultipart/listParts/partUploadAuth/completeMultipart) — 全链路任何 await 点取消都 ≤250ms 响应; ②后台顶栏标题旁加版本号徽标(点击弹窗显示完整版本/commit/构建日期, 提示强刷), 解决用户反复测试旧缓存页面的问题
- 版本 v1.11.263 (commit d679e90) H5 已部署 (线上 index-BtNBdXMT.js) + 小程序已构建

## v1.11.264: 取消/急停改为"立即强制复位 UI"(终极兜底)
- 用户换浏览器+强刷仍反馈取消不生效 → 放弃"等主流程响应取消"的思路, 改为: **点取消/急停同步强制复位 UI**(ep._uploading=false/清状态/清队列/立即 toast), 不依赖底层请求死活
- 实现: ①cancelEpisodeUpload 点击即同步复位 UI + cancelled + abort 全部 + 保存续传点 + ep._control=null + 释放锁 dequeueNext; ②uploadGenCounter 任务代号(取消/急停自增), startUpload 存 ep._uploadGen=myGen, onProgress/onStatus/成功写 URL 前检查 gen 不匹配则忽略 → 旧任务即使后台残留也不会污染新任务; ③上传各阶段加 console.log([上传] gen=N ...) 便于 F12 定位; ④abortAllUploads 同步强制复位
- 版本 v1.11.264 (commit da41f7a) H5 已部署 (线上 index-CS-TjWDg.js) + 小程序已构建
- **重要教训**: 用户反复反馈"修了没好"的关键是浏览器一直跑旧代码(CDN缓存max-age=300 + 未强刷), 必须给后台加可见版本号让用户自证加载版本

## v1.11.265: 根治"无法读取文件大小" — 大视频 fetch 全读的根因 (重要)
- **进展**: v1.11.264 后用户确认第2/8节**取消已生效**; 新报"上传中断，无法读取文件大小"
- **根因**: probeFileSize 和 readBlobSlice 在 H5 都执行 `fetch(blobURL).then(r=>r.blob())` — **每次把整个视频读进内存**! 几 GB 视频 → fetch 读全文件超时(10s)/内存爆 → size=0 → "无法读取文件大小"; 这也是之前 84% 卡死的同源问题(每片读取都 fetch 全文件)
- **修复**: ①chooseVideo 的 H5 返回 res.originalFile(File对象) → 全程透传 fileObj; ②probeFileSize(fileObj) 直接 .size(零开销); ③readBlobSlice(fileObj) 直接 fileObj.slice(start,end) — **Blob.slice 零拷贝不读全文件**, 内存只占在传的 6×16MB; ④uploadMultipartToCos 签名加 fileObj, size 优先 control.size(chooseVideo的res.size) → fileObj.size → fetch 兜底
- 版本 v1.11.265 (commit df02edc) H5 已部署 (线上 index-DpT9RoYd.js) + 小程序已构建

## v1.11.266: 修复排队视频不自动续传 (C/OSS 弹窗阻塞队列)
- 用户反馈: 排队等待的视频在优先视频上传完成后没有自动开始
- **根因**: startUpload 成功路径 `await maybeMigrateToOss(...)` — 先 await adminSettingsGet(网络请求, 30s 超时) 再 uni.showModal 弹窗**挂起等用户点击** → try 不结束 → finally/dequeueNext 不执行 → 排队的下一个永远不开始
- **修复**: maybeMigrateToOss 改为 setTimeout 1.2s 后 fire-and-forget (.catch 保护), 不阻塞队列; 上传完成立即 dequeueNext 自动开始下一个
- 部署前遇 CloudBase MCP 认证过期 → 设备码重新授权 (PYKA-E2PS, auth status READY) 后成功
- 版本 v1.11.266 (commit 0dd6ef4) H5 已部署 (线上 index-DAmfRFBF.js) + 小程序已构建
