# 2026-08-11 工作日志

## 排盘记录时间格式统一 v1.11.60 (08:20-08:25)
- 背景: 用户反馈 YYYY-MM-DD HH:mm 时间格式 H5/小程序"都没改好"
- 排查: 线上 chunk 已含 getFullYear(新格式已上线), 保存端三处(tools saveHistory/fmtTime + paipan saveDisk)v1.11.59 已加年份
- **根因**: 用户看到的是**旧记录**(v1.11.59 之前保存的 MM-DD HH:mm), 新格式只对新记录生效
- 解决: 显示端兼容 - paipan-records.vue 和 paipan.vue 历史弹窗新增 **fmtRecTime(rec)**: 已含年份直接返回, 旧格式 MM-DD HH:mm 用 rec.ts 时间戳补年份 → 新旧记录显示统一
- 测试: 新/旧/跨年记录(2025-12-31)格式正确; MP 产物 fmtRecTime 完整打包(paipan-records.js @1059 + paipan.js); H5 部署 109文件; GitHub 已推送
- ⚠️ 经验: 用户报"没改好"时先查是否是旧数据缓存/旧记录, 显示端做兼容格式化比改保存端更彻底
- 版本: 1.11.59 → 1.11.60

## 排盘记录/历史功能增强 v1.11.61 (08:35-08:40)
- **①八字label去掉**: paipan-records.vue 记录项 label 加 v-if="rec.type !== 'bazi'" (八字只显示干支两行); paipan.vue 历史弹窗同样处理
- **②六爻/六壬历史按钮**: tools.vue 两个面板标题改 tp-title-row + 📜历史按钮(tp-hist-btn); 历史弹窗(tp-mask/tp-sheet/th-*)按 type 筛选 liuyao/liuren 记录, 点击恢复盘面(直接设 lyFull/lrFull), 删除/编辑备注(editToolHistoryRemark 用 showModal editable)
- **③奇门预测事件**: form.bazi.eventText 字段; 模板 activeTool==='qimen' && useNow 时显示"预测事件"输入框(tp-event-input, placeholder 灰字); runPaipan 写入 qm.eventText; saveHistoryRecord 存 rec.eventText; paipan saveDisk 从 data.qimen.eventText 读存记录
- **④记录显示事件**: paipan-records.vue rec-event + paipan.vue hist-event 另起一行显示"🔮 事件"
- 验证: MP tools.wxml 📜历史×2/预测事件×1/历史弹窗✅; 记录页 rec-event✅ label条件✅; tools.js eventText×8 + restore 逻辑✅; H5 部署 109文件; GitHub 已推送
- 版本: 1.11.60 → 1.11.61

## 六爻/六壬预测事件输入框 v1.11.62 (08:45-08:48)
- form.liuyao.eventText + form.liuren.eventText 字段; 六爻面板(标题下摇卦按钮上)/六壬面板(日期时辰行下开始排盘按钮上)各加"预测事件"输入框(tp-event-input 复用奇门样式)
- saveHistory 加第6参数 eventText 存 rec.eventText; saveLiuyao/saveLiuren 传值; tools 历史弹窗 th-event 显示; paipan-records rec-event 自动显示六壬/六爻事件
- 验证: MP wxml 预测事件×3(奇门+六爻+六壬)/placeholder×3/th-event✅; H5 部署 109文件; GitHub 已推送
- 版本: 1.11.61 → 1.11.62

## 六壬时刻选择+当前时间开关 v1.11.63 (08:50-08:53)
- form.liuren 加 time('13:30')/useNow(true默认开); 六壬面板加"当前时间"开关(tp-now-row, 默认开启提示文案), 日期时辰行改 picker mode=time 具体时刻(去掉午时selector)
- runLiuren: useNow 用当下日期+时刻起盘(同步回表单), 否则手动日期+时刻; 新增 timeToShichenIdx(t) 时刻→时辰序(子时23-1跨天特判), fullLiuren 仍用时辰序
- 测试: 6 个时刻边界全对(13:30→未7/23:15→子0/00:30→子0/07:45→辰4/12:30→午6/21:30→亥11)
- 验证: MP wxml mode=time×2/当前时间开关文案✅ JS 子时跨天+useNow:!0 默认true✅; H5 部署 109文件; GitHub 已推送
- 版本: 1.11.62 → 1.11.63

## 六爻/六壬保存此盘弹窗 v1.11.64 (09:13-09:16)
- saveLiuyao/saveLiuren 改为弹窗流程: 构建 _pendingSave(type/label/gzText/data/evt) → 弹"保存此盘"自定义弹窗(ts-save-input 备注输入 placeholder灰字, 复用 tp-mask/tp-sheet) → confirmToolSave 确认后 saveHistory(type,label,gzText,data,remark,eventText)
- 补 tools.vue 缺失的 .sheet-title 样式; 保存按钮文案仍是"保存此卦/保存此课"但点击弹"保存此盘"备注框(与奇门/紫微一致)
- 验证: MP wxml 保存此盘标题/placeholder/ts-save-input✅ JS saveHistory 含 remark:l+eventText:u 字段✅; H5 部署 109文件; GitHub 已推送
- 版本: 1.11.63 → 1.11.64

## 修复道号显示不一致 v1.11.65 (09:23-09:27)
- 现象: 齐静春(uid4)道号在"我的"页与个人主页显示不一致
- **根因**: 云函数 user.profile 返回 dao_code, 但 user.vue onShow 刷新只回写 avatar 不回写 dao_code → "我的"页用登录时 store 缓存, 个人主页 profile.vue 每次从接口拉最新 → 道号被管理员修改后两页不一致
- 修复: ①user.vue onShow 回写 dao_code+invite_code(setUserInfo) ②profile.vue 顶部道号加 invite_code 兜底(与我的页 dao_code||invite_code 逻辑一致)
- 查库: users 集合 nickname=齐静春 dao_code=invite_code=ZHS00001 数据本身一致 (MCP: cloudbase.readNoSqlDatabaseContent)
- 验证: MP user.js setUserInfo({avatar,dao_code...})回写✅ profile.js invite_code×3✅; H5 部署 109文件; GitHub 已推送
- ⚠️ 经验: 我的页 store 缓存 vs 个人主页接口拉取 = 数据不一致常见根因; onShow 刷新应回写全部展示字段(avatar/dao_code/vip等)

## 生成 Android APK v1.11.65 (19:58-20:05)
- 流程: mobile/ 是 Capacitor 工程(webDir=dist/build/h5) → `npx cap sync android` → `cd mobile/android && ./gradlew assembleRelease` (33s, BUILD SUCCESSFUL)
- 产物: mobile/android/app/build/outputs/apk/release/app-release.apk (4.2M, 签名 daoyuan.keystore/daoyuan2026, versionCode 149, versionName 1.11.65, 包名 cn.codebuddy.zhs.workbuddy, 应用名道元易学)
- 发布: APK 复制为 daoyuan-v1.11.65.apk + scripts/download-page/index.html → dist/build/h5/apk/ + download/ → 上传静态托管 (111文件) → 线上 https://cloud1-d8gs2k9m311f7272f-1464523137.tcloudbaseapp.com/apk/daoyuan-v1.11.65.apk (200, 4.16MB) + /download/ 下载页 (200)
- ⚠️ 注意: 上传前需 CloudBase 认证(设备码 auth start_auth→用户浏览器授权→status READY), 会话过期需重新授权; download-page/index.html 的 APK 文件名由 bump-version.js 自动更新
- ⚠️ 经验: 部署 APK 到静态托管需把文件放进 dist/build/h5 再上传整个目录 (uploadFiles 已废弃)
- GitHub 已推送
- 版本: 1.11.64 → 1.11.65

## 历史按钮移到开始排盘页 v1.11.66 (20:06-20:10)
- 四柱/奇门/紫微共用面板标题改 tp-title-row + 📜历史按钮(openToolHistory(activeTool)); 历史弹窗通用化: TOOL_NAME 映射 5 工具(bazi=四柱/qimen=奇门/ziwei=紫微/liuren=六壬/liuyao=六爻), 八字记录两行五行色(th-gz-bazi/hz-line/wx-*样式), 隐藏 bazi label
- useToolHistory 分支: bazi/qimen/ziwei → 写 paipan_data 跳结果页对应 tab (同 paipan-records); liuyao/liuren → 恢复本页盘面
- 验证: MP wxml 📜历史×3(共用+六爻+六壬) th-gz-bazi✅ TOOL_NAME✅ paipan_data跳转✅; H5 部署 109文件
- ⚠️ push 卡住(GitHub 443): 本地 commit 60af405 已完成, push 待网络恢复重推; uni build 会清空 dist/build/h5, 重新构建后 apk/download 目录需重新复制(线上 APK 仍可下载, upload 不删线上文件)
- 版本: 1.11.65 → 1.11.66

## 修复订单删除+批量删除 v1.11.67 (20:10-20:15)
- **根因**: order/list.vue loadOrders 调 getOrders({status:'全部'}) **未传 uid** → 云函数 listOrders 返回所有用户前50单 → 用户订单列表出现他人订单(含uid=0测试单), 点删除被 deleteUserOrder 的 uid 归属校验拦截 → "订单不存在或无权删除"
- 修复: ①前端 loadOrders 传 uid ②云函数 listOrders 无 uid 直接返回 [] (安全兜底, 防订单泄露)
- **批量删除**: 订单列表顶部加批量操作条(batch-bar): ☑批量删除进入管理模式 → 每单勾选框(card-check) + 全选/删除所选(n)/取消; batchDelete 遍历调 deleteOrder(带uid), 统计成功/失败数, 删除后刷新
- 验证: 云函数网关实测 无uid→[] ✅ uid=4→只返回自己2单 ✅; MP wxml 批量删除/勾选框/全选✅; H5 部署 109文件; 云函数 dy-api 已重新部署 ✅
- ⚠️ push 仍卡住(GitHub 443): 本地 commit 9971f66 已完成 (v1.11.66 的 60af405 也待推), 网络恢复后 push
- 版本: 1.11.66 → 1.11.67

## APP 模拟支付 + 微信 logo 图标 v1.11.68 (20:13-20:18)
- **①APP/H5 提交订单显示模拟支付**: checkout.vue 支付方式名平台化(wechatPayName: 小程序=微信支付, #ifndef MP-WEIXIN=模拟支付, 三处 payMethods 用常量); order/detail.vue payName 同样平台化; 提交按钮文案已有条件编译(小程序=微信支付, 其他=模拟支付)
- **②💚绿心→微信logo**: PIL 生成 src/static/pay-wechat.png (72x72, 微信绿#07C160圆角方块+白色对话气泡+眼睛), checkout.vue 模板 image 显示
- ⚠️ **关键坑**: uni-app MP 构建下 image 静态 src 会被编译成 {{p.b}} 数据绑定但数据无该字段 → 图标不显示; 必须**数据驱动**: payMethods 元素加 img:'/static/pay-wechat.png' + :src="p.img"
- 验证: MP checkout.js pay-wechat.png×3✅ wxml src数据绑定✅ 小程序端无模拟支付字样✅; H5 部署 110文件(含新图标); 线上图标 200✅; **GitHub push 恢复成功(1439a7b..0741f1f)**
- 版本: 1.11.67 → 1.11.68

## 后台首页直播入口+动态删除对齐 v1.11.69 (21:55-22:00)
- **①直播入口默认关闭**: 数据库 settings home 组原 show_live='1'(开启) → 改为 '0'; 代码层(dashboard默认false/云函数默认false/首页buildTabs仅show_live===true才加直播tab)本就默认关闭; 实测 app.payConfig → show_live:False ✅
- ⚠️ **关键坑**: writeNoSqlDatabaseContent action=update 的 update 是**整体替换语义**(非$set) → 只传 show_live 会把 home 文档 group/show_follow/show_publish 全清掉, 导致关注tab/发布按钮被误关; 必须传全字段恢复 (已用 _id 匹配恢复 group+show_follow='1'+show_publish='1'+show_live='0')
- **②动态管理删除按钮对齐**: 推荐/取消推荐按钮加 .op-fixed(固定宽150rpx 居中), "删除"不再受前面文字长度影响, 各行动态删除按钮上下对齐
- 验证: MP dashboard.wxml op-fixed✅; H5 部署 110文件; GitHub 推送成功(0741f1f..dd6f70e)
- 版本: 1.11.68 → 1.11.69

## 反馈上传图片+订单操作对齐 v1.11.70 (22:14-22:20)
- **①意见反馈上传图片**: feedback.vue 加图片九宫格(fb-imgs/fb-img-item/fb-img-add, 最多3张) → chooseImage→getStorage().uploadFile 云存储 feedback/ 目录 → resolveCloudUrl 显示(小程序cloud://原生渲染, H5转URL) → 提交存 images: [fileID]; 云函数 submitFeedback 加 images 字段(数组,最多3); 后台反馈管理 loadFeedbacks 转 _imgs 显示缩略图(fb-thumb 120rpx)+previewFbImg 预览
- 验证: 接口 user.feedback 带 images 成功存储并返回✅ 测试数据已删; MP wxml fb-img-add/op-col×4/fb-thumb✅; 云函数已部署; H5 部署 110文件; GitHub 推送成功(dd6f70e..147fb1d)
- **②订单管理操作对齐**: 操作列改固定4列(op-col 108rpx居中, w-ops-4 470rpx), 未显示的操作用 visibility:hidden 占位 → 代收款/发货/退款/删除各列上下对齐, 不受各订单状态组合/按钮数量影响
- ⚠️ 认证会话约2小时过期: 部署前需重新设备码认证(auth start_auth→浏览器授权)
- 版本: 1.11.69 → 1.11.70
