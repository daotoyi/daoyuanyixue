# 2026-08-06 工作日志

# ============ CloudBase → 微信云开发环境迁移 (v2.0.2) ============

## 背景
- 旧环境 zhenhesheng-d6gkez7p221305432 (腾讯云 APPID 1309518368, 账号 UIN 100023638829) 认证主体与目标小程序不一致
- 目标: 迁移到微信云开发环境 cloud1-d8gs2k9m311f7272f (小程序专属, 同主体, 个人版免费期到 2027-02-06)

## 关键发现
1. **CLI 双账号切换**: tcb CLI 凭证在 ~/.config/.cloudbase/auth.json, 切换账号 = 备份/恢复该文件
   - 旧账号登录(浏览器旧会话) → env list 只见 zhenhesheng
   - 小程序账号登录(授权时选「小程序公众号」方式) → env list 只见 cloud1
   - 两份凭证 uin 都显示 100023638829 (登录者 UIN), 但权限空间不同
2. **微信云开发环境**: 由微信开发者工具创建, 挂在「小程序专属腾讯云账号」, tcb CLI 用小程序账号凭证可管理 (fn/hosting/storage 均支持)
3. **HTTP 网关**: 微信云开发环境默认域 cloud1-d8gs2k9m311f7272f-1464523137.ap-shanghai.app.tcloudbase.com (新 APPID=1464523137), 需 `tcb routes add` 创建 /dy-api 路由指向 SCF 才能 HTTP 调用
4. **数据库迁移通道**: tcb CLI 无集合导出命令; node-sdk 签名不兼容; MCP 认证交互走不通 → 最终方案: 控制台导出 NDJSON(每行一条) + 临时云函数 db-import (event.data 传数据, createCollection+分批 add) + CLI fn invoke -d 调用
   - **坑1**: db-import 需在 package.json 声明 @cloudbase/node-sdk ^2.9.0 (否则运行时 require 失败崩溃)
   - **坑2**: CLI fn invoke 传参必须用 -d (位置参数会 event 成字符串)
   - **坑3**: 新环境集合不存在需先 createCollection
   - **坑4**: 已删除的临时函数要从 cloudfunctions/ 移除避免重复部署
5. **IP 白名单**: 微信 API (api.weixin.qq.com) 需加调用方公网 IP 白名单 (182.118.238.115), 本机出口 IP 不稳定

## 迁移清单 (全部完成)
- 云函数: dy-api / getOpenId 部署到 cloud1 (含 DEEPSEEK_KEY, 重部署含新 envId)
- 数据库: 16 集合 91 条 (users 8/orders 4/products 12/categories 8/coupons 10/course_categories 5/courses 6/favorites 2/feedbacks 1/footprints 13/live_bookings 2/live_streams 3/messages 2/moments 5/settings 3/user_courses 7) 验证 dy-api 可读 ✅
- 云存储: 旧环境为空, 跳过
- 静态托管: H5(115文件)/APK(15)/下载页(1) → cloud1
- HTTP 路由: /dy-api → SCF dy-api (tcb routes add)
- 代码: 全局替换 envId (23处) + APPID 1309518368→1464523137 (16处), 涉及 9 文件
- 验证: 新网关 POST products.list 返回 12 商品, CORS 正常 (OPTIONS 204)

## 用户导出数据位置
- /Users/wenhua/Downloads/00-zhs/deploy-data/ (16 个 database_export-*.json)
- AppSecret: 用户提供, 已因 IP 白名单绕过未使用 (改为云函数通道)

## 遗留/注意
- 微信支付需在新环境「云开发控制台→微信支付」重新开通绑定商户号
- 小程序代码: wx.cloud.init env=cloud1 已生效, 开发者工具导入 dist/build/mp-weixin 上传
- 旧环境保留 (zhenhesheng), 验证通过后可退役
- 部署脚本 .workbuddy/scripts/tcb.sh 的 envId 已改 cloud1

## 数据嵌套 BUG 修复 (20:17)
- **症状**: 首页/商城/直播空白, 接口返回 {"_id", "data": {...}} 嵌套结构
- **根因**: node-sdk 的 add 是 add(doc) 直接传文档 (dy-api 一直这么用), 我误用 web SDK 的 add({data: doc}) 语法 → 每个文档被包一层 data 字段
- **修复**: 临时云函数 db-import-fix: 清空集合 + add(doc) 逐条重插 (超时默认3s不够→config update fn --timeout 120)
- **验证**: moments/products/live/user.login 全部平铺返回正常 ✅
- **教训**: CloudBase node-sdk (云函数) add 语法 = add(doc); 微信云开发批量 add({data: 数组}) 与 node-sdk 行为不同, 统一逐条 add(doc)
