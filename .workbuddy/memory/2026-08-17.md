# 2026-08-17 工作日志

## v1.11.150: 订单售后功能（商品/课程反馈 + 后台售后管理）

### 云函数 (dy-api/index.js)
- 新增 `aftersales` 集合支持，5 个接口：
  - `aftersale.submit`: 用户提交售后（校验订单归属+类型仅商品/课程，集合不存在时自动 createCollection 容错）
  - `aftersale.my`: 用户查自己的售后记录
  - `admin.aftersales.list`: 后台列表（可按状态筛选）
  - `admin.aftersales.reply`: 后台处理（回复+状态流转「处理中/已处理」+ 推送消息给用户）
  - `admin.aftersales.delete`: 删除记录
- MANAGER_ROLES 增加 3 个 admin.aftersales.* 路由（管理员可处理售后）

### 前端
- **共享组件** `src/components/aftersale-popup/aftersale-popup.vue`（easycom autoscan 自动注册 `<aftersale-popup>`）：
  - 历史售后记录展示（含 cloud:// 图片转签名 URL、商家回复回显）
  - 新增反馈表单（textarea 500字 + 图片上传最多3张，复用 feedback.vue 的 getStorage+resolveCloudUrl 模式）
- **订单列表 list.vue**: 商品/课程订单且非待付款 → 显示「售后」按钮（有处理中记录显示「售后中」高亮）；onShow 同步拉取售后记录
- **订单详情 detail.vue**: action-bar 放开已完成/已退款状态显示，商品/课程订单显示「售后反馈」按钮

### 后台 dashboard.vue
- 新增「售后管理」模块（modules + MANAGER_MODULES）
- 表格：用户/订单、售后内容+图片预览、时间、状态（待处理/处理中/已处理）、操作（处理/删除）
- 处理弹窗：显示订单详情+问题描述+图片，状态 pill 选择，回复 textarea，确认后推送消息

### 部署
- 三端构建成功（H5 + mp-weixin）
- 云函数 dy-api 部署成功
- H5 静态托管上传成功
- Git 提交推送 0d2fe30 → GitHub

### 关键设计
- 售后按钮仅对商品/课程订单、已支付(非待付款)状态显示
- 每单允许多条售后记录，历史记录在弹窗内展示
- 集合自动创建容错，无需手动建表

## v1.11.151: 课程7日退款 + 后台订单管理改进 + 订单分析模块

### 1. 课程7日退款
- **云函数** `courseRefund`: 校验 order_type=course + status=已完成 + 7日内(pay_time/created_at) + user_courses progress=0且无 opened_lessons → 退款(元宝退回/微信v3退款) + 删除 user_courses 记录(收回课程权限) + 标记订单已退款 + 推送消息
- **路由** `order.courseRefund` 注册
- **前端** list.vue + detail.vue: 课程订单已完成且7日内 → 显示「申请退款」按钮(canCourseRefund 判断 + doCourseRefund 弹窗确认)
- **api.js** 导出 `courseRefund`

### 2. 后台订单管理改进
- orderTypeFilterOptions 增加 `{ value: 'tool_unlock', label: 'AI解盘' }` 分类
- 订单表格订单号和商品之间增加「用户名」列(w-user)，adminList 云函数对 orders 集合关联 users 表返回 nickname

### 3. 订单分析模块
- **云函数** `adminOrderAnalysis`: 拉全部已支付订单 → 按类型(product/course/tool_unlock)聚合 count+amount → 用户消费排名(排除退款订单, top20, 补全 nickname) → 返回 { pieData, ranking }
- **路由** `admin.orderAnalysis` 注册 + MANAGER_ROUTES 放行
- **dashboard.vue** 新增「订单分析」模块(📈图标):
  - 饼图: conic-gradient CSS 渲染(无第三方图表库), 中心显示总订单数, 右侧图例(颜色+标签+单数+金额)
  - 用户消费排名: TOP20 表格(排名/用户名/订单数/消费金额), 前3名高亮
- pieColors = ['#c4753a', '#6e8b4e', '#8b6a9e', '#b85c5c']

### 部署
- 三端构建成功（H5 + mp-weixin）
- 云函数 dy-api 部署成功
- H5 静态托管上传成功
- Git 提交推送 700e5b3 → GitHub

## v1.11.152: 默认关闭首页直播/发布动态 + 生成 APK

### 默认值语义反转 (appPayConfig)
- `allow_publish_moment`: `!== '0'`(默认开) → `=== '1' || === true`(默认**关**)
- `rec_show_live`: `!== '0'`(默认开) → `=== '1' || === true`(默认**关**)
- catch 兜底返回值同步改为 false
- publishMoment 权限检查同步反转: moment 组未显式开启即拒绝发布

### 数据库 settings 实际值 ($set 局部更新)
- home: show_live='0', show_publish='0'
- moment: allow_publish_moment='0'
- recommend: rec_show_live='0'
- 注意: writeNoSqlDatabaseContent 需要 `action` 参数(顶层), update 用 `{"$set":{...}}` 避免整体替换

### 部署
- 云函数 + H5 静态托管已部署, Git b86aa05
- APK: cap sync → gradlew assembleRelease → 复制 dist/build/h5/apk+download → 上传托管
- cap sync 必须在 mobile/ 目录下执行
