# 2026-08-29 工作日志

## v1.11.222: 盘道详情不限字数 + 盘道/直播场次排序 (推荐页同步)
- 需求：① 后台-盘道-详情内容不要限制字数 ② 盘道活动场次顺序可调整，首页推荐页的直播活动顺序也同步（不体现序号）
- 改动：
  1. `cloudfunctions/dy-api/index.js`：
     - `adminPandaoCreate/Update`：content 去掉 `.slice(0,2000)` 限制（不限字数）；desc 放宽到 2000；新增 sort 字段（默认=id）
     - `pandaoList()`：改为按 sort 排序（无 sort 用 id），后台调整顺序前台同步
     - `listLiveStreams()`：按 sort 排序（有 sort 优先，无则按 start_time 倒序）
     - `adminLiveCreate/Update`：新增 sort 字段
     - `adminList()`：pandao_sessions / live_streams 两个集合返回前按 sort 排序（后台列表顺序与前台一致）
  2. `src/pages-sub/admin/dashboard.vue`：
     - 盘道场次列表操作区加 ↑↓ 按钮（movePandao 交换 sort）
     - 直播列表操作区加 ↑↓ 按钮（moveLive 交换 sort）
     - 盘道详情内容 textarea 加高（pd-content-ta 320rpx，不限字数长文编辑）
- 排序机制：后台 ↑↓ 交换两个元素的 sort 值（sort || id），保存后重新加载；前台推荐页通过 pandaoList/listLiveStreams 接口自然同步；**不显示序号**
- 版本 v1.11.222 (commit ab6dbde) 构建部署：H5 + 云函数 dy-api 均已更新，小程序已构建
- ⚠️ version.js 三个字段（APP_VERSION/APP_COMMIT/APP_FULL_VERSION）已同步为 v1.11.222

## 微信支付"小程序违规"排查 (8-28 晚, 延续)
- 现象：requestPayment:fail 小程序违规，支付功能暂时无法使用；站内信无违规、虚拟支付已开通、支付权限 OK
- **根因确认：小程序未接入「订单信息录入」(订单管理)**——微信 2024-11 起强制交易类小程序接入，不接入禁用支付
- 解决：微信后台 → 支付与交易 → 订单管理 → 订单信息录入 → 填 `pages-sub/order/detail?order_no=${商品订单号}`（⚠️ 分包路径必须带 -sub，填 pages/order/detail 会报"无效的路径"）
- 发货信息：无需每单手动填——课程虚拟订单支付成功自动上报(logisticsType=3)；实体商品后台发货时填物流公司+运单号自动上报(logisticsType=1)；⚠️ 微信要求 48 小时发货时限

## v1.11.223: 修复盘道详情内容前端字数限制
- 现象：v1.11.222 已去掉云函数 2000 字限制，但后台盘道详情内容仍限制字数
- **根因：uni-app `<textarea>` 默认 maxlength=140，不显式设置就截断**——上一版只改了云函数，漏了前端组件属性
- 修复：盘道详情内容 textarea 加 `:maxlength="-1"`；顺手给后台所有 textarea 统一加（直播描述/课程描述/课时文本/商品描述/回复内容/分类描述/图片列表）
- 版本 v1.11.223 (commit 9e09ac7) 已构建部署 H5 + 小程序已构建
- ⚠️ 教训：uni-app textarea 默认 140 字上限，凡是长文本输入框必须显式 :maxlength="-1"

## v1.11.224: 盘道说明(desc)作为副标题显示前端
- 需求：后台盘道管理的"说明"内容作为副标题显示在前端
- 改动：
  1. `src/pages-sub/pandao/detail.vue`：标题下方新增 `.pd-subtitle` 显示 session.desc（灰色小字）；活动介绍仍显示 content（content 为空时 desc 兜底）
  2. `src/pages/index/index.vue`：推荐页盘道卡片标题下新增 `.rec-pd-desc` 显示 pd.desc（单行省略）；盘道频道列表原本已有 pandao-desc 显示
- 版本 v1.11.224 (commit 8486704) 已构建部署 H5 + 小程序已构建

## v1.11.225: 课时文本说明移至课时播放页视频下方
- 需求：课时文本说明(ep.text)点击进课时后显示在视频下面，大纲目录不显示
- 改动：
  1. `src/pages-sub/course/detail.vue`：删除大纲中 lesson-text 显示及 .lesson-text 样式
  2. `src/pages-sub/course/lesson.vue`：视频播放器下方新增 `.ls-note` 区块显示 lesson.text（pre-wrap 保留换行）
- 版本 v1.11.225 (commit 37f5d36) H5 已部署 + 小程序已构建
- ⚠️ push 失败（github 443 网络不通），本地已 commit，待网络恢复后重推

## v1.11.226: 修复支付回调丢失导致订单状态不同步
- 现象：订单 DY1787932222623985 微信侧已支付(¥0.50, 08-28 23:50:36 SUCCESS)，但本地订单管理仍"待付款"
- 根因：微信支付回调丢失（回调到达前云函数部署/网络抖动），本地订单永远停在待付款；前端支付成功后只 load() 不主动查单
- 修复（三层防护）：
  1. **前端**：小程序支付成功后主动调 `wxpayQuerySync` 查单同步（order/detail.vue + order/list.vue 支付分支，回调丢失兜底）
  2. **云函数**：新增 `admin.orders.reconcile` 批量对账接口——遍历最近待付款订单逐个微信查单，SUCCESS→markOrderPaid 更新，CLOSED→已取消；days 默认3天 limit 50
  3. **后台**：订单管理页新增"对账修复"按钮（一键对账，显示修复笔数）
- 当前订单已通过 pay.querySync 手动修复为"待发货"✅（对账接口需管理员 token，未在网关验证）
- 版本 v1.11.226 (commit 1405c94) H5 已部署 + 云函数 dy-api 已更新 + 小程序已构建；git push 成功（连带 v1.11.225 一起推送）
- ⚠️ 排查方法备忘：`node cloudfunctions/dy-api/wxpay-v3.js` 直调 queryOrder 查微信侧真实状态；CLS 日志搜 `log:"订单号"` 查本地记录

## v1.11.227: 课程价格为0(免费)时所有课时不上锁
- 需求：课程价格设置为0（免费）后，所有付费课时都不上锁
- 改动（纯前端，3处锁定判断加 isFreeCourse 条件）：
  1. `src/pages-sub/course/detail.vue`：大纲锁图标 `v-if="!isFreeCourse && ep.free === false && !isOwned"`；openLesson 付费拦截同样加条件；新增 `isFreeCourse` computed（isFreePrice 判断课程价格）；buyCourse 免费课程直接跳第一个有视频的课时（不走结算）
  2. `src/pages-sub/course/lesson.vue`：locked 判断加 `!isFreeCourse.value`
- 版本 v1.11.227 (commit 4aed9b0) H5 已部署 + 小程序已构建；git push 成功

## v1.11.228: 注册验证码 + 忘记密码 (腾讯云短信)
- 需求：①注册输入手机号/邮箱后发验证码，验证通过才能注册 ②登录页增加忘记密码，验证码重置密码
- 用户确认：腾讯云短信通道（后台短信配置预留项），邮件暂不做
- 云函数 `cloudfunctions/dy-api/index.js`：
  1. `tencentSmsSend()`：腾讯云短信 API v3 TC3-HMAC-SHA256 签名，零依赖（crypto+https）
  2. `auth.sendCode`：发验证码——手机号→腾讯云短信（+86前缀），邮箱→提示未开通；60秒防重发；验证码存 verify_codes 集合（scene=register/forgot，5分钟有效）
  3. `register` 增加 code 参数校验（先查重后验码，避免无效消耗）
  4. `auth.resetPassword`：验证码校验（forgot 场景）+ 重置密码
  5. 短信配置从 settings 集合 sms 组读取（provider/secret_id/secret_key/sign/template_id/region）
- 前端 `src/pages-sub/login/login.vue`：注册模式加验证码输入+获取按钮（60s倒计时）；登录模式加"忘记密码"链接→弹窗（账号+验证码+新密码）；api.js 加 sendVerifyCode/resetPassword
- ⚠️ **未配置短信时功能不可用**：需在后台【系统设置-短信配置】填 SecretId/SecretKey/签名/模板ID 后生效；腾讯云短信需开通 SMS 服务并审核模板
- 部署后约10秒生效（首次调用 404 → 等待后正常）
- 版本 v1.11.228 (commit 1f0fa67) H5 + 云函数已部署 + 小程序已构建；接口验证通过

## v1.11.229: 短信 TC3 签名日期格式修复 + SmsSdkAppId 支持
- ⚠️ **关键坑：腾讯云 TC3-HMAC-SHA256 签名的 Date 必须是 `YYYY-MM-DD`（带横线）**，写成 YYYYMMDD 会报 `AuthFailure.InvalidAuthorization: Date in Credential not valid format`
- tencentSmsSend 增加 `smsSdkAppId` 参数（SendSms 接口必需，短信控制台创建应用后 140 开头）；sendVerifyCode 增加 sign/template_id 缺失时的明确提示
- 后台短信配置表单增加「短信应用ID(SdkAppId)」字段
- 用户已提供 SecretId/SecretKey 并写入数据库 settings 集合 sms 组（writeNoSqlDatabaseContent action=update + update 参数整体替换）
- **探测发现：该密钥下签名列表/模板列表均为空** → 用户需在腾讯云短信控制台创建：①短信应用(SdkAppId) ②签名(审核2h) ③验证码模板(审核)
- 版本 v1.11.229 (commit f3bddca) 已构建部署
- 调试脚本: /tmp/sms-probe.js (TC3 签名直调腾讯云 API 查签名/模板列表)

## v1.11.230 + 短信配置完成: 腾讯云短信验证码全链路打通 ✅
- v1.11.230 (commit 73cc89e): 短信双方案支持（provider=cloudbase 扩展 / tencent 直连），后台短信配置加「方案」下拉开关（picker select 类型）
- fix commit 015a465: CloudBase 扩展注册名是 **SMS**（大写，非 'sms'），扩展内部走 https://{env}.service.tcloudbase.com/tcb-ext-sms
- **最终配置（settings 集合 sms 组, provider=tencent）**：
  - SmsSdkAppId: **1401184472** | 签名: **北京真和盛文化** (ID 715503, 已审核) | 模板: **2716970** (验证码, 内容 `{1}为您的登录验证码，请于{2}分钟内填写...`, 已审核)
  - SecretId/SecretKey: 用户 AKID****(已清除) / FONE****(已清除)
- **实测成功**：SendSms 返回 Code:"Ok" send success，序列号 99:421054646217879773828045393 → 手机 18500353930 收到验证码
- ⚠️ **教训：测试手机号 13800138000 不是用户真实号 → 发短信报 SmsSdkAppId 校验失败误导排查**；用户真实手机号 **18500353930**
- ⚠️ 注意：真实手机号用于测试可能产生短信费用；验证码入库 verify_codes 集合 (tencent 直连方案自管验证码, 5分钟有效)

## v1.11.231: 登录/注册界面取消邮箱方式
- 需求：登录/注册界面取消邮箱方式，仅手机号
- 改动：`src/pages-sub/login/login.vue`（纯前端）：
  - 登录标签「手机号 / 邮箱 / 道号」→「手机号 / 道号」；注册标签「手机号 / 邮箱」→「手机号」
  - 忘记密码弹窗占位符去掉邮箱；sendCode/doResetPassword/submit 校验去掉 isEmail 分支，仅手机号正则
  - 云函数 register/login 保留 email 字段支持（老账号不受影响，前端不再传）

## v1.11.232: 修复小程序接管板块在系统设置所有菜单下重复显示
- 现象：系统设置里短信配置、公众号等所有菜单下都显示了「小程序接管」板块
- **根因：dashboard.vue 中"小程序接管"卡片用的是裸 `v-else`，实际配对的是前面 C/OSS 卡片的 `v-if="activeSettingsTab === 'oss' && ossEnabled"`**，导致除 oss 外的所有 tab 都命中 else 分支
- 修复：`v-else` → `v-else-if="activeSettingsTab === 'wxmp'"`，小程序接管只在自己 tab 显示
- ⚠️ 教训：Vue 模板中 `v-else` 必须紧跟在对应的 `v-if` 块之后（中间可隔注释），裸 v-else 会静默配对到最近的 v-if，造成条件失控
- 版本 v1.11.232 (commit ea8cf80) H5 已部署 + 小程序已构建

## v1.11.233: C/OSS 视频存储管理显示文件大小
- 需求：视频存储管理列表刷新后显示每个存储文件的大小
- 云函数 `adminOssVideosList`：新增 `httpsHeadSize(url)`（HEAD 请求取 content-length）；本地存储（tcb.qcloud.la / cloud://）先 getTempFileURL 换签名 URL 再 HEAD，C/OSS 直接 HEAD；每项返回 `size_bytes`（失败 null）
- 前端 dashboard.vue：视频表格加「大小」列（oss-col-size 120rpx），新增 `fmtFileSize()`（B/KB/MB/GB 智能显示，无数据显 -）
- 版本 v1.11.233 (commit 590c440) H5 + 云函数已部署 + 小程序已构建
- ⚠️ 注意：HEAD 大小会逐个视频发起请求，课程多时列表接口稍慢；推送仍被 GitHub 拦截（本地已提交）

## v1.11.234: 视频存储管理刷新列表显示本地/C/OSS存储体积
- 需求：刷新列表表头后，分别展示本地和 C/OSS 上的存储体积（总大小）
- 改动（纯前端 dashboard.vue）：新增 `ossLocalBytes` / `ossRemoteBytes` computed（size_bytes 求和，null 跳过）；刷新按钮右侧 summary 显示「本地 X 个 · 256.3 MB / C/OSS Y 个 · 1.2 GB」
- 版本 v1.11.234 (commit 89f0f40) H5 已部署 + 小程序已构建

## v1.11.235: 验证码按钮红色居中 + 注册页布局紧凑
- 需求：①获取验证码按钮居中+带点颜色 ②注册页布局上下紧凑
- 改动 login.vue（纯前端）：
  - code-btn / forgot-code-btn 去掉 `plain` 类（透明灰字）→ 品牌红 #c41e3a 背景白字；加 `width:auto !important` 覆盖全局 `.form-card .btn-p{width:100%}`（该样式把验证码按钮撑满导致偏左/变形）；input 加 min-width:0
  - 紧凑：form-card padding 40→32rpx 上下、form-tabs margin-bottom 40→28rpx、ft padding-bottom 20→16rpx、field margin-bottom 30→22rpx、field-label margin 12→10rpx、field-input height 88→84rpx、brand padding 60/70→44/48rpx
- 版本 v1.11.235 (commit 524f1ae) H5 已部署 + 小程序已构建

## v1.11.236: 课程视频支持 4GB+ 大文件 (COS Multipart Upload 分片上传)
- 需求：课程视频超 4G 无法上传成功；用户明确: 单文件>5GB 自动分块上传
- 根因：H5 直传 COS 单次 PUT/POST 上限 5GB, 4GB 附近已不稳定; 且原 uploadFile 对非图片会先整文件读内存转 base64 再直传(4G 视频直接爆内存)
- 方案: COS Multipart Upload, 云函数签发分片预签名 URL, 前端 fetch 直传分片
- 云函数 dy-api 新增 4 接口 (路由 storage.*):
  - createMultipart: cos.multipartInit → uploadId (ContentType=video/mp4)
  - partUploadAuth: cos.getObjectUrl(Method:PUT, Query:{partNumber,uploadId}, Expires:600) → 预签名URL (含 x-cos-security-token 参数, 前端直接 PUT 无需 header)
  - completeMultipart: parts 缺省时 cos.multipartListPart 服务端查已传分片 ETag (规避浏览器 CORS 读不到 ETag 头) → multipartComplete → 返回 fileID
  - abortMultipart: 失败清理
- ⚠️ SDK 坑: multipartListPart 返回字段是 **Part** 不是 Parts; PartNumber 是字符串 "1" 需 Number() 转换后与前端数字数组匹配, 否则"未找到已上传的分片"
- 依赖: package.json 新增 cos-nodejs-sdk-v5 (平台部署自动安装, 已验证)
- 密钥: 云函数运行时 TENCENTCLOUD_SECRETID/SECRETKEY/SESSIONTOKEN (自动注入, 权限=本环境 COS), getCos() 懒加载
- 前端 src/api/cloudbase.js:
  - MULTIPART_THRESHOLD=3GB (>3GB 走分片, 覆盖用户实测 4G 失败场景; COS 硬限 5GB), PART_SIZE=64MB, 并发 3
  - 关键修复: 大文件判断提前到 base64 读取之前 (!isImage 时先 probeFileSize), 且非图片不再走 base64 读取(白读内存); 否则 4G 视频在读 base64 时就崩了
  - probeFileSize(H5 blob.size / App getFileInfo), readBlobSlice(H5 Blob.slice / App readFile position+length)
  - uploadFile 增加 onProgress 参数 (dashboard 上传视频时 showLoading 显示百分比, 2% 步进)
- 验证: 全链路测试通过 (create → 2 分片 PUT → complete → fileID 返回); 测试文件已清理 (manageStorage action=delete force=true)
- 版本 v1.11.236 (commit a0440d3 + 5eaa02e) H5 已部署 + 云函数已部署 + 小程序已构建; 推送仍被 GitHub 拦截

## v1.11.237: 多课时并行上传, 每课时行内独立显示进度
- 需求：同时上传多个课时时, 在每个课时板块分别显示上传进度 (之前用全局 uni.showLoading 只显示最后一个)
- 改动 dashboard.vue (纯前端):
  - 每集对象增加响应式字段 `_uploading` / `_progress` (Vue3 嵌套对象自动响应式, 直接赋值即刷新)
  - uploadEpisodeVideo: 去掉全局 showLoading/hideLoading, 改为 ep._uploading=true + onProgress 更新 ep._progress; 上传中重复点击提示"该课时正在上传中" (并发保护); 成功 _progress=100, 失败复位
  - 模板: ep-row 视频行内, `v-if="ep._uploading"` 显示进度条(.ep-up-bar fill 宽度=%) + 百分比文字, `v-else` 显示上传按钮
  - CSS: .ep-up / .ep-up-bar / .ep-up-fill(品牌红 #c41e3a, transition .2s) / .ep-up-pct
- 版本 v1.11.237 (commit 6870cdb) H5 已部署 + 小程序已构建; 推送仍被 GitHub 拦截

## v1.11.238: 移除 CloudBase 短信扩展 + 视频上传进度条暂停/取消
- 需求：①去掉 CloudBase 短信扩展模块 ②视频上传进度条增加"取消""暂停"按钮并实现功能
- ① 移除短信扩展:
  - 云函数 index.js: 删 SMS_EXT_NAME/registerExtension/smsExtCall 及 sendVerifyCode 方案A分支、verifyCode 的 cloudbase 校验分支 → 仅保留腾讯云短信直连(TC3), 验证码走本地 verify_codes 集合
  - package.json 去掉 @cloudbase/extension-sms 依赖
  - 前端 dashboard.vue 短信配置: provider options 只留 ['tencent'], desc 改"腾讯云短信直连"
  - 验证: createMultipart 接口部署后正常
- ② 上传进度条暂停/取消:
  - **架构变更: 非图片(视频)上传统一走 COS 分片** (删除 3GB 阈值判断, 删 MULTIPART_THRESHOLD 常量), 好处: 所有视频都有真实进度 + 统一支持暂停/继续/取消; 图片仍走 canvas 压缩 base64 中转
  - cloudbase.js: uploadFile 加第4参 control={paused,cancelled}; 新增 waitIfPaused(control) (暂停循环等待300ms, 取消抛 {code:'UPLOAD_CANCELLED'}); uploadMultipartToCos 在 init/每片PUT前/每轮并发前/complete前检查
  - dashboard.vue: ep._control/_paused/_cancelled; togglePauseEpisodeUpload (暂停↔继续), cancelEpisodeUpload (置 cancelled=true, 循环中止→abortMultipart→catch code=UPLOAD_CANCELLED 提示"已取消上传"); 进度条旁加 [暂停/继续][取消] 按钮 (.ep-up-btn / .ep-up-btn.danger)
  - 单视频 uploadCourseVideo 未加按钮(未要求), 保持全局 loading 进度
- 版本 v1.11.238 (commit d769c36) H5 已部署 + 云函数已部署 + 小程序已构建; 推送仍被 GitHub 拦截

## v1.11.239: 短信配置字段标签去掉(tencent直连)字样
- 纯前端 dashboard.vue: sms 配置 fields 的 label 去掉 "(tencent直连)" 后缀 (SecretId/SecretKey/短信应用ID/短信签名/验证码模板ID/区域)
- 版本 v1.11.239 (commit 70b3491) H5 已部署 + 小程序已构建

## v1.11.240: 清理卡壳的分片上传任务 (storage.abortAllMultipart)
- 用户反馈上传任务卡壳 → 排查发现 COS 残留 4 个未完成 multipart upload (测试遗留: _probe_full/_probe_test/_verify_sms_removed)
- 云函数新增 `storage.abortAllMultipart`: cos.multipartList(分页, Prefix 可选) 列出所有未完成 upload → 逐个 multipartAbort → 返回清理列表; 路由注册
- ⚠️ SDK: multipartList 返回字段 **Upload** (非 Uploads), 分页字段 NextKeyMarker/NextUploadIdMarker/IsTruncated
- 已部署云函数并执行清理: aborted_count=4 (残留分片释放, 不占存储)
- 后续可用: 上传中断/卡壳后随时调用此接口回收; 前端页面卡住的上传刷新页面即可停掉
- 版本 v1.11.240 (commit 4859682) 云函数已部署 + H5 已部署 + 小程序已构建
