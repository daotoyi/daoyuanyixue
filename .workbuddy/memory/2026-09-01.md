# 2026-09-01 工作记录

## APK v1.11.278 构建 + 下载页换自定义域名 (去掉 CloudBase 测试域名警告)
- **触发**: 用户下载 APP 时看到腾讯云「测试域名仅供开发测试」警告页, 且链接指向
  `/apk/daoyuan-v1.11.221.apk`(旧版本)。用户已启用自定义域名, 要求去掉该提示
- **APK 构建** (v1.11.278, 4.1MB, 包内 version.js 校验 = v1.11.278 ✅):
  - ⚠️ **`cap` 不在根目录**: capacitor CLI 装在 `mobile/node_modules/.bin/cap`,
    必须 `cd mobile && ./node_modules/.bin/cap sync android`, 直接 `npx cap` 会报
    "could not determine executable to run"
  - 流程: 更新下载页版本 → 重建 H5 → cap sync → `./gradlew assembleRelease --no-daemon` → 复制产物
- **⭐ 重大发现: 自定义域名 club.zhenhesheng.cn 的路径映射不统一**
  | 访问路径 | 实际读取 | 证据 |
  |---|---|---|
  | `club.zhenhesheng.cn/apk/xxx.apk` | `h5//apk/xxx.apk` | 404 报错里 `Key: h5%2F%2Fapk%2F...` |
  | `club.zhenhesheng.cn/download/` | **根目录** `/download/` | h5/download/ 已更新为 278, 但该路径仍返回 221 |
  → 即 `/apk/` 走 **h5/ 前缀**, `/download/` 走 **根目录**, 两者不一致。
  **结论: 发布下载页与 APK 时, `/h5/apk`+`/h5/download` 和 `/apk`+`/download` 两处都要传**,
  只传一处会导致自定义域名下"页面是新的但下载 404"或反之
- **修复动作**:
  1. 下载页版本 1.11.221 → **1.11.278**; 链接域名
     `cloud1-...tcloudbaseapp.com` → **`https://club.zhenhesheng.cn`**(2 处)
  2. APK 与下载页复制到 `dist/build/h5/apk/`、`dist/build/h5/download/`
  3. 三次上传: `dist/build/h5 → /h5/`(84 文件)、`h5/download → /download/`、`h5/apk → /apk/`
     ⚠️ **严格限定 cloudPath 到 /download/ 和 /apk/, 绝不传根路径**(铁律: 根 = 官网)
- **验证结果**:
  | 项 | 结果 |
  |---|---|
  | `club.zhenhesheng.cn/download/` | 200, 指向 278 + 自定义域名 ✅ |
  | `club.zhenhesheng.cn/apk/daoyuan-v1.11.278.apk` | 200, APK 魔数 PK 正确 ✅ |
  | 测试域名同路径 | 200 ✅ |
  | 官网首页(根) | 仍是「真和盛 \| 道家文化传承」未被覆盖 ✅ |
  | H5 应用 /h5/ | v1.11.278 正常 ✅ |
- **未改动**: `src/utils/static-url.js` 的静态资源域名仍是测试域名 —— 那是图片请求,
  不会触发该 HTML 警告页; 且改了需验证 `club.zhenhesheng.cn/static/*` 是否落到 h5/, 暂不动
- **版本**: v1.11.278 (与 H5/小程序一致, APK 用当前已同步版本号构建, 未单独升版)

## v1.11.279 订单页删除按钮溢出修复 + 版本号错乱清理 (续 08-31 中断)
- **背景**: 上次升 279 中断, bump 在已存在错误 v1.11.279 tag(指向 278 提交)时又 bump 出 v1.11.280, 两 tag 同指 8b6ccdd 且只本地未推; 工作区被写错成 280
- **清理**: 删错误 tag v1.11.279/v1.11.280; 还原 version.js/build.gradle 到 278; 重 bump 278→279 (versionName 1.11.279, versionCode 374→375 正确递增)
- **订单修复**: list.vue .of-right 加 flex-wrap:wrap + min-width:0, 多按钮(确认收货/售后/删除)换行不溢出屏幕右侧; .of-time 限宽不压缩。commit f387700
- **tag 修正**: v1.11.279 用 -f 指向发布提交(非 bump 时旧 HEAD 8b6ccdd)
- **部署**: H5 构建并 upload /h5/ (82 文件 ✅); mp-weixin 构建成功 ✅; 修 inject-tcb-domains.js 的 manifest.json 路径(src/, Vite 版) 使其可运行, 生成 project.private.config.json 域名参考
- **待**: 小程序上传需用户在微信开发者工具手动完成; GitHub 推送
- **版本**: v1.11.279 (与 H5/小程序一致)

## v1.11.280 大视频上传优化 (抖音/视频号/小红书同款)
- **痛点**: 课程视频大文件上传慢 + 移动端偶发闪退。根因: ①每片一次云函数取预签名(数百片=数百次 RTT); ②固定 16MB 分片 + 固定并发 6 → 大文件数百并发连接压垮移动端
- **改动**:
  1. 云函数 `dy-api` 新增 `storage.batchPartUploadAuth` 路由(位于 storagePartUploadAuth 之后): 一次调用批量取回所有分片 PUT URL(每批 50, BATCH=50 上限因 getObjectUrl 并发), 消除每片 RTT。**必须重部署才生效**
  2. 前端 `src/api/cloudbase.js` `uploadMultipartToCos` 整体重写: 自适应分片 `pickPartSize`(>4GB→128MB, >1GB→64MB, >256MB→32MB, 否则16MB, 超 10000 片翻倍至 512MB 上限) + 自适应并发(初始4, 2~8, 测速>10MB/s 拉满8 / <3MB/s 降到2) + 批量签名 `batchSign`(调 storage.batchPartUploadAuth 一次取回全部分片URL到 partUrlMap) + wave 分批 Promise.allSettled 调度 + 每片回传 `{speedBps, etaSec, concurrency, partSize}` + 自适应超时(45s~按带宽) + 签名超 25min 自动刷新补齐轮 + 断点续传(listParts 为准) + 百度网盘式"永不放弃"补齐重试(最多8轮) + 硬超时兜底不变
  3. 后台 `dashboard.vue` 课程上传行展示 `速度 · 剩 时间`; 单视频上传 loading 显示速度; 新增 `formatSpeed/formatEta`; `resumePercent` 改用 `rec.partSize` 估算分片数
  4. 删除旧常量 MULTIPART_PART_SIZE/MULTIPART_CONCURRENCY/PUT_TIMEOUT_MS, node --check 通过, grep 无残留引用
- **版本**: bump 279→280, commit 8a487ae, tag v1.11.280 -f 指向发布提交; gen-version v1.11.280 cb76c3c
- **部署**: ① 云函数 dy-api updateFunctionCode 成功(accessUrl https://club.zhenhesheng.cn/dy-api, 批量签名路由已生效) ② H5 构建 upload /h5/ (82文件0失败) ③ mp-weixin 构建成功(需微信开发者工具手动上传) ④ GitHub push main(8a487ae) + tag v1.11.280 均成功
- **注意**: APK 按约定不随迭代构建, 下载页仍指向 278 APK; 本次未动下载页/APK

## v1.11.281 修复大视频"合并分片失败: One or more of the specified parts could not be found"
- **报错复现**: 书法精品课45讲 第一课时上传, 上传中断后合并报 COS `part not found`
- **根因(两个)**: ①COS 分片列表最终一致性 — v1.11.280 把并发拉到8且"传完立即合并", 刚传完的分片可能还没被 listParts 看到 → multipartComplete 报 part not found; ②"PUT 返回200但未真正落盘 / 上传中断留半片" 进了 donePartNumbers, 云端实际没有, 服务端 listParts 过滤后静默丢掉 → 报缺失或生成缺片损坏文件
- **修复**:
  1. 云函数 `storageCompleteMultipart`: 改为重试循环(最多5次, 800/1600/3200/6400ms 退避), 每次重试重新 `multipartListPart` 取最新 ETag+PartNumber 再 complete; 仅对 `part not found`/`no such upload` 做可重试判定; 重试耗尽后诊断缺失分片号并明确报错(便于客户端定向重传)
  2. 客户端 `uploadMultipartToCos` 合并前新增"校验补齐"阶段: 以服务端 `storage.listParts` 为权威, 遍历 1..totalParts 找出云端缺失分片 → 重新批量签名 + `uploadOne` 补齐(最多4轮); 全部存在后才调 completeMultipart。杜绝"以为传完实则缺失"导致的 part not found 与缺片损坏
- **版本**: bump 280→281, commit 12e8f88, tag v1.11.281 -f 指向发布提交; gen-version v1.11.281 12e8f88
- **部署**: 云函数 dy-api 重部署(合并重试生效) ✅; H5 构建 upload /h5/ (82文件0失败) ✅; mp-weixin 构建 ✅; GitHub push main(12e8f88)+tag v1.11.281 ✅
- **结论**: 该报错为 v1.11.280 高并发+即时合并引入的一致性竞态, 非逻辑永久缺陷; 用户在 H5 后台刷新后重试即可(断点续传只补缺失分片)

## v1.11.282 修复"上传过程中自动断"(根因: 分片过大→长连接被掐断)
- **用户反馈**: 281 仍报"上传中断，合并分片失败"。定位: dashboard catch(3177) 给任何失败冠"上传中断："前缀, 故"合并分片失败"是合并阶段 part not found; 其根因是有分片根本没落到 COS
- **真正根因(自动断)**: v1.11.280 把分片放大到 32~128MB → 每个 PUT 是 30s~2min 长连接, 在手机/WiFi/代理/NAT 空闲超时下极易被中间设备掐断; 长连接一断该片丢失 → 合并时云端缺片 → part not found
- **修复(抖音/视频号/小红书同款小分片)**: `pickPartSize` 反转为**默认 4MB 小分片**(长连接仅数秒, 几乎不被掐断; 单分片失败仅丢 4MB 秒级补齐); 仅当文件 >40GB 逼近 COS 10000 片上限才按需翻倍放大。同步更新顶部说明, 防后人改回"越大越好"
- **关键提示**: 281 已含合并前"校验补齐"块(listParts 为权威补齐缺失分片)+ 云函数 complete 重试兜底; 用户 281 仍报错高度疑似**H5 静态资源被浏览器缓存**(跑的是旧 280 包, 无校验块)。故 282 必须**硬刷新(Cmd/Ctrl+Shift+R)**绕过缓存才生效
- **版本**: bump 281→282, commit 271b70c, tag v1.11.282 -f 指向发布提交; gen-version v1.11.282 271b70c
- **部署**: 云函数本次无改动(281 重试版已在跑, 无需重部署); H5 构建 upload /h5/ (82文件0失败) ✅; mp-weixin 构建 ✅; GitHub push main(271b70c)+tag v1.11.282 ✅(ls-remote 已确认)
- **待用户**: 硬刷新 H5 后台 → 重选书法精品课视频上传; 微信小程序 `dist/build/mp-weixin` 版本号填 1.11.282 手动上传

## v1.11.283 系统设置-C/OSS存储界面新增视频删除功能(更换视频用)
- **需求**: 后台 系统设置-C/OSS存储 列表, 给每个视频加"删除"按钮, 删除本地存储 / C/OSS 存储的真实文件, 以便重新上传更换视频
- **数据模型(关键)**: 每节课时只有**一个 `ep.video` 字段**(本地=CloudBase 存储 URL 或 cloud:// fileID; C/OSS=对象存储 URL); `inOss` 由 `isVideoOnOss(ep.video, ossCfg)` 按 URL 判定(互斥, 非并存)。migrate 是直接覆盖该字段(本地 blob 不删, 变孤儿)
- **云函数 `dy-api` 新增**: `admin.oss.videos.delete` 路由(`adminOssVideoDelete`)。辅助函数: `httpsDelete`(HTTP DELETE)、`cosDeleteUrl`/`ossDeleteUrl`(腾讯云COS/阿里云OSS 预签名 DELETE URL, 复用 ossCfg 用户凭证)、`localFileIdFromUrl`(URL→cloud:// fileID)。逻辑:
  - `onOss`(C/OSS): 取 provider(空则按 host myqcloud.com/aliyuncs.com 推断)→ 从 URL 解析 key → httpsDelete(预签名DELETE)
  - 本地: `app.deleteFile({ fileList:[fileID] })`(status 0/-1 均视为成功, -1=文件已不存在)
  - 删完 `db.collection('courses').doc(_id).update({ episodes: newEps })` 清空该课时 `video:''`
  - **必须重部署才生效**(本次已重部署)
- **前端**:
  - `src/api/api.js` 新增 `adminVideoDelete = _admin('admin.oss.videos.delete')`
  - `dashboard.vue`: 操作列改为「搬运到 C/OSS」(仅本地且可管理) + 红色「删除」按钮(本地/C/OSS 均显示, 带确认弹窗 showModal, 显示存储位置); 新增 `deleteOssVideo(v)` 调 adminVideoDelete 后 `loadOssVideos()` 刷新; 新增 `.op.del` 样式(红底白字危险操作)
  - 删除后该课时 video 清空 → 列表自动不再显示(因 list 仅推 `ep.video` 非空项), 用户去课程上传处重传即可
- **版本**: bump 282→283, commit 093e234, tag v1.11.283 -f 指向发布提交; gen-version v1.11.283 093e234
- **部署**: 云函数 dy-api 重部署 ✅(删除路由生效); H5 构建 upload /h5/ (82文件0失败) ✅; mp-weixin 构建 ✅; GitHub push main(093e234)+tag v1.11.283 ✅(ls-remote 确认)
- **注意**: 删 C/OSS 视频不清理迁移遗留的本地孤儿 blob(无 DB 引用); 若需清理孤儿需另写脚本。APK 未构建, 下载页仍 278; 小程序版本号填 1.11.283 手动上传

## v1.11.284 搬运超时修复 + 服务商选择 + 列表筛选 + 删除同排
- **搬运到C/OSS报 request:fail timeout 根因**: `src/api/api.js` 的 `uni.request` 写死 `timeout:15000`(15s); 而 migrate 是云函数**下载整段视频再上传**, 大视频超 15s → 客户端超时。三层修复:
  1. 客户端: `_callFunction(action, data, opts)` 支持 `opts.timeout`; `_admin(action, opts)` 透传; `adminVideoMigrate` 用 `{ timeout: 120000 }`
  2. 云函数 `adminOssVideoMigrate` 改**流式搬运**: 新增 `httpsPipe(srcUrl, dstUrl)` (GET 源 → 直传 PUT 目标, 不整段缓冲, 规避大文件 OOM/超时); 优先流式, 源无 Content-Length 时退回原 `httpsGetBuffer`+`httpsPutBuffer` 缓冲模式。删除了原 migrate 里提前 `let buf = await httpsGetBuffer` 的整段下载
  3. 云函数执行超时: `manageFunctions action=updateFunctionConfig functionName=dy-api timeout=120`(默认超时太短会杀掉长搬运)。**注意: 函数代码改动必须 updateFunctionCode + 配置改 timeout 需单独 updateFunctionConfig, 二者独立**
- **C/OSS 设置服务商可选**: `settingsTabs` 的 oss 组 `provider` 字段改为 `type:'select', options:[{label:'腾讯云COS',value:'cos'},{label:'阿里云OSS',value:'oss'}]`; 设置表单 select 渲染器增强(`onSelectSetting`/`settingSelectLabel`)兼容 string[] 与 {label,value}[] 两种 options(不影响 sms 的 ['tencent'])
- **列表动态文案 + 筛选**:
  - 云函数 `adminOssVideosList` 响应新增 `oss_provider`(默认 'cos')
  - 前端 `ossProvider` ref(loadOssVideos 赋值) + `ossStorageLabel()`(oss→'OSS', 否则'COS'); 类型列/存储位置/搬运按钮/筛选文案随服务商显示 COS 或 OSS
  - 新增 `ossFilter`('all'|'local'|'remote') + `ossVideoFiltered` computed; 表头工具栏加分段筛选(全部/本地/对象存储名); 表格改遍历 `ossVideoFiltered`
- **删除按钮同排**: 操作列 `.ops` 改为 `display:flex; flex-wrap:nowrap`(搬运+删除同一行); `.oss-col-ops` 宽度 180→240rpx; `.op` 字号 22→20rpx; 删除红色 `.op.del` 在搬运按钮之后
- **版本**: bump 283→284, commit 505d60d, tag v1.11.284 -f 指向发布提交; gen-version v1.11.284 505d60d
- **部署**: 云函数 dy-api updateFunctionCode ✅ + updateFunctionConfig timeout=120 ✅; H5 构建 upload /h5/ (82文件0失败) ✅; mp-weixin 构建 ✅; GitHub push main(505d60d)+tag v1.11.284 ✅(ls-remote 确认)
- **待用户**: 硬刷新 H5 后台 → 系统设置-C/OSS存储 验证搬运(不再超时)/筛选/动态文案; 小程序 `dist/build/mp-weixin` 版本号填 1.11.284 手动上传。APK 未构建, 下载页仍 278

## v1.11.285 — C/OSS 搬运进度条
- **需求**: 系统设置-C/OSS存储 搬运时也体现进度条(用户之前搬大视频黑屏等、还超时, 要真实进度反馈)
- **根因/方案**: 云函数流式搬运(`httpsPipe`)在 SCF 内同步执行, 客户端拿不到内部进度 → 采用「云端写字节级进度 + 前端轮询」(非客户端直传, 避免跨域/客户端直传重构风险)
- **云函数改动**:
  - `httpsPipe(srcUrl, dstUrl, onProgress)` 新增 onProgress(loaded, total) 回调(GET 源 data 事件累加字节; pipe 边下边传, 单阶段进度=已下载=已上传)
  - `adminOssVideoMigrate` 内新增 taskId = data.taskId || `${course_id}_${ep}_${Date.now()}`; 节流(800ms) `reportProgress(percent, phase)` 写进度
  - 进度存储: 新增 `upsertMigrateProgress(rec)` — `db.collection('oss_migrate_progress').where({taskId}).get()` 存在则 update 否则 add(**腾讯云 TCB 写入不存在集合会自动创建**, 不依赖 doc id / createCollection API); 集合名 `oss_migrate_progress`
  - 新增 `adminOssVideoMigrateProgress(data)` 路由(`admin.oss.videos.migrate.progress`): 按 taskId where 读进度返回 {phase, percent}; 找不到返回 {phase:'unknown', percent:0}
  - 完成 `reportProgress(100,'done')`(done 不受 99 上限约束); 返回 ok 带 taskId
- **前端改动**:
  - api.js 新增 `adminVideoMigrateProgress = _admin('admin.oss.videos.migrate.progress', {timeout:15000})`
  - dashboard `migrateOssVideo(v)` 改写: 生成 taskId → 行内标记 `item._migrating/_migratePercent`(ossVideoList 为 ref 深层响应) → setInterval(poll,1000) 调 progress 更新百分比 → await migrate(带 taskId, 120s 超时) → 停轮询/刷新列表; 失败清标记
  - 列表行模板: 搬运中 `v._migrating` 显示 `.migrate-progress` 进度条(蓝渐变 fill + 百分比), 否则显示原搬运/删除按钮
  - 样式: `.migrate-progress/.mp-bar/.mp-fill/.mp-pct`(列宽 240rpx 内占满)
- **版本**: bump 284→285, commit 0269cf5, tag v1.11.285 -f 指向发布提交; gen-version v1.11.285
- **部署**: 云函数 dy-api updateFunctionCode ✅(进度路由生效; 120s 超时配置上轮已设保留); H5 upload /h5/ (82文件0失败) ✅; mp-weixin 构建 ✅; GitHub push main(0269cf5)+tag v1.11.285 ✅(ls-remote 确认)
- **待用户**: 硬刷新 H5 后台 → 系统设置-C/OSS存储 搬运视频看行内进度条; 小程序 `dist/build/mp-weixin` 版本号填 1.11.285 手动上传。APK 未构建, 下载页仍 278
- **注意**: 进度依赖 SCF 多实例并发(原 migrate 请求占用实例A时, 前端轮询 progress 落新实例B读 DB); 若单实例排队最坏只看到 0%→100% 跳变(不卡死, 因 migrate 120s 上限)。DB 最终一致, 轮询 1s 足够

## v1.11.286 — 上传带宽被锁修复(实测58Mbps仅跑928KB/s)
- **用户报**: 网络实测上行 58Mbps, 但视频上传速率仅 928KB/s(≈7.4Mbps), 差约 7.8 倍
- **根因诊断**: 单 TCP 连接被限速(~1MB/s, 代理/VPN/COS 单链接限速常见) + 浏览器对 `*.myqcloud.com` 走 HTTP/2 多路复用(所有分片 stream 挤在同一条 TCP) + 旧"慢就降并发"逻辑把并发压到 2 → 总速率≈单连接 928KB/s。speedtest 多线程测出 58Mbps 掩盖了单连接瓶颈
- **修复(已发布 v1.11.286 / a65891b)**:
  1. 前端 `src/api/cloudbase.js` 反转并发自适应: MIN 2→6, INIT 4→8, MAX 8→16; 旧逻辑"speedBps<3MB/s 降并发"改为"仅当本波分片失败/超时才降并发, 否则缓慢拉满"——慢网不降并发(单连接被限恰恰需更多连接聚合带宽)
  2. 云函数 `dy-api` 批量预签名 `storageBatchPartUploadAuth` 新增多上传域名轮询: 新增常量 `COS_UPLOAD_HOSTS=[]`(默认空=单域名零风险); 按 partNumber 取模分配不同 host → 前端 PUT 形成多条独立 TCP 连接, 突破单连接硬限。**需开启 COS 全球加速后把加速域名填入数组才生效**(签名 query 不含 host, 换同桶 host 通用; 但需目标域名已开+CORS 覆盖)
- **部署**: 云函数 dy-api updateFunctionCode ✅(多域名框架); H5 upload /h5/ (82文件0失败) ✅; mp-weixin 构建 ✅; GitHub push main(a65891b)+tag v1.11.286 ✅(ls-remote 确认)
- **待用户(关键)**: 硬刷新 H5 后台 → 重新传视频, h1/小程序环境并发 6 连接应达 ~6×0.9MB/s≈40Mbps(6倍提升); 若要逼近 58Mbps 满速, 需去腾讯云 COS 控制台开启该桶「全球加速」并把加速域名 `636c-cloud1-d8gs2k9m311f7272f-1464523137.cos.accelerate.myqcloud.com` 填入 `COS_UPLOAD_HOSTS`(我可代填+重部署), 分片轮询两域名=两条独立 TCP 聚合带宽。APK 未构建, 下载页仍 278

## v1.11.287 — 搬运报 request:fail 修复 + 上传进度条布局右对齐
- **问题1 用户报**: 1.11.286 搬运到COS有进度条了, 但一会仍报「网络请求失败，request:fail」
- **根因诊断**: `migrate` 是云端长连接阻塞请求(响应要等整段传完才返回); 搬运期间云端只把进度写 DB(轮询可见), HTTP 连接处于空闲 → **网关空闲超时先掐断连接**(报 request:fail), 但 SCF 实例仍在后台继续跑(直到函数超时)。客户端只 await 该请求 → 误判失败。原函数 120s 超时也偏短
- **修复(已发布 v1.11.287 / 84933e4)**:
  1. 前端 `migrateOssVideo` 改写韧性逻辑: fire migrate 请求(超时 900000)后, 连接断开(catch)不立即失败 → 改 `while` 轮询 `migrate.progress` 直到 `phase==='done'||percent>=100`(云端真搬完) 才 success; 进度冻结 >30s 才判真失败; 等待上限 880s(略小于函数 900s)。彻底免疫网关空闲掐断
  2. 云函数 `updateFunctionConfig timeout=900`(原 120)兜底 SCF 实例最长跑 900s; 代码无需改(流式搬运+DB进度已具备)
  3. api.js `adminVideoMigrate` 超时 120000→900000
- **问题2**: 上传视频出现「网络波动，自动重试中」时, 进度条+暂停/取消按钮朝左偏移
  - **根因**: `.ep-up` 默认 `justify-content:flex-start`, 状态文字(width:100%)占满整行后把进度条/按钮挤到左边
  - **修复**: `.ep-up` 加 `justify-content:flex-end` → 进度条+按钮永远贴右(状态文字在其上方独立成行)
- **部署**: 云函数 dy-api updateFunctionConfig timeout=900 ✅(代码未改无需 redeploy, 但 MCP 通道本就只改 config); H5 upload /h5/ (82文件0失败) ✅; mp-weixin 构建 ✅; GitHub push main(84933e4)+tag v1.11.287 ✅(ls-remote 确认)
- **待用户**: 硬刷新 H5 后台 → 搬运大视频(即使中途连接报断也继续轮询, 真搬完才提示成功); 上传进度条+按钮现贴右。小程序 `dist/build/mp-weixin` 版本号填 1.11.287 手动上传。APK 未构建, 下载页仍 278
- **注意**: 若单视频 >900s 仍搬不完(极巨大), 需改 chunked 分片搬运(v1.11.287 未做); 目前 900s 普通课程视频足够

## v1.11.288 — 上传"自己退出"修复(单连接限速下误超时→自我放弃)
- **用户报**: ①视频有分片吗? ②上传速率还是 778KB/s ③上传一会怎么还会自己退出?
- **确认**: 分片一直在用(4MB 小分片 × N 片 + 批量预签名直传 COS, 非整文件传)。778KB/s 根因 = `COS_UPLOAD_HOSTS=[]` 空 → 所有并发分片打同一 COS 域名, 浏览器 HTTP/2 把它们合并成**一条 TCP 连接**, 该连接被限速 ~778KB/s(单连接限速 / 高 RTT BDP 限制)
- **"自己退出"根因**: `uploadMultipartToCos` 单片超时写死 45s; 但 16 片共用一条 ~778KB/s 管子, 每 4MB 片实际需 ~85s > 45s → 分片超时失败 → 补齐轮 → 连失败 8 轮抛「多次失败」→ 前端 catch 弹「上传中断」并切下一个(即用户看到的"自己退出")
- **修复(纯前端, 不花钱, 已发布 v1.11.288)**:
  1. `src/api/cloudbase.js` `uploadOne` 超时改为**自适应**: `perConnBps = max(64KB/s, speedBps/并发)`; `partTimeout = min(600s, max(60s, PART_SIZE/perConnBps*2.5 + 15s))` → "慢但仍在传"的分片永不被误杀, 真挂起仍有硬超时兜底
  2. 补齐重试轮上限 8→16("永不放弃"真正落实; 慢网靠自适应超时撑住后正常不到此轮)
- **速度真正解法(待用户控制台操作)**: COS 桶开启「全球加速」后, 把加速域名 `636c-cloud1-d8gs2k9m311f7272f-1464523137.cos.accelerate.myqcloud.com` 填入 `COS_UPLOAD_HOSTS`(云函数常量)并重部署 → 分片轮询两域名=两条独立 TCP 聚合带宽, 且加速域名走腾讯骨干降低 RTT, 有望逼近 58Mbps 满速
- **部署**: H5 upload /h5/ (82文件0失败) ✅; mp-weixin 构建 ✅; GitHub push + tag 待执行

## v1.11.289 — 上传仍报"上传中断停止"根因修复 + 状态文字布局贴右
- **用户报**: ①"网络波动，自动重试中……"这行字要放在暂停/取消按钮下面、也贴右 ②上传过程中仍报"上传中断"最终停止上传
- **根因(仍单连接)**: v1.11.288 只放宽单片超时(→221s), 但单连接下每片实际要 ~85s, 网络/代理真实超时(30~120s)会先掐断连接 → 分片真失败 → 重试耗尽 → 停止。并发被旧逻辑拉到 16 → 每片耗时随并发飙升是元凶
- **修复(纯前端, 已发布 v1.11.289)**:
  1. `uploadMultipartToCos` 并发自适应改为"按实测聚合带宽 / 单分片安全时长(20s)反推安全并发": 单连接限速时自动降到≈3(每片 ~15s << 网络超时, 单连接总带宽不变=不丢速), 开全球加速/多域名后聚合带宽飙升→安全并发自动放开到 MAX=16 多连接聚合提速。保守兜底 per-conn 128KB/s→64KB/s 覆盖更慢网
  2. 前端 `.ep-up` 改 flex column + align-items flex-end; 进度条/百分比/速度/暂停/取消 包进 `.ep-up-row`(一行贴右); `.ep-up-status`(网络波动提示) 移到下方、text-align:right 贴右; `.ep-up.queued` 保持 row
- **部署**: H5 upload /h5/ (82文件0失败) ✅; mp-weixin 构建 ✅; GitHub push + tag 待执行
- **待用户**: 硬刷新 H5 → 重传视频(每片≤20s, 不再被掐断/自我退出); 小程序版本号填 1.11.289 手动上传。APK 未构建, 下载页仍 278。满速仍需开 COS 全球加速(见上)
- **待用户**: 硬刷新 H5 后台 → 重传视频(不再自我退出, 但单连接仍限速 778KB/s 属预期, 要满速需开全球加速); 小程序版本号填 1.11.288 手动上传。APK 未构建, 下载页仍 278

## 桶名澄清(用户 17:50 反馈)
- **关键易混点**: 用户在腾讯云 COS 控制台"存储桶列表"只看到 `zhs-1464523137`(ap-beijing), 误以为这是视频上传桶。实际**视频上传直传桶 = `636c-cloud1-d8gs2k9m311f7272f-1464523137`(ap-shanghai, 云开发环境存储桶, 代码第3147行 COS_BUCKET)**; `zhs-1464523137` 是「系统设置→C/OSS存储」里用户自建的**搬运目标桶**, 两者不同(appid 都是 1464523137 同主账号)
- **为什么控制台看不到上海 636c 桶**: 它是云开发(TCB)环境存储桶, 标准 COS 控制台默认不显眼列出; 正确入口=云开发控制台 → 环境 cloud1-d8gs2k9m311f7272f → 存储, 或 COS 控制台搜 `636c-cloud1`(不选区域筛选)
- **开全球加速必须开在 636c 上海桶**(不是 zhs 北京桶, 开 zhs 对上传速度无帮助); 开完把加速域名填 `COS_UPLOAD_HOSTS` 重部署云函数

## 桶计费/存储路径澄清(用户 18:05 问)
- **两种桶收费"标准"一致**: 底层都是腾讯云 COS, 同地域同存储类型单价同一套; 差异只在账单科目(云开发桶挂 CloudBase 环境账单, 自建桶挂 COS 账单)与附加项(云开发桶默认 tcb.qcloud.la CDN 域名). 增值功能费(全球加速/清单/检索)两桶同标准
- **所有上传(视频分片直传/图片/文件)均存云开发上海桶** `636c-cloud1-d8gs2k9m311f7272f-1464523137`, fileID 全为 `cloud://` 体系(代码 `storage.uploadFile` 单一入口)
- **C/OSS 搬运 = 复制到自建北京桶 `zhs-1464523137`, 默认不删源**(`adminOssVideoMigrate` 仅 GET→PUT, 无 deleteFile); 故搬运后不删源=双份存储费(云开发桶源+自建桶副本); 要在 C/OSS 界面手动「删除」才删云开发桶源→只付自建桶. 想省云开发桶费必须搬完手动删源

## 全球加速开不了(用户 18:35 反馈)
- **更正 172 行**: 云开发环境桶 `636c-cloud1-d8gs2k9m311f7272f-1464523137` 在 COS 控制台/云开发控制台**无「域名与传输管理>全球加速」入口**; 非试用版/套餐限制, 而是**产品架构未集成**(WebSearch 云开发套餐功能对照表确认: 个人/标准/企业/企业高级版云存储功能均无"全球加速"项, 仅 CDN 下载加速/自定义缓存/万象/图片处理; 付费升级云开发套餐也不会出现该开关). 开云开发桶全球加速此路不通
- **真满速唯一路**: 改视频上传目标桶 → 自建 `zhs-1464523137`(ap-beijing, 标准桶可开加速), 在 zhs 桶开全球加速, 云函数改用 zhs 桶永久密钥(SecretId/SecretKey)签名直传, 上传走 `zhs-1464523137.cos.accelerate.myqcloud.com` 逼近满速. 需用户授权密钥(存云函数环境变量, 不落明文)
- **备选**: 维持 v1.11.289 单连接 778KB/s 稳定传完(零成本零改动)
- **用户决定(18:35)**: 选「维持现状稳定慢传」, 不开加速、不切桶、零改动. 后续若想提速再走切 zhs 桶方案

## H5 部署路径 bug 修复(18:58 用户报"强刷还是 1.11.287")
- **根因**: `scripts/deploy-cloudbase.js` h5 的 `cloudPath=/`(且 MEMORY.md 铁律也错写成 /), 导致 v1.11.288/289 全传到**根 /** 覆盖官网首页; 而用户访问的 **/h5/** 路径自 v1.11.287 起从未被更新 → 强刷仍见 287
- **验证**: 本地 version.js=1.11.289(git tag 同); 线上 `club.zhenhesheng.cn/h5/assets/version.Dzqpu-aN.js` 含 1.11.289, /h5/ 首页 last-modified=11:01:50(部署时刻) → 确认 /h5/ 现=289
- **修复**: 改脚本 `cloudPath=/h5/`(已修正) + 重新部署 289 到 /h5/(failedFiles:[]); 同步修正 MEMORY.md 第19行与静态托管铁律(云Path 必须 /h5/)
- **副作用已修复(19:37)**: 官网首页恢复完成
  - 跨项目操作(按铁律先声明): 只读 `zhs-website/zhenhesheng.cn/` 静态产物 → rsync 复制到 /tmp/zhs-official-restore 并**排除 download/ 目录**(防覆盖 APK 下载页) → mcporter upload cloudPath=/
  - **绝不碰 zhs-website 源码**(仅读取产物), 部署 35 文件 failedFiles:[] ✅
  - **源站已验证恢复**: 测试域名 `cloud1-d8gs2k9m311f7272f-1464523137.tcloudbaseapp.com/` 首页标题「真和盛 | 道家文化传承」, 含官网导航(about/classics/origin/pandao/practice/shop.html), 无 H5 特征; /h5/(289)/download/(APK页)/apk/ 均完好
  - **待用户刷新 CDN**: `club.zhenhesheng.cn` 自定义域名 CDN 仍缓存旧 H5 首页 → 腾讯云 CDN 控制台「缓存刷新」URL `https://club.zhenhesheng.cn/`(或云开发控制台静态托管自定义域名刷新), 1 分钟生效; 用户硬刷新浏览器即可见官网
  - 清理: 根 `/assets/index-*.js` 等为 H5 死文件(无害), 暂留
  - **平台限制(铁律补充)**: `*.tcloudbaseapp.com` 测试域名的「测试域名警告页」是 CloudBase 平台层强制注入, 非托管文件, **无法删除/覆盖去除**; 消除办法只有改用自定义域名 `club.zhenhesheng.cn`(无警告). mcporter cloudbase 的 manageHosting **无 CDN 缓存刷新(purge/refresh)工具**, 刷新 CDN 须用户在腾讯云 CDN 控制台或云开发静态托管自定义域名处手动操作
  - **v1.11.290 (C/OSS 存储三问题修复)**: ①问题1+3根因=上传完成仅把 ep.video 写本地 courseForm 对象未立即回数据库, 1.2s 后 maybeMigrateToOss 触发时云函数查 db 的 course.episodes[i].video 为空→报"课时不存在或无视频"; migrateOssVideo 的 catch 把云函数明确失败只 console.warn 后傻等30s轮询→用户感知"退出/卡死". 修复: 前端上传完成(maybeMigrateToOss 传 ep.video)与列表点击(migrateOssVideo 传 v.video)均把 video 传给云函数; 云函数 adminOssVideoMigrate 优先用 data.video 作 srcUrl(放宽 `!ep||!ep.video` 检查为基于 srcUrl); migrateOssVideo catch 区分"明确业务失败(直接 toast 停止)"vs"网关超时断开(轮询等待 done)". ②问题2: C/OSS 列表"本地"→"云开发COS"(类型列/筛选/统计/描述四处理), 删除按钮文案"本地云存储"→"云开发COS"(删云开发COS 功能本就支持: 非 inOss 走 app.deleteFile 删 cloud://). ③部署: 云函数 dy-api 已 updateFunctionCode(success)+ H5 /h5/(failedFiles:[]) 均完成; 线上 /h5/assets/version.BstTkkAA.js=1.11.290 验证✅
  - **遗留隐患**: 上传视频后若选"暂存本地"(不点"存储到C/OSS"), video 仅写本地 courseForm 未回库, 需用户之后点"保存课程"才持久化, 否则刷新丢失. 本次聚焦搬运修复未动上传持久化(避免引入新bug); 已提示用户选暂存本地后需保存课程
- **v1.11.291 (搬运"失败或超时"误导 + 云开发COS 显示)**: ①问题1根因=云函数 adminOssVideoMigrate 内部出错只 `return fail(...)`, 但网关先掐断连接(request:fail)时客户端永远收不到真实错误→轮询到 880s 笼统报"搬运失败或超时". 修复: 把下载→上传→更新课时整体 try/catch, 任何失败 `reportProgress(0,'error',msg)` 写 `oss_migrate_progress.phase=error` 落库 + `return fail(msg)`; adminOssVideoMigrateProgress 返回 `error` 字段; 前端 migrateOssVideo 轮询检测 `phase==='error'`→直接 toast 真实错误. ②卡死判定改用 DB `updatedAt`(>45s 未更新才判真卡死), 替代原 percent 相等法(慢速传输会被误杀). ③客户端 adminVideoMigrate 超时 900000→60000, 长搬运更快切轮询、进度条更早出现(云端仍后台跑). ④CSS: .oss-col-type width 100rpx→160rpx + white-space:nowrap + font-size 22rpx, 使"云开发COS"单行显示不换行. ⑤部署: 云函数 updateFunctionCode + updateFunctionConfig timeout=900(重试一次成功) + H5 /h5/(failedFiles:[]); 线上 /h5/assets/version.CXLsFHWa.js=1.11.291 ✅; GitHub main+tag 已推送. 小程序版本号 1.11.291; APK 未构建
- **v1.11.292 (C/OSS 视频存储管理-筛选改下拉菜单)**: 把原按钮组筛选(全部/云开发COS/腾讯COS)改为两个自定义下拉: ①课程/课时下拉 `oss-select`(选项=从 ossVideoList 去重 course_id→course_title, 默认"全部课程", 按 course_id 过滤列表); ②存储位置下拉 `oss-select`(全部位置/云开发COS/腾讯COS, 对象存储目标随服务商显示 腾讯COS 或 阿里云OSS). 下拉用 `.oss-select`(position:relative)+`.oss-select-panel`(absolute,z-index:60)+`.oss-dropdown-mask`(fixed 遮罩, 点击关闭). 新增 ref: ossStorageFilter/ossCourseFilter/ossCourseOpen/ossStorageOpen; computed: ossCourseOptions/ossCourseLabel/storageTargetLabel/ossStorageLabel2; 函数 selectOssCourse/selectOssStorage. 旧 ossFilter/of-btn 已移除. 仅前端改动(云函数无需动); H5 /h5/(failedFiles:[]) + 线上 version.DEuL_217.js=1.11.292 ✅; GitHub main+tag 已推送. 小程序版本号 1.11.292; APK 未构建

## v1.11.293 — 上传进度条下方实时显示网速/剩余时间 + C/OSS 表头内嵌下拉
- **需求1**: 上传视频课程时, 进度条下方实时显示网速与剩余时间
  - 原 `ep-up-speed` 在进度条同行右侧且 `v-if="ep._speed"` 才显示(速度为0不显示)
  - 改为独立行 `.ep-up-meta`(进度条正下方), 且"总是"显示: `ep._speed>0` 时显示 `formatSpeed · 剩 formatEta`, 否则显示「计算中…」, 让用户全程看到在传
- **需求2**: 系统设置-C/OSS存储, 两个下拉(全部课程/全部位置)从工具栏移入"课程/课时""存储"表头单元格旁
  - **关键坑**: `.table` 有 `overflow-x:auto` → 其内 `position:absolute` 下拉面板会被纵向裁剪; 故面板改 `position:fixed` + `uni.createSelectorQuery().select('#id').boundingClientRect` 取坐标定位(近底部上弹), 配 `.oss-th-panel-mask`(fixed 遮罩)点击关闭
  - 表头单元格加 `.oss-th-filter`(row 布局 label+下拉并排, specificity 压过 `.oss-col-title` 的 column); `.oss-col-type` 宽度 160→200rpx 容纳"存储 ▾全部位置"
  - 新增 ref `coursePanelStyle/storagePanelStyle` + 函数 `toggleOssCourseFilter/toggleOssStorageFilter/placeOssFilterPanel/closeOssFilters`; 旧 `selectOssCourse/selectOssStorage` 保留
  - 仅前端改动(云函数无需动)
- **版本**: bump 292→293, commit 1dba16a, tag v1.11.293 -f 指向发布提交; gen-version v1.11.293
- **部署**: H5 构建 upload /h5/ (82文件0失败) ✅; 线上源站 `https://cloud1-...tcloudbaseapp.com/h5/assets/version.B_mHKZmP.js` 含 1.11.293 已 curl 校验 ✅; mp-weixin 构建 ✅
- **待用户**: 硬刷新 H5 后台 → C/OSS存储 点表头"课程/课时"/"存储"旁下拉筛选; 上传视频看进度条下方网速/剩余. 小程序版本号 1.11.293 手动上传; APK 未构建
- **GitHub push 待重试**: 本地 commit+tag 已完成, 但 `git push origin main` + `git push origin v1.11.293 -f` 因当前网络无法连 github.com:443(HTTP2 framing/Couldn't connect/Empty reply)连续失败; 网络恢复后重跑 `git push origin main && git push origin v1.11.293 -f`

## v1.11.294 — C/OSS 表头筛选只留倒三角按钮 (22:43)
- **触发**: 用户要求删掉"全部课程/全部位置"文字标签, 下拉触发只保留"课程/课时"和"存储"表头旁的倒三角(▾)按钮
- **改 dashboard.vue**: 表头 select 由"label+caret"改为仅 `.oss-caret-btn`(圆形 40rpx, ▾ 居中); 选中非 all 时加 `.on`(红底白三角)提示已筛选; 保留 `id="ossCourseSel/ossStorageSel"`(placeOssFilterPanel 用其定位 fixed 面板); 删除无用 CSS `.oss-select-panel` / `.oss-select-label` / `.oss-select-caret`
- **版本**: bump 293→294, commit 36ef40b, tag v1.11.294 -f 指向发布提交; gen-version v1.11.294
- **部署**: H5 构建 upload /h5/ (82文件0失败) ✅; 线上源站 `version.CnA7Fi-X.js` 含 1.11.294 已 curl 校验 ✅
- **GitHub push 仍失败**: `git push origin main` + `git push origin v1.11.294 -f` 仍 Empty reply (github 不可达); 网络恢复后重跑 `git push origin main && git push origin v1.11.294 -f`

## v1.11.295 — C/OSS 搬运弹窗标明文件名 (22:59)
- **触发**: 用户要求视频上传完成弹"保存到本地 / 搬运到 C/OSS"框时, 弹窗标明是哪个文件名
- **改 dashboard.vue**: 新增 `ossFileNameOf(videoUrl)`(URL basename 取文件名); `maybeMigrateToOss(v)` 弹窗 content 追加 `课程：<课程·课时>` + `文件：<文件名>`; 两处调用(单视频 line2827 / 多集 line3183)补传 `course_title`/`episode_title`/`video`
- **版本**: bump 294→295, commit 9c05d89, tag v1.11.295 -f 指向发布提交; gen-version v1.11.295
- **部署**: H5 构建 upload /h5/ (82文件0失败) ✅; 线上源站 `version.BYwf30C2.js` 含 1.11.295 已 curl 校验 ✅
- **GitHub push 成功**: `git push origin main` + `git push origin v1.11.295 -f` 本次网络恢复已推送 (7f3ae11..9c05d89, 新 tag v1.11.295)
