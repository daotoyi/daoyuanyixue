# 2026-08-18 工作日志

## v1.11.153: 订单分析SVG饼图+表格修复+APP动态分享

### 1. 订单分析饼图修复 + 金额环形图
- **问题**: conic-gradient 在 APP WebView 中不渲染 → 每种类型颜色相同
- **方案**: 改用 SVG `<circle>` + `stroke-dasharray`/`stroke-dashoffset` 绘制环形图
  - PIE_C = 2π × 40 ≈ 251.33, 每段 stroke-dasharray = `${seg} ${PIE_C - seg}`, offset = `-accC`
  - `transform="rotate(-90 50 50)"` 让起点在 12 点钟方向
- **新增**: 按金额统计的环形分布图（与单数图并排，图例显示百分比）
- `pieSegments` computed 同时计算 count/amount 两种分段
- `pct()` 辅助函数格式化百分比

### 2. 订单管理表格横向滚动 + 表头防重合
- `.tr` min-width: 1080rpx → 1200rpx
- `.w-no` width: 260rpx → 320rpx, 去掉订单号 ellipsis（完整显示可横向滚动查看）
- `.w-name` min-width: 0 → 200rpx（防止 flex 收缩导致表头文字重合）
- `.w-price` width: 120rpx → 140rpx
- 所有 `.td` 增加 `overflow: hidden; flex-shrink: 0`（防止文字溢出到相邻列）
- `.w-status` `.w-ops` 增加 `overflow: hidden`

### 3. APP 动态分享直接打开微信
- `shareMoment()` 检测 APP 环境: `window.Capacitor || window.plus`
- APP 端优先 `navigator.share()`（系统分享面板含微信）
- 降级: 复制链接 + `weixin://` scheme 直接打开微信
- H5 端保持原有复制链接逻辑
- 小程序端不变（`open-type="share"` 原生分享）

### 部署
- H5 + 小程序构建成功, APK 打包成功 (4.2M)
- H5 静态托管 + APK + 下载页已上传
- Git 提交推送 221cf7f → GitHub
- 注意: MCP 认证会过期, 需设备码重新授权

## v1.11.154: 后台越权漏洞修复 + 订单分析环形图单色修复

### 问题1: 齐静春(ZHS00001)账户有管理员权限 (严重安全漏洞)
- **根因**: 云函数 `requireAdmin` / `requireStaffAllowed` 完全信任客户端传的 `opRole`/`role` 字段:
  `if (role === 'admin' || role === 'manager') return true` → 任何用户带 `opRole:"admin"` 即可越权访问后台
  (实测 uid=4 role=user 的齐静春伪造 opRole 拿到全量后台数据/用户列表)
- **DB 实况**: 齐静春数据库角色确实是 `user`(uid=4, 13800138001/123456 测试账号), 非管理员
- **修复**: 新增 `dbUserRole(data)` helper (按 opUid/uid/user_id 查数据库真实角色), 全部权限判断以 DB 为准:
  1. requireAdmin / requireStaffAllowed → 一律查 DB
  2. publishMoment 发布权限 → 查 DB
  3. register 公开注册 → 强制 role='user' (原来接受 data.role 可注册即管理员)
  4. adminUserUpdate / adminUserDelete 的 opRole 兜底 → 删除, 只查 DB
- **验证**: 齐静春伪造 opRole=admin → 403; 超管 uid=1 → 200; 云函数热实例缓存约 30-60s 后生效

### 问题2: 订单分析环形图只有商品一种颜色
- **根因**: 后端 adminOrderAnalysis 的 typeMap 固定返回全部3类(product/course/tool_unlock),
  课程/AI解盘 count=amount=0 时前端 legend 仍显示3种颜色, 但环形图 seg=0 不可见 → 只剩商品色
- **修复**: 后端过滤 `count>0 || amount>0` 才返回 → pieData 只含实际成交类型, 图例与环形图一致
- **验证**: 线上 pieData 现只返回 `[{key:product, count:6, amount:1268.06}]`

## v1.11.155: 史文华充值1万元宝 + 解盘统一元宝支付 + 盘面保存附带AI内容

### 1. 给史文华充值 10000 元宝
- 史文华 uid=120395 (ZHSM555, role=user), 原 balance=0.00
- 走线上 `admin.users.update` 接口 (opUid=1 超管) 改 balance="10000", 自动累计 total_recharge=1000 (元, 1元=10元宝, VIP等级用)
- 经验: 后台充值走接口比 MCP 直写库安全 (自动维护 total_recharge, 无需手工拼全字段)

### 2. AI 解盘 9 元宝解锁: 三端统一默认元宝支付 + 不足提示充值
- 原逻辑: H5/APP 元宝扣款, 小程序微信支付 (双分支 #ifdef MP-WEIXIN)
- 新逻辑: 统一 `payWithBalance({title, tool, applyFn})` helper, 直接调 unlockTool(pay_method:'balance')
  - 成功: 同步 userStore.balance (接口返回新余额) + applyXxxUnlock()
  - 失败 msg 含"元宝不足" → showModal 提示 → 跳 /pages-sub/user/assets 充值页
- 涉及: paipan.vue (bazi/qimen/ziwei 3个) + tools.vue (liuyao/liuren 2个), 共5个支付函数
- 云函数 aiAsk 成功返回增加 balance 字段 (前端同步余额); 3个问答函数(askAI/askLyAI/askLrAI)同样加"元宝不足→充值"弹窗
- 清理: paipan.vue/tools.vue 不再用 wxpayPrepay/wxRequestPayment (import 已移除)
- 验证: 线上 unlockTool/aiAsk 余额不足均返回 400 "元宝不足..." → 前端检测弹窗充值

### 3. 保存盘面附带 AI 解盘/问答内容
- paipan.vue: saveDisk 时 collectAiSnapshot() 收集 jpData/qmData/zwData 非空模块 + aiAnswer/aiQuestion 存入 rec.aiJp/aiQm/aiZw/aiQa
- tools.vue: saveLiuyao/saveLiuren 附加 aiJp/aiLr/aiQa, saveHistory 增加 extra 参数
- 恢复: useHistory → restoreAiSnapshot() / useToolHistory → restoreToolAi(), 还原内容 + 标记 AiDone + markPaidKeys 补记解锁状态 (避免恢复历史后重复收费)
- 跨页: paipan.vue 历史跳 tools 页时经 liuyao_restore_ai/liuren_restore_ai 传递 AI 内容
- 注意: zwData 初始结构含 sizheng key, 恢复 base 需保持一致

### 部署
- 云函数已部署 (aiAsk 返回 balance)
- H5 + 小程序构建, H5 静态托管待部署
- Git 提交 v1.11.155

## v1.11.156: 管理后台入口角色判断修复

- **问题**: "我的"页(user.vue)管理后台入口 `isAdmin` 写成了 `admin || staff` → 员工(staff)看到入口但登录页会拒绝("员工账号无后台登录权限"), 管理员(manager)反而看不到入口
- **修复**: 改为 `admin || manager` (与 index.vue/后台登录页/云函数权限一致; staff 员工和普通 user 均不显示入口)
- 纯前端改动, 无需部署云函数
- H5 + 小程序构建完成, H5 已部署静态托管, Git 提交推送 ff52c06

## v1.11.157: 头像跨端显示修复 (App 显示/H5 不显示 + 小程序后台用户管理不显示)

### 根因 (两条叠加)
1. **数据库脏数据**: uid=2 道元易学(ZHSM888) avatar 存了 `blob:https://localhost/...`
   (H5 uni.chooseImage 返回的临时对象地址, 仅当前页面会话有效, 跨端/刷新即失效)
2. **代码缺陷 (user.vue pickAvatar)**:
   - H5/App 分支: `String(fileID).replace(/^cloud:\/\/[^/]+\//,'')` 把 cloud:// fileID 去掉前缀
     存成 `avatars/u2_xxx.png` 相对路径 → 私有桶访问不了
   - catch 分支: 上传失败把本地 blob 临时路径直接入表单 → 保存后 blob 进库 (ZHSM888 现状)
   - 且 avatar.js 小程序端 `#ifdef MP-WEIXIN` 直接原样返回 cloud://, 但当前是 CloudBase 独立环境
     (cloud1- 前缀) 非微信原生云环境 → 小程序 image 原生渲染 cloud:// 不生效 → 后台用户管理头像全空

### 修复
- **user.vue**: 上传成功三端统一存 cloud:// fileID (不再去前缀), 失败不存本地路径提示重试;
  profileForm 拆 avatar(显示URL) + avatarRaw(入库fileID); 保存用 avatarRaw; 保存后顶部头像即时刷新
- **avatar.js**: 去掉 MP-WEIXIN 原样返回逻辑, 三端统一走云函数 fileUrl 转签名 URL
  (tcb.qcloud.la 已在 downloadFile 白名单)
- **数据库修复**: uid=2 avatar 从 blob 改为 `cloud://cloud1-.../avatars/u2_1787032000543.png`
  (云存储最新文件), 线上 fileUrl 转换 + 签名 URL 均验证 200
- 验证: 线上 app.fileUrl 转换 uid2 头像 → 签名 URL curl 200

## v1.11.158: 小程序接管报"未配置第三方平台参数"排查 + 域名修正

### 问题
- 后台「小程序接管」填 AppID → 报 `未配置开放平台第三方平台参数（WXMP_COMPONENT_APPID/SECRET）`
- **根因**: config.local.js 4 项第三方平台参数全为空 (WXMP_COMPONENT_APPID/SECRET/TOKEN/AES_KEY),
  需先在微信开放平台创建第三方平台(企业认证)拿到参数, 填入 config.local.js 并**重新部署云函数**
- 用户已创建第三方平台, 参数待提供

### 顺手修复: 环境迁移残留的旧域名 (重要)
项目已从旧环境 `zhenhesheng-d6gkez7p221305432`(-1309518368) 迁到 `cloud1-d8gs2k9m311f7272f`(-1464523137), 但多处还残留旧域名:
1. `index.js:1510` download URL → 1464523137 ✅ 已改
2. `index.js:3419` 注释授权事件接收 URL → 1464523137 ✅ 已改
3. `index.js:3535` wxmpGetAuthUrl redirect 默认值 → 1464523137 ✅ 已改
4. `cloudbaserc.json` envId/staticDomain/storageDomain 全旧 → 改为 cloud1 环境 + 636c 存储域名 ✅ 已改
5. `docs/miniprogram-onboarding.md` 授权事件接收 URL → 1464523137 ✅ 已改
- ⚠️ 用户如果在开放平台已按旧文档配过授权事件 URL, verify_ticket 会推到旧环境收不到, 需改配新 URL
- Git: 93ceb57 v1.11.158 已推送 GitHub

## v1.11.159: 服务号消息推送全链路 (小程序消息→同主体服务号)

### 用户需求
1. 开放平台第三方平台新建教程（用户已注册开放平台但未创建第三方平台）
2. 小程序消息通过同一主体服务号推送给用户（服务号已认证, 订阅通知模板未申请, 场景=全部）

### 第三方平台创建要点 (给用户的教程)
- ⚠️ 纠正 (08-18 16:35): 官方文档明确 **定制化型无 AppSecret/无 Token/不可调接口**, 只用于"标记服务商开发关系";
  **小程序接管必须选「平台型服务商」**(有 AppID+Secret 可调接口代上传/提审/发布)
- 权限集勾 67/88/99/93/84; 授权事件接收URL: https://cloud1-d8gs2k9m311f7272f-1464523137.ap-shanghai.app.tcloudbase.com/dy-api?action=wxmp.authCallback
- 授权发起页域名: cloud1-d8gs2k9m311f7272f-1464523137.tcloudbaseapp.com
- ⚠️ 用户开放平台账号主体=**个人**, 显示"定制化上限0个"(个人无法创建第三方平台)

### 开放平台主体规则 (08-18 16:37 确认, 重要)
- **开放平台账号认证主体不支持更换**; 个人→企业属主体更换(A转B), 非工商变更, 不可修正
- 可修正的特殊情况仅限: 工商改名/多证合一/错漏字/个体户升级企业 (需工商变更证明)
- 个人主体无"开发者资质认证"入口 → 无法创建第三方平台
- ✅ 正确路径: **用真和盛文化营业执照+新邮箱重新注册企业账号** → 主体验证(对公打款或微信认证300) →
  开发者资质认证(300, 一次性) → 创建平台型服务商
- 消息与事件接收URL 可暂不填; 用户提供 4 项参数(WXMP_COMPONENT_APPID/SECRET/TOKEN/AES_KEY)后配置部署

### 服务号推送代码改造 (index.js)
- **sendGzhMsg 字段配置化**: GZH_TEMPLATE_FIELDS (默认 thing1,thing2), 与申请模板字段一致即可, 无需改代码
- **sendGzhMsgAll 群发**: 遍历 gzh_openid 非空用户逐个发 (课程/活动广播)
- **gzhSubscribeUrl 新接口**: 生成订阅授权链接 mp.weixin.qq.com/mp/subscribemsg?action=get_confirm (一次性订阅通知授权)
- 路由注册: user.gzhSubscribeUrl (⚠️ 注意别重复注册, 曾造成 bindGzh 双条目)
- 推送场景接入: 3处支付成功(wxpayCallback/orderPayBalance/v3回调) + 课程发布群发 + 盘道场次创建群发 (发货已有)
- gzh-bind.html: 绑定成功后新增「开启微信通知提醒」按钮 + act=sub 回调展示
- config.local.js 加 GZH_TEMPLATE_FIELDS; 文档补「七、服务号消息推送」章节

## v1.11.160: 我的页面昵称显示旧名修复 (ZHSM001 昊辰→真和盛)

### 现象
- ZHSM001 (uid=1) 我的页面显示"昊辰"(旧名), 主面/后台显示"真和盛"; 用户要求查其他用户

### 根因 (代码缺陷, 非数据库问题)
- 数据库 uid=1 nickname 已是'真和盛' ✅; login 接口也返回最新名
- **user.vue onShow 刷新 userInfo 时漏写 nickname** (只刷 avatar/dao_code/invite_code/balance)
  → 后台改昵称后, 已登录 App 缓存 userInfo.nickname 不更新 → 个人中心永远显示旧缓存名
- 影响: 所有"被后台改过昵称且未重新登录"的用户; 前端全站显示统一走 userStore.userInfo

### 修复
- `src/pages/user/user.vue` onShow setUserInfo 补 nickname + bio (以 user.profile 服务端为准)
- userProfile 接口本来就返回 nickname, 无需改云函数; 部署 H5 后用户下次进我的页面自动纠正
- Git: 505bc1b v1.11.160 已推送

## v1.11.161: 后台订单分析产品统计 + 售后反馈分组 (1506dd6)

### 需求 1: 产品销售统计
- 云函数 adminOrderAnalysis: 新增 productMap 按 items[].name 聚合 (排除已退款), 返回 products 数组 (销售额降序)
- 前端 dashboard.vue: 订单分析新增「产品销售统计」表格, 表头销售数量/销售额可点击切换升降序 (toggleProductSort, 默认销售额降序)
- items 结构: {id, name, price, qty}; 排除充值订单不显示问题——recharge 也有 items 会进统计(元宝充值 xxx 元宝)

### 需求 2: 售后反馈同订单分组
- 前端 computed aftersaleRows: 按 order_no 分组, 组内 id 升序(最早一条=主条目), 组间按组内最新 id 降序
- 主条目显示用户/订单/商品/金额 + 多条时"共 N 条反馈"徽标; 子条目缩进显示"↳ 后续反馈 #N" + 订单号
- 子条目样式: .tr-sub 浅色背景, .as-sub-tag 橙色缩进标签

### 部署
- 云函数+H5 已部署, 线上 orderAnalysis 已验证返回 products 字段; 小程序产物已构建待用户上传
- GitHub push 网络失败(HTTP2 framing), 本地 commit 已完成, 待重推

### 其他: 小程序审核"订单中心 path"
- 答复用户: `pages-sub/order/list` (我的订单页在分包 pages-sub 下, 编译产物 app.json 已核实)

### 数据库检查结果 (users 全表 12 人)
- ⚠️ uid=120395 ZHSM555 staff nickname='昊辰' — 真实员工账号(08-07建, 有头像/余额9801/消费447.81),
  是否与 uid=1 改名前同名需用户确认是否改名, 未擅自修改
- 其余 uid 2/3/4/5/120396~120402 昵称各自独立, 无旧名残留 (缓存缺陷已统一修复)
- 教训: 查用户显示问题先分「数据库值」vs「客户端缓存」; user.profile 返回字段要全量同步缓存

## v1.11.162: 首页管理改名 + 订单分析时间筛选 + 最近订单10笔 (8b427ec)

- 首页管理「动态tab」标签改名「用户动态」(仅文案, 字段仍 show_follow)
- 订单分析: 云函数 adminOrderAnalysis(data) 新增 data.range 参数 (week=7/month=30/quarter=90/year=365 天),
  按 created_at 解析过滤 ("2026/8/18" 斜杠格式 Date.parse 直接可解, 横杠格式 replace(/-/g,'/'));
  前端 module-head 加 filter-pills (全部/近一周/近一月/近一季度/近一年), 切换即重载
  → 环形图(单数/金额)/产品销售统计/用户排名全按所选时间范围过滤
- 数据概览: adminRecentOrders limit 5→10; 标题「最近订单（N 笔）」显示实际数量 (.panel-count 样式)
- _admin API 工厂本身透传 data, 前端直接 adminOrderAnalysis({range}) 即可, 无需改 api.js
- 上一轮 v1.11.161 的 GitHub 欠推已随本轮一起推上 (505bc1b..8b427ec)


### 关键约束 (微信规则)
- 服务号「订阅通知」= 一次性授权, 1次授权=1条消息; 无感多次推送需老号「模板消息」权限(2020前注册), 另行改造
- 订阅通知 + 网页授权(snsapi_base) 都要求服务号已认证 ✅ (用户已确认认证)
- 网页授权域名需在服务号后台配置 tcloudbaseapp.com 域名

### 部署
- H5 构建 + gzh-bind.html 复制部署静态托管
- 云函数 dy-api 重新部署 (新增路由铁律)
- Git: 9b9e11c v1.11.159 已推送
