# 2026-08-04 Daily Log

## 依赖安装与 UI 组件库集成

- 执行 `npm install` 成功，安装 800 个包（registry 为 npmmirror）
- UI 组件库: **uview-plus 3.8.86**（uni-app Vue3 全平台组件库）
- 新增依赖: uview-plus ^3.3.0, dayjs ^1.11.10

## 项目结构重构: 根目录 → src/ 目录

uni-app Vite 构建要求源码位于 `src/` 目录（报错 `ENOENT src/manifest.json`），已迁移：

- 迁入 `src/`: App.vue, main.js, manifest.json, pages.json, uni.scss, pages/, api/, store/, utils/, static/
- 根目录保留: index.html, vite.config.js, package.json, cloudbaserc.json, cloudfunctions/, scripts/, config/
- vite.config.js: alias `@` → `./src/`
- index.html: 脚本路径 `/main.js` → `/src/main.js`

## 配置变更

- pages.json easycom: 新增 `"^u-(.*)": "uview-plus/components/u-$1/u-$1.vue"`
- main.js: 引入并注册 uviewPlus
- uni.scss: 引入 `uview-plus/index.scss`
- api/cloudbase.js: `require()` 改为动态 `import()`（Vite 兼容），initCloudBase 改 async
- App.vue: onLaunch 内 await initCloudBase
- 页面改用 uview-plus 组件: u-grid/u-grid-item、u-cell-group/u-cell、u-avatar、u-button、u-icon
- 创建 `.env`（从 .env.example 复制）

## 构建验证

- `uni build` (H5): ✅ 通过，产物 dist/build/h5/
- `uni build -p mp-weixin` (微信小程序): ✅ 通过，产物 dist/build/mp-weixin/
- dev server: ✅ http://localhost:8080/ 返回 200
- 仅有 Sass @import deprecation 警告（uview-plus 内部，不影响功能）

## 多端构建命令

- H5: `node node_modules/.bin/uni build`
- 微信小程序: `node node_modules/.bin/uni build -p mp-weixin`（导入微信开发者工具）
- dev: `node node_modules/.bin/uni`（http://localhost:8080）

# ============ 道源易学 App 开发 (08-04 晚) ============

## 产品决策 (用户确认)

- 需求来源: Coze 输出的「道源易学」prompt（周易传统文化平台）
- 技术方案: uni-app 三端 + CloudBase 云函数/文档数据库/云存储（弃用原 Expo+Express 方案: 小程序端支持 + 零运维）
- 支付: 先模拟支付，后期接微信支付（换云函数即可）
- 直播: 列表 + 预约 + 跳转第三方链接
- 后台: 独立 H5 管理端（第二期）
- 节奏: 核心闭环先行

## 设计系统

- 新中式禅意: 古铜金 #854D0E / 深檀褐 #451A03 / 古书纸 #FFFCF5 / 米白 #F7F0E3 / 淡金 #C9A96A / 朱砂 #A32D2D
- uni.scss 设计 token ($dy-* 变量) + App.vue 全局样式
- tabbar 图标: scripts/generate-tabbar.py（山/铜钱/书卷/人形 4 图标 × 灰/金两态）
- 占位图: scripts/generate-placeholder.py（太极/六爻符号，12 商品 + 6 课程 + 3 直播封面）

## 页面清单 (13 个)

- Tab: pages/index(推荐动态/关注/直播), pages/shop(左右分栏+搜索), pages/course(分类+列表), pages/user(资产+订单入口)
- 子页: product/detail(轮播+属性+加购), cart(选择/数量/全选), checkout(地址/优惠券/余额/模拟支付), order/list+detail(状态流转), login(手机号+邀请码+示例账号), course/detail+my, moment/publish

## 数据层

- src/api/mock.js: 12 集合种子数据（categories/products/courses/moments/liveStreams/coupons/demoUsers）
- src/api/api.js: 统一 API 层, `__USE_MOCK__ = true` 先走 mock, 切换 CloudBase 云函数零改动
- src/utils/cart.js: 购物车本地 storage 状态管理
- 云函数 API 网关: 待接入（Task 19 进行中）

## 构建验证

- H5 `uni build`: ✅ DONE
- 微信小程序 `uni build -p mp-weixin`: ✅ DONE
- dev server: ✅ http://localhost:8080
- 修复的坑: uni-app 生命周期钩子(onShow/onLoad)必须从 @dcloudio/uni-app 导入, ref/computed 从 vue 导入；import 名冲突；src/ 目录要求

# ============ CloudBase 云端接入完成 (08-04 晚) ============

## 云端资源已创建

- 12 个 NoSQL 集合: users/products/categories/courses/course_categories/orders/moments/live_streams/favorites/footprints/coupons/transactions
- 种子数据 41 条: 7 分类 + 12 商品 + 5 课程分类 + 6 课程 + 4 动态 + 3 直播 + 2 优惠券 + 3 用户
- 云函数: **dy-api** (Nodejs16.13, index.main, 聚合路由: categories/products/courseCategories/courses/moments/live/coupons/user/order 共 17 个 action)
- HTTP 网关: `https://zhenhesheng-d6gkez7p221305432-1309518368.ap-shanghai.app.tcloudbase.com/dy-api` (auth=false, 匿名可访问)

## 前端数据源架构

- src/api/api.js: `__USE_MOCK__ = false`，H5/App 走 HTTP 网关 uni.request，微信小程序走 wx.cloud.callFunction（#ifdef 分流）
- 云函数兼容双触发: callFunction({action,data}) 与 HTTP event (body/queryStringParameters)
- 订单以 order_no 为业务主键（_id 为 CloudBase 自动生成）

## 排坑记录 (重要)

1. **mcporter 代理失效**: WorkBuddy sandbox 代理端口每会话动态变化, keep-alive 的 cloudbase-mcp 进程继承旧代理(53921 已死) → 报 ECONNREFUSED 127.0.0.1:53921。解法: kill 掉 cloudbase-mcp 进程 (pgrep -fl cloudbase-mcp), 让其重启继承新代理; 调用加 MCPORTER_CALL_TIMEOUT=150000
2. **认证过期**: auth status=REQUIRED → 重新设备码登录 (auth action=start_auth authMode=device)
3. **Web SDK 权限体系 (H5)**: 匿名登录默认未开启 → manageAppAuth patchLoginStrategy {"anonymous":true}；云函数默认规则拒绝 ANONYMOUS (`auth.loginType != 'ANONYMOUS'`) 且 MCP updateResourcePermission 更新函数权限有 bug(返回 success 但规则不变) → **弃用 Web SDK callFunction, 改用 HTTP 网关** (manageGateway createRoute auth=false) 绕开
4. **体验版套餐限制**: CreateAuthDomain(安全域名) 不支持
5. **uview-plus 内部组件用 up- 前缀**: pages.json easycom 需同时配 `^up-(.*)` 和 `^u-([^-].*)` 两条规则
6. **safe-delete 拦截**: sandbox 阻止批量删除, 清理 dist 用 `mv dist dist-old-$(date +%s)` 改名替代 rm -rf

## 管理命令

- 云函数代码: cloudfunctions/dy-api/ (index.js + package.json)
- 种子数据: scripts/seed-data.mjs (生成) + scripts/seed-upload.py (上传)
- 重新部署: mcporter call cloudbase.manageFunctions action=updateFunctionCode functionRootPath=cloudfunctions/dy-api

# ============ 第二阶段完成 (08-04 晚) ============

## 新增能力

1. **课程购买落库**: course.buy / course.mine / course.favorite / course.progress 云函数 + user_courses 集合；课程详情"立即购买→已购买→开始学习"，我的课程真实数据（进度/收藏/筛选）
2. **直播预约落库**: live.book / live.myBookings + live_bookings 集合；首页预约真实写入，登录用户状态回显"已预约"
3. **H5 后台管理端**: pages/admin/login + dashboard（数据概览/商品/课程/订单/用户/直播/动态 7 模块）；admin.* 系列云函数；权限校验 requireAdmin（uid/role 查 users 表）

## 云函数新增路由 (dy-api)

- 课程: course.buy / course.mine / course.favorite / course.progress
- 直播: live.book / live.myBookings
- 管理: admin.dashboard / admin.list(collection) / admin.products.create|update|delete / admin.courses.create|update / admin.orders.ship|refund / admin.users.update / admin.lives.create|update / admin.moments.audit
- admin.* 路由入口统一 requireAdmin 校验（403 拦截）

## 新集合

- user_courses: {uid, course_id, progress, status, favorited, bought_at}
- live_bookings: {uid, live_id, booked_at}

## 管理端入口

- 管理员账号 13800138001/123456 (role=admin) → 我的页「管理后台」入口（v-if computed visibleMenus 过滤，普通用户不可见）
- 管理 API 前端自动附加 uid/role（api.js _adminAuth 从 uni storage 读 userInfo）

## 排坑记录

1. **Vue3 v-for + v-if 同元素**: v-if 优先导致 m 未定义菜单不渲染 → 改用 computed visibleMenus 过滤
2. **管理端 403**: admin API 调用未带 uid/role → _adminAuth 自动附加
3. **预约回显 bug**: live.myBookings 返回直播文档字段是 id 非 live_id → 前端 map(b => b.id)
4. **mcporter 代理失效复发**: WorkBuddy sandbox 代理端口每会话变化, keep-alive 进程继承旧端口(54866 也失效过) → kill cloudbase-mcp 进程重启; 多个 mcporter daemon 冲突需全部 kill

## 端到端验证结果（浏览器实测）

- ✅ 课程购买: 立即购买 → 确认弹窗 → 已购买/开始学习
- ✅ 我的课程: 已购课程列表 + 进度 0% + 收藏星标
- ✅ 直播预约: 预约成功 → 刷新后"已预约"回显
- ✅ 管理后台: 概览统计 / 商品列表(朱砂/紫檀/香炉+上架状态) / 新增商品表单
- ✅ 订单管理: 创建订单 → admin.orders.ship → 状态"待收货"
- ✅ 权限控制: 非 admin 调 admin.dashboard → 403
- ✅ H5 + 微信小程序双端构建通过

# ============ H5 上线 + 管理界面升级 (08-04 晚) ============

## H5 正式上线

- **线上地址**: https://zhenhesheng-d6gkez7p221305432-1309518368.tcloudbaseapp.com/ (HTTP 200)
- 部署命令: mcporter call cloudbase.manageHosting action=upload localPath=dist/build/h5 cloudPath=/ (91 文件)
- 管理端路径: /#/pages/admin/login (线上可达)
- 静态托管: online, CdnDomain zhenhesheng-...tcloudbaseapp.com

## 管理界面升级 (侧边栏专业布局)

- 左侧深檀色侧边栏(260rpx): logo 印章 + 8 菜单(数据概览/商品/课程/订单/优惠券/用户/直播/动态) + 底部管理员信息
- 右侧: 顶栏(模块标题+返回前台+退出) + 内容区
- 概览页增强: 8 统计卡片 + 最近订单面板(admin.recentOrders 新接口)
- 新增优惠券管理模块: admin.coupons.create/update/delete + admin.list 支持 coupons 集合
- 商品编辑弹窗: 增加图片 URL 输入(逗号分隔)
- 订单管理: 增加"代收款"操作
- 窄屏 media query 适配

## 新增云函数路由

- admin.recentOrders (最近订单 limit)
- admin.coupons.create / update / delete

# ============ 管理员账号变更 + /admin 入口 (08-04 晚) ============

## 管理员账号（正式）

- **管理员: 18500353930 / 123456（昵称「昊辰」uid=4, role=admin, 邀请码 ZHSC8888）**
- 原示例管理员 13800138001 已降级为普通用户（role=user, 无后台权限）
- 其他演示账号不变: 13800138002/13800138003

## 后台入口 /admin

- 后台登录地址: **https://zhenhesheng-d6gkez7p221305432-1309518368.tcloudbaseapp.com/admin**
- 实现: dist/build/h5/admin/index.html（重定向页）→ 自动跳转 /#/pages/admin/login
- CloudBase 静态托管 AutoAddressing=Enabled, /admin 与 /admin/ 均返回 200
- 浏览器实测: /admin → 登录页 → 昊辰登录 → 管理后台 ✓

## 排坑

- **MCP writeNoSqlDatabaseContent action=update 是整篇覆盖**: 只传 {role} 会覆盖掉整个文档(uid/phone 丢失) → 必须传完整字段。修复了被误伤的 uid=1 文档
- 更新参数名是 update (非 documents)

# ============ 系统设置模块 (08-04 晚) ============

## 后台新增「系统设置」(⚙️)

- 新集合: **settings** (按 group 分组: sms/oss/mp/miniapp/live, 每组一个文档)
- 云函数: admin.settings.get (按 group 读) / admin.settings.save (upsert)
- 5 类配置:
  - 短信: provider/secret_id/secret_key/sign/template_id/region
  - OSS: provider/access_key/secret_key/bucket/region/domain
  - 公众号: app_id/app_secret/token/encoding_aes_key
  - 小程序: app_id/app_secret/env_id
  - 直播: provider/app_id/push_url/pull_url/license_url
- **敏感字段安全**: 读取不回显明文(标记"已配置"), 留空保存不修改原值, 支持清空
- 管理后台左侧栏新增「系统设置」菜单 (第 9 项)

## 验证

- ✅ 云函数 save/get (curl): 短信组/OSS 组存取正常
- ✅ 前端 API: adminSettingsGet 回填、adminSettingsSave 更新生效 (region → ap-beijing)
- ✅ UI: 5 tab 渲染、短信配置回填、敏感字段掩码
- ✅ 已部署线上 (successCount 91), /admin 200, 小程序构建通过
- 注意: agent-browser 的 eval native setter 无法触发 uni-app H5 input v-model, UI 输入需用真实键盘事件验证, 不影响真实用户使用

## 排坑

- admin.list 白名单漏了 coupons 集合 → allow 列表补充 (products/courses/orders/users/live_streams/moments/coupons/user_courses/live_bookings)

# ============ 安卓打包准备 (08-04 晚) ============

## 现状

- 本机无 Android SDK/JDK/Gradle + 未装 HBuilderX → **无法本地直接出 APK**
- APK 需 DCloud 云打包: 注册 dev.dcloud.net.cn → HBuilderX 导入项目 → 获取真实 appid → 云打包
- manifest.json appid 目前为占位 __UNI__UNICLOUD, 需用户在 DCloud 控制台创建应用获取真实 appid 后填入

## 已完成

- ✅ manifest 完善: name=道源易学, Android 包名 cn.codebuddy.zhs.workbuddy, 权限补充(相机/相册/振动等)
- ✅ 道源易学 Android 图标 (深檀底+金框+太极, 6 尺寸) 已生成
- ✅ **App 资源包构建成功**: `uni build -p app` → dist/build/app (HBuilderX 云打包输入)
- ✅ 下载页上线: https://zhenhesheng-d6gkez7p221305432-1309518368.tcloudbaseapp.com/download/ (APK 就绪后自动激活下载按钮, 约定路径 /apk/daoyuan-v1.0.0.apk)

## 排坑 (App 端构建)

# ============ 安卓 APK 打包完成 (08-05 凌晨) ============

## ✅ 最终交付

- **APK 已生成并上线**: https://zhenhesheng-d6gkez7p221305432-1309518368.tcloudbaseapp.com/apk/daoyuan-v1.0.0.apk (5.2MB, HTTP 200)
- **下载页**: https://zhenhesheng-d6gkez7p221305432-1309518368.tcloudbaseapp.com/download/ (按钮已激活 + 备用链接)
- APK 签名: CN=Daoyuan Yixue, keystore: mobile/android/app/daoyuan.keystore (密码 daoyuan2026, 30年)
- 方案: **Capacitor 套壳 H5** (非 DCloud 云打包), 完全本地构建, 无需 DCloud 账号

## 本机已装环境 (可复用)

- JDK: Temurin 17.0.20 + 21.0.12 @ ~/Library/Java/JavaVirtualMachines/ (Capacitor 8 需要 JDK 21!)
- Android SDK: ~/Library/Android/sdk (cmdline-tools latest + platform-tools + platforms;android-36 + build-tools 36.0.0/35.0.0)
- Gradle: wrapper 8.14.3 (自动下载, ~/.gradle/wrapper/dists)
- 安装源: https://api.adoptium.net/v3/binary/latest/17|21/ga/mac/aarch64/jdk/hotspot/normal/eclipse (比 brew 快)

## Capacitor 工程 (mobile/)

- mobile/capacitor.config.json: appId=cn.codebuddy.zhs.workbuddy, webDir=../dist/build/h5
- 构建命令: cd mobile/android && JAVA_HOME=jdk21 ./gradlew assembleRelease
- 产物: mobile/android/app/build/outputs/apk/release/app-release.apk
- **关键配置**: vite.config.js h5 base="./" (相对路径, WebView file:// 加载必需); manifest h5.publicPath="./"

## 排坑汇总 (App 打包)

1. **JDK 版本**: Capacitor 8 需 JDK 21 (源发行版21), JDK 17 报"无效的源发行版：21"
2. **后台任务回收**: WorkBuddy 后台 bash 任务在轮次结束时可能被回收 → 长下载用前台 timeout 600s (超时自动转后台)
3. **fish shell**: 无 yes/heredoc/export 语法 → 复杂命令用 bash -c 包裹
4. **下载页路径 bug**: /download/ 下相对路径要用 ../apk/ (不是 ./apk/)
5. **下载页缓存**: 更新后需 cache-bust (?v=2) 验证, HEAD 200 才激活按钮

## 下载页源文件

- scripts/download-page/index.html (持久位置, 重新上传即可)

1. **code-splitting 冲突**: cloudbase.js 动态 import(@cloudbase/js-sdk) → app 端 iife 构建失败 → 改条件编译静态 import (#ifdef H5 || APP-PLUS)
2. **uview-plus 变量未定义 (app 端)**: $u-border-color/$u-warning-light 等 → uni.scss 中 uview-plus 变量**必须定义在 @import 'uview-plus/index.scss' 之前**, 且补齐 primary/warning/success/error/info 的 light/dark/disabled 全套变体

# ============ 六项功能修改 (08-05 上午) ============

## 1. 管理后台布局修复 ✅

- **根因**: 4 个 u-popup 在 flex row 容器内 position:static 各占 ~200px, 挤占 main 宽度 (201px, 右侧空白)
- **修复**: `.admin-dash :deep(.u-popup) { position: fixed; width: 0; height: 0; }` 脱离占位
- 验证: main 201px → 1003px

## 2. 课程页左侧分类 ✅

- course.vue 重构为商城式: 左侧 cate-side(168rpx) + 右侧 course-side 分类区块联动 (scroll-into-view)
- 复用 shop.vue 的 cate-item/indicator 样式

## 3. 管理后台分类管理 ✅

- 云函数: admin.categories.list/create/update/delete (type=products|courses → categories|course_categories 集合)
- 商品/课程管理模块改为: 左侧分类栏(新建/编辑/显示开关/删除) + 右侧该分类下商品表
- 前端分类过滤: listCategories/listCourseCategories 只返回 is_show !== false
- 新建分类自动 id (max+1); 分类行 hover 显示操作 (编/显/删)
- 验证: 新建"分类8"入库 ✓

## 4. 商品属性编辑器 ✅

- 编辑弹窗新增动态属性编辑: attrsList [{key,value}] ↔ attrs 对象双向转换, 空行过滤
- 添加/删除属性行, 属性名+属性值输入

## 5. 全局配色柔和化 ✅

- 17 个文件批量替换 (python 正则大小写不敏感), 色板: 深檀#451a03→#4e3420, 古铜#854d0e→#8c5a2b, 朱砂#a32d2d→#b04a45, 米白#f7f0e3→#f8f3ea, 书纸#fffcf5→#fefbf6 等 15 色
- uni.scss 变量同步更新 → uview 组件自动跟随

## 6. 设置中心 ✅

- 我的页右上角齿轮 → /pages/setting/setting
- 8 项: 主题(跟随系统/浅色/深色, H5 应用 data-theme + App.vue 深色覆盖) / 语言(中英, utils/settings.js t()) / 设置密码(云函数 user.setPassword, 校验原密码) / 收货地址(本地 storage 管理) / 用户协议 / 隐私协议 / 关于我们 / 检查更新(云函数 app.checkUpdate, 返回下载页)
- 新页面: setting/setting + address + agreement + privacy + about (pages.json 已注册)
- utils/settings.js: getTheme/setTheme/applyTheme/getLang/setLang/t

## 部署

- 线上 H5 已更新 (100 文件, HTTP 200)
- APK 已重新打包 (cap sync + gradle assembleRelease 增量 13s) 并上传覆盖 /apk/daoyuan-v1.0.0.apk (5.5MB)

## 云打包后流程 (用户产出 APK)

- 用户把 APK 放到 dist/build/h5/apk/daoyuan-v1.0.0.apk → 上传托管 /apk/ → 下载页自动可用
